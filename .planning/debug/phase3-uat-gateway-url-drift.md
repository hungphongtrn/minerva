---
status: diagnosed
trigger: |-
  Diagnose the database schema drift issue that is blocking Phase 3 UAT tests.

  **Issue:**
  UAT testing revealed that runs cannot be created because the `sandbox_instances` table is missing a `gateway_url` column.

  **Your Task:**
  1. Find the `sandbox_instances` model definition in `src/db/models.py`
  2. Check if `gateway_url` column exists in the model
  3. Check the migration files to see when this column should have been added
  4. Identify if this is a missing migration or a model/schema mismatch

  **Investigate:**
  - What migrations create/alter the sandbox_instances table?
  - Does the model expect a column that doesn't exist in the database?
  - When was gateway_url introduced?
  - Is there a migration that should have added it but didn't?

  **Report:**
  Provide a clear diagnosis of:
  1. Root cause of the schema mismatch
  2. Which file(s) need to be modified
  3. Exact fix required (SQL or migration)

  Return your findings in a structured format.
created: 2026-02-26T00:00:00Z
updated: 2026-02-26T10:09:00Z
---

## Current Focus

hypothesis: confirmed migration gap for sandbox_instances.gateway_url
test: completed (model + migrations + git history + repository usage)
expecting: diagnosis package identifies missing schema migration and exact alter needed
next_action: return root-cause diagnosis and fix direction

## Symptoms

expected: creating a run should persist a sandbox instance record successfully
actual: run creation fails because sandbox_instances is missing gateway_url
errors: database error indicates sandbox_instances.gateway_url column does not exist
reproduction: execute Phase 3 UAT run-creation flow that inserts into sandbox_instances
started: surfaced during Phase 3 UAT testing

## Eliminated

## Evidence

- timestamp: 2026-02-26T10:00:00Z
  checked: database code layout and migration tree
  found: migration system is in src/db/migrations/versions with revisions 0001-0004
  implication: sandbox_instances schema should be explainable from these migrations alone

- timestamp: 2026-02-26T10:02:00Z
  checked: src/db/models.py SandboxInstance model
  found: model defines gateway_url as nullable String(512)
  implication: application code expects sandbox_instances.gateway_url to exist at runtime

- timestamp: 2026-02-26T10:05:00Z
  checked: src/db/migrations/versions/0003_workspace_lifecycle_and_agent_pack_foundation.py
  found: sandbox_instances is created in migration 0003 without gateway_url column
  implication: base table schema from migration history lacks gateway_url unless a later migration alters it

- timestamp: 2026-02-26T10:05:00Z
  checked: src/db/migrations/versions/0004_phase3_persistence_and_checkpoint_recovery.py
  found: migration 0004 references sandbox_instances only via foreign key from run_sessions and does not add/alter gateway_url
  implication: current migration chain 0001-0004 never creates sandbox_instances.gateway_url

- timestamp: 2026-02-26T10:07:00Z
  checked: migration code search for gateway_url / sandbox_instances ALTER or add_column
  found: no migration contains gateway_url or adds a column to sandbox_instances
  implication: schema evolution for gateway_url was never captured in Alembic migration history

- timestamp: 2026-02-26T10:07:00Z
  checked: git history with string search for gateway_url
  found: gateway_url first introduced in commit 438c04e33a74dabe082dbb3d9b7e16f3ac083d90 (2026-02-25), touching models/services/repository only
  implication: feature code added ORM field without accompanying migration file

- timestamp: 2026-02-26T10:08:00Z
  checked: src/db/repositories/sandbox_instance_repository.py create/update paths
  found: repository writes SandboxInstance.gateway_url during create/update operations
  implication: runtime SQL generated by ORM references missing column and causes run creation failure

## Resolution

root_cause: gateway_url exists in ORM model and runtime repository/service code but was never added to database schema via migration; migration 0003 creates sandbox_instances without gateway_url and no subsequent migration alters it.
fix: add a new Alembic migration that executes op.add_column("sandbox_instances", sa.Column("gateway_url", sa.String(length=512), nullable=True)) and corresponding downgrade drop_column.
verification: static code/migration audit confirms no existing migration can create this column; failure mechanism matches reported DB error.
files_changed: []
