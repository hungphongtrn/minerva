---
phase: 03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 03.4-01
files_modified:
  - src/tests/integration/test_gateway_audit_daytona.py
  - .planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
user_setup:
  - service: daytona
    why: "Provision a real sandbox for live gateway audit evidence"
    env_vars:
      - name: DAYTONA_API_KEY
        source: "Daytona Dashboard / CLI auth"
      - name: DAYTONA_PICOCLAW_SNAPSHOT_NAME
        source: "Operator-configured snapshot name (created via `minerva snapshot build`)"
      - name: LLM_API_KEY
        source: "Your LLM provider"

must_haves:
  truths:
    - "We have written evidence (report) that states whether Picoclaw meets the minimum bar for Minerva's full run experience in Daytona."
    - "The audit evidence is reproducible via an automated test command."
  artifacts:
    - path: src/tests/integration/test_gateway_audit_daytona.py
      provides: "Live Daytona audit execution (skips when not configured)"
    - path: .planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md
      provides: "Short audit checklist + verdict (PASS/FAIL)"
  key_links:
    - from: src/tests/integration/test_gateway_audit_daytona.py
      to: src/scripts/picoclaw_gateway_audit.py
      via: "Programmatic auditor invocation"
      pattern: "PicoclawGatewayAuditor"
---

<objective>
Run the Picoclaw gateway audit against a real Daytona sandbox (when configured) and produce a short written report/checklist that records evidence and the audit verdict.

Purpose: Make the migration decision evidence-based (no guesswork).
Output: Live audit test + audit report with verdict.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-CONTEXT.md
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-RESEARCH.md

@src/scripts/picoclaw_gateway_audit.py
@src/services/picoclaw_bridge_service.py
@src/services/run_service.py
@src/api/oss/routes/runs.py
</context>

<tasks>

<task type="auto">
  <name>task 1: add a Daytona live audit integration test (evidence collector)</name>
  <files>
src/tests/integration/test_gateway_audit_daytona.py
  </files>
  <action>
Create `src/tests/integration/test_gateway_audit_daytona.py` with a single live test that:
- Skips unless `DAYTONA_API_KEY` and `LLM_API_KEY` are set (use `pytest.skip` with clear message).
- Runs the auditor from `src/scripts/picoclaw_gateway_audit.py` in Daytona provisioning mode and captures the returned result dict.
- Asserts only on structural invariants (so it can support both PASS and FAIL outcomes):
  - result includes `health`, `execute`, `streaming_probe`, `continuity_wiring`
  - result includes a boolean `meets_minimum_bar` and `reasons` list

Do NOT assert that Picoclaw passes the bar; the report will record the verdict.
  </action>
  <verify>
uv run pytest src/tests/integration/test_gateway_audit_daytona.py -q
  </verify>
  <done>
Live Daytona audit test can run (or skip cleanly) and produces a structured result for the report.
  </done>
</task>

<task type="auto">
  <name>task 2: write the audit report (checklist + verdict + next action)</name>
  <files>
.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md
  </files>
  <action>
Create `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md` as a short checklist/report that includes:
- Scope (Daytona-only, evidence-only; not fixing Picoclaw)
- Minimum bar definition (streaming/events, tool calls, session continuity)
- Evidence commands (exact `uv run pytest ...` command and the audit CLI command)
- Findings table (bullet list is fine) that records what the audit observed
- Verdict: PASS or FAIL for "minimum bar"
- Recommendation: Keep Picoclaw OR Migrate to Zeroclaw
- Soak window definition (pick a concrete default per discretion): 24h, >=100 runs, error rate <1%, and zero auth failures

Populate the report with actual results after running the audit test at least once (do not paste full logs; summarize).
  </action>
  <verify>
uv run pytest src/tests/integration/test_gateway_audit_daytona.py -q
  </verify>
  <done>
Audit report exists and includes a clear PASS/FAIL verdict with reproducible evidence commands.
  </done>
</task>

</tasks>

<verification>
- If configured, `uv run pytest src/tests/integration/test_gateway_audit_daytona.py -q` runs without errors
- If not configured, it skips with clear remediation to set env vars
</verification>

<success_criteria>
- The repo contains both automated evidence (test) and human-readable evidence (report)
- The report makes the migration decision unambiguous
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-02-SUMMARY.md`
</output>
