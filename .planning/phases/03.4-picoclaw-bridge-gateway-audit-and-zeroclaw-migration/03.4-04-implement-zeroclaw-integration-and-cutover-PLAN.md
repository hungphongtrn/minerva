---
phase: 03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 03.4-03
files_modified:
  - src/services/zeroclaw_gateway_service.py
  - src/services/run_service.py
  - src/api/oss/routes/runs.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/services/sandbox_orchestrator_service.py
  - src/tests/services/test_zeroclaw_gateway_service.py
  - src/tests/smoke/test_gateway_cutover.py
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-02

must_haves:
  truths:
    - "Minerva uses Zeroclaw as the runtime gateway integration path for Daytona-backed runs (big-bang cutover)."
    - "Runs still produce a complete OSS SSE run experience (queued/provisioning/running/message/completed/failed)."
  artifacts:
    - path: src/services/zeroclaw_gateway_service.py
      provides: "Zeroclaw gateway client implementing health-first, fail-closed execution"
    - path: src/tests/smoke/test_gateway_cutover.py
      provides: "Automated cutover proof (no Picoclaw path used for runtime execution)"
  key_links:
    - from: src/services/run_service.py
      to: src/services/zeroclaw_gateway_service.py
      via: "execute_via_gateway() / execute() invocation"
      pattern: "ZeroclawGatewayService"
    - from: src/infrastructure/sandbox/providers/daytona.py
      to: src/integrations/zeroclaw/spec.py
      via: "Spec-driven runtime start command + config path"
      pattern: "load_zeroclaw_spec"
---

<objective>
Implement Zeroclaw as a full replacement gateway integration and cut over Minerva's runtime path to use it for Daytona runs.

Purpose: If Picoclaw does not meet the minimum bar, this is the replacement implementation (no rollback required).
Output: Zeroclaw gateway client, provider runtime wiring, RunService wiring, and a cutover smoke test.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-CONTEXT.md
@src/integrations/zeroclaw/spec.py
@src/services/picoclaw_bridge_service.py
@src/services/run_service.py
@src/infrastructure/sandbox/providers/daytona.py
@src/services/sandbox_orchestrator_service.py
@src/api/oss/routes/runs.py
</context>

<tasks>

<task type="auto">
  <name>task 1: implement Zeroclaw gateway client (spec-driven) + tests</name>
  <files>
src/services/zeroclaw_gateway_service.py
src/tests/services/test_zeroclaw_gateway_service.py
  </files>
  <action>
Create `src/services/zeroclaw_gateway_service.py` implementing the same core reliability patterns as `src/services/picoclaw_bridge_service.py`:
- health-first execution (poll health endpoint before execute)
- bearer auth support (if spec says bearer)
- typed, fail-closed errors with remediation
- bounded retries/timeouts (reuse configuration patterns from existing bridge service)

Use `load_zeroclaw_spec()` to determine:
- gateway port
- health path
- execute path
- streaming mode (for this phase, do not implement new client-facing event types; if streaming is required, translate it into the existing OSS SSE lifecycle/message events only)

Add `src/tests/services/test_zeroclaw_gateway_service.py` mirroring key tests from `test_picoclaw_bridge_service.py`:
- auth header attachment
- timeout -> typed error
- malformed response -> typed error
- fail-closed when missing required auth token (if spec requires bearer)
  </action>
  <verify>
uv run pytest src/tests/services/test_zeroclaw_gateway_service.py -q
  </verify>
  <done>
Zeroclaw gateway client exists, is spec-driven, and has deterministic unit coverage for health/auth/timeout/error typing.
  </done>
</task>

<task type="auto">
  <name>task 2: wire Daytona provider + orchestrator runtime bridge config for Zeroclaw</name>
  <files>
src/infrastructure/sandbox/providers/daytona.py
src/services/sandbox_orchestrator_service.py
  </files>
  <action>
Update Daytona runtime provisioning to start Zeroclaw instead of Picoclaw, driven entirely by `src/integrations/zeroclaw/spec.json`:
- Replace Picoclaw-specific config generation with a Zeroclaw config writer at `runtime.config_path` from spec.
- Replace the runtime start command used in `_start_bridge_runtime()` with `runtime.start_command` from spec.
- Preserve existing invariants:
  - config file must be written outside `/workspace/pack`
  - mount isolation (pack read-only, workspace writable) remains unchanged
  - listener readiness check still verifies `gateway.port` is listening on 127.0.0.1

Update `SandboxOrchestratorService._generate_runtime_bridge_config()` to generate the correct runtime config payload for the provider. Keep token generation behavior (unique per sandbox) and ensure the token is used by the Zeroclaw runtime config in the field indicated by the spec (e.g., bridge auth token).

Do not remove Picoclaw code yet; cleanup happens in Plan 05.
  </action>
  <verify>
uv run pytest src/tests/services/test_sandbox_provider_adapters.py -q
  </verify>
  <done>
Daytona provisioning uses Zeroclaw start/config contract from spec and preserves mount-isolation safety checks.
  </done>
</task>

<task type="auto">
  <name>task 3: cut over RunService + OSS /runs to use Zeroclaw gateway path</name>
  <files>
src/services/run_service.py
src/api/oss/routes/runs.py
src/tests/smoke/test_gateway_cutover.py
  </files>
  <action>
Big-bang cutover:
- Update `src/services/run_service.py` to invoke Zeroclaw gateway client for execution instead of `PicoclawBridgeService`.
- Preserve existing retry/reprovision semantics in `_execute_via_bridge()` (bounded recovery loop). Rename helper(s) if needed, but keep the same behavior.
- Ensure OSS `POST /runs` streaming behavior in `src/api/oss/routes/runs.py` remains intact and still yields lifecycle + message + terminal events.

Add `src/tests/smoke/test_gateway_cutover.py` asserting:
- `RunService` no longer instantiates/uses `PicoclawBridgeService` for execution.
- Zeroclaw service is the path used.

This smoke test is intentionally code-structural (no Daytona dependency) to prove the big-bang cutover is complete.
  </action>
  <verify>
uv run pytest src/tests/smoke/test_gateway_cutover.py -q
  </verify>
  <done>
All in-repo run execution paths use Zeroclaw integration; OSS /runs continues to stream lifecycle and final message events.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/tests/services/test_zeroclaw_gateway_service.py -q` passes
- `uv run pytest src/tests/smoke/test_gateway_cutover.py -q` passes
</verification>

<success_criteria>
- Codebase is cut over to Zeroclaw for Daytona runtime execution
- No migration requires rollback/fallback toggles
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-04-SUMMARY.md`
</output>
