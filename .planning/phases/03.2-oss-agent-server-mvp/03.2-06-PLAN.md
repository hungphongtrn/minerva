---
phase: 03.2-oss-agent-server-mvp
plan: 6
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - src/cli/commands/init.py
  - src/tests/cli/test_env_example_contract.py
autonomous: true
gap_closure: true
requirements:
  - AGNT-01
  - AGNT-02

must_haves:
  truths:
    - "`minerva init` generates/updates `.env.example` and prints a preflight checklist"
    - "Checked-in `.env.example` matches the CLI-generated contract"
  artifacts:
    - path: ".env.example"
      provides: "documented env var contract for OSS deployment"
      min_lines: 80
    - path: "src/cli/commands/init.py"
      provides: "init command + deterministic .env.example generator"
      contains: "def _generate_env_example"
    - path: "src/tests/cli/test_env_example_contract.py"
      provides: "keeps .env.example in sync with generator"
      min_lines: 40
  key_links:
    - from: "src/cli/commands/init.py"
      to: ".env.example"
      via: "_generate_env_example()"
      pattern: "write_text\(content\)"
---

<objective>
Close GAP-01 by making the checked-in `.env.example` match the CLI-generated environment contract and adding a guard test to keep them in sync.

Purpose: Prevent drift between the runtime generator and the repository artifact; OSS operators rely on `.env.example` as the source of truth.
Output: Updated `.env.example` + sync test.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-CONTEXT.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-VERIFICATION.md

@src/cli/commands/init.py
@.env.example
</context>

<tasks>

<task type="auto">
  <name>task 1: Regenerate and commit `.env.example` from the init generator</name>
  <files>
.env.example
src/cli/commands/init.py
  </files>
  <action>
- Update `src/cli/commands/init.py` so the `.env.example` template is produced by a single reusable function (e.g., `_render_env_example_template()` returning a string).
  - Keep the template deterministic (no timestamps, no environment-dependent content).
- Regenerate the checked-in `.env.example` so it matches the generator output exactly (including section ordering and comments).
- Keep `.env.example` ASCII-only unless the file already contains non-ASCII (today it is ASCII-only); do not add emoji or other Unicode to the checked-in file.
  </action>
  <verify>
uv run minerva init --force --verbose
  </verify>
  <done>
- `.env.example` is no longer the old 22-line stub; it contains Daytona/snapshot/LLM/observability sections matching the generator.
- Running `uv run minerva init --force` produces identical `.env.example` content on subsequent runs.
  </done>
</task>

<task type="auto">
  <name>task 2: Add a contract test to keep `.env.example` in sync</name>
  <files>src/tests/cli/test_env_example_contract.py</files>
  <action>
- Add `src/tests/cli/test_env_example_contract.py`:
  - Imports the env template renderer from `src/cli/commands/init.py`.
  - Reads the repository `.env.example` and asserts it matches the rendered template exactly (allow at most a trailing newline difference).
  - Fails with a clear diff hint telling maintainers to run `minerva init --force` when it diverges.
- Ensure the test runs fast and does not require Postgres/Daytona credentials (pure file/string comparison only).
  </action>
  <verify>
uv run pytest -q src/tests/cli/test_env_example_contract.py
  </verify>
  <done>
- A single failing test reliably detects drift between the generator and checked-in `.env.example`.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest -q src/tests/cli/test_env_example_contract.py` passes.
</verification>

<success_criteria>
- GAP-01 closed: `.env.example` is aligned with `minerva init` output and cannot silently drift.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-oss-agent-server-mvp/03.2-06-SUMMARY.md`
</output>
