---
status: testing
phase: 03.2-oss-agent-server-mvp
source:
  - 03.2-01-SUMMARY.md
  - 03.2-02-SUMMARY.md
  - 03.2-03-SUMMARY.md
  - 03.2-04-SUMMARY.md
  - 03.2-05-SUMMARY.md
  - 03.2-06-SUMMARY.md
  - 03.2-07-SUMMARY.md
  - 03.2-08-SUMMARY.md
started: 2026-03-02T09:14:36Z
updated: 2026-03-02T12:00:00Z
---

## Current Test

number: 10
name: Session Continuity & Provisioning
expected: |
  The first run for a new user triggers 'provisioning' events (cold start). Subsequent runs with the same `X-Session-ID` (and same user) skip provisioning and reuse the active sandbox.
awaiting: user response

## Tests

### 1. CLI Help & Subcommands
expected: Running `uv run minerva --help` displays the help message listing all implemented subcommands: init, migrate, serve, scaffold, register, snapshot_build.
result: pass

### 2. Environment Initialization
expected: Running `minerva init` creates a `.env` file (if not exists) based on the comprehensive `.env.example` template.
result: pass

### 3. Preflight Validation
expected: Running `minerva init` or `minerva serve` displays a preflight checklist showing the status of required (DB, Daytona) and optional (S3, LLM) dependencies with remediation guidance for failures.
result: pass

### 4. Idempotent Snapshot Build
expected: Running `minerva snapshot build` clones the Picoclaw repository and builds a Daytona snapshot with real-time log streaming. Subsequent runs should detect the existing snapshot and reuse it.
result: pass

### 5. Agent Pack Scaffolding
expected: Running `minerva scaffold --out ./test-agent` creates a directory with `AGENT.md`, `SOUL.md`, `IDENTITY.md`, and a `skills/` directory containing a `README.md`.
result: pass

### 6. Agent Pack Registration & Sync
expected: Running `minerva register ./test-agent --workspace-id <uuid>` validates the pack, registers it in the database, and syncs its contents to a versioned Daytona volume (`agent-pack-{id}-{digest}`).
result: pass

### 7. Operator Health & Readiness
expected: Accessing `/health` returns 200 with component statuses. Accessing `/ready` returns 200 only if all blocking dependencies (DB, Daytona auth, Snapshot) are healthy, otherwise 503 with remediation.
result: pass

### 8. Prometheus Metrics
expected: Accessing `/metrics` returns Prometheus-formatted metrics including FastAPI request counts and durations.
result: pass

### 9. SSE Run Execution
expected: `POST /runs` with `X-User-ID` and `X-Idempotency-Key` returns an SSE stream with typed events (lifecycle like 'provisioning', 'running' and domain like 'message', 'tool_call').
result: pass

### 10. Session Continuity & Provisioning
expected: The first run for a new user triggers 'provisioning' events (cold start). Subsequent runs with the same `X-Session-ID` (and same user) skip provisioning and reuse the active sandbox.
result: pass

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
