---
phase: 03.2-oss-agent-server-mvp
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/cli/__init__.py
  - src/cli/main.py
  - src/cli/commands/__init__.py
  - src/cli/commands/init.py
  - src/cli/commands/migrate.py
  - src/cli/commands/serve.py
  - src/cli/commands/scaffold.py
  - src/cli/commands/register.py
  - src/cli/commands/snapshot_build.py
  - src/services/preflight_service.py
  - .env.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05
  - AUTH-06
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - PERS-01
  - PERS-02
  - PERS-03
  - PERS-04
  - SECU-01
  - SECU-02
  - SECU-03
  - SECU-04
  - SECU-05

must_haves:
  truths:
    - "Developer can run `minerva --help` from the repo via uv"
    - "`minerva init` generates/updates `.env.example` and prints a preflight checklist"
    - "`minerva migrate` runs Alembic migrations (no auto-migrate on `serve`)"
    - "`minerva serve` refuses to start when DB schema is behind Alembic head"
  artifacts:
    - path: "src/cli/main.py"
      provides: "argparse CLI entrypoint for minerva"
      exports: ["main"]
    - path: "src/services/preflight_service.py"
      provides: "config preflight checks + checklist reporting"
      contains: "class PreflightService"
    - path: "pyproject.toml"
      provides: "uv-installable CLI entry point"
      contains: "[project.scripts]"
    - path: ".env.example"
      provides: "documented env var contract"
      min_lines: 30
  key_links:
    - from: "pyproject.toml"
      to: "src/cli/main.py"
      via: "[project.scripts]"
      pattern: "minerva\\s*=\\s*\"src\\.cli\\.main:main\""
    - from: "src/cli/commands/serve.py"
      to: "alembic.ini"
      via: "Alembic schema version check"
      pattern: "ScriptDirectory\\.from_config"
---

<objective>
Ship an installable `minerva` CLI (uv tool entrypoint) with preflight validation, plus explicit `migrate` vs `serve` semantics.

Purpose: Make Minerva usable as an OSS product without manual wiring or unsafe auto-migrations.
Output: Working CLI entrypoint + preflight service + updated `.env.example`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-CONTEXT.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-RESEARCH.md

@pyproject.toml
@src/main.py
@src/config/settings.py
@alembic.ini
</context>

<tasks>

<task type="auto">
  <name>task 1: Add uv-installable `minerva` CLI skeleton</name>
  <files>
pyproject.toml
src/cli/__init__.py
src/cli/main.py
src/cli/commands/__init__.py
src/cli/commands/init.py
src/cli/commands/migrate.py
src/cli/commands/serve.py
src/cli/commands/scaffold.py
src/cli/commands/register.py
src/cli/commands/snapshot_build.py
  </files>
  <action>
- Update `pyproject.toml` to support a real script entry point:
  - Add `[build-system]` using `hatchling`.
  - Add `[project.scripts]` with `minerva = "src.cli.main:main"`.
  - Bump `fastapi` minimum to `>=0.135.0` (SSE built-in requirement for this phase).
  - Add deps used later in Phase 03.2: `gunicorn`, `prometheus-client`, `prometheus-fastapi-instrumentator`, `structlog`.
- Create `src/cli/main.py` using `argparse` with exactly these subcommands (stubs are OK in this task):
  - `minerva init`
  - `minerva migrate`
  - `minerva serve`
  - `minerva scaffold`
  - `minerva register`
  - `minerva snapshot build`
- Each subcommand dispatches to `src/cli/commands/*.py` so later plans can implement behavior without modifying `src/cli/main.py`.
  </action>
  <verify>
uv run minerva --help
  </verify>
  <done>
- `uv run minerva --help` prints command list including `snapshot build`.
- No command imports crash at startup.
  </done>
</task>

<task type="auto">
  <name>task 2: Implement preflight checklist + init/migrate/serve behavior gates</name>
  <files>
src/services/preflight_service.py
src/cli/commands/init.py
src/cli/commands/migrate.py
src/cli/commands/serve.py
.env.example
  </files>
  <action>
- Create `src/services/preflight_service.py` implementing:
  - A deterministic checklist output (items with `code`, `service`, `severity`, `message`, `remediation`).
  - Required dependency checks (BLOCKING): Postgres connectivity using `DATABASE_URL`, Daytona connectivity using AsyncDaytona config env vars.
  - Optional dependency checks (WARNING): S3 checkpoint config (`CHECKPOINT_*`), LLM config (`LLM_API_KEY`, `LLM_API_BASE`, `LLM_MODEL`).
  - No network calls should happen at import time.
- Implement `minerva init`:
  - Regenerate `.env.example` grouped by service (db, daytona, picoclaw snapshot, s3 checkpoints, llm, observability).
  - Run preflight and print a human-readable checklist; exit non-zero if any BLOCKING failures.
- Implement `minerva migrate`:
  - Runs `alembic upgrade head` via Alembic Python API (not shelling out) using `alembic.ini`.
- Implement `minerva serve`:
  - Before starting uvicorn, assert DB schema is at Alembic head; if behind, print an actionable error: "run `minerva migrate`" and exit non-zero.
  - Do NOT auto-migrate.
- Update `.env.example` to include (documented) the Phase 03.2 env surface, including Daytona connection vars and placeholder Picoclaw snapshot vars (actual snapshot build logic lands in later plans).
  </action>
  <verify>
uv run pytest -q src/tests/services/test_checkpoint_storage_and_archive.py
  </verify>
  <done>
- `.env.example` includes Daytona + snapshot + LLM + observability sections.
- `minerva init` exits non-zero when Postgres/Daytona missing, but only warns for S3/LLM missing.
- `minerva migrate` and `minerva serve` implement the "never auto-migrate" contract.
  </done>
</task>

</tasks>

<verification>
- CLI entrypoint works: `uv run minerva --help`.
- Migration gate is enforceable without starting the server.
</verification>

<success_criteria>
- `minerva` CLI exists as a uv script entrypoint.
- Preflight checklist exists and is used by `minerva init`.
- `serve` refuses to start when DB schema behind head.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-oss-agent-server-mvp/03.2-01-SUMMARY.md`
</output>
