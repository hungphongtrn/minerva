---
phase: 03.2-oss-agent-server-mvp
plan: 8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/sandbox/providers/base.py
  - src/services/sandbox_orchestrator_service.py
  - src/services/daytona_pack_volume_service.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/tests/services/test_phase3_2_pack_volume_sync.py
  - src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
autonomous: true
gap_closure: true
requirements:
  - AGNT-02
  - WORK-02
  - WORK-05

must_haves:
  truths:
    - "Daytona sandboxes mount the registered pack Volume as static identity files"
    - "Per-sandbox Picoclaw `~/.picoclaw/config.json` is written via Daytona sandbox file API"
  artifacts:
    - path: "src/services/daytona_pack_volume_service.py"
      provides: "volume-per-digest naming + SDK-correct file upload"
      contains: "class DaytonaPackVolumeService"
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "snapshot-based provisioning + volume mount + real identity checks + config upload"
      contains: "class DaytonaSandboxProvider"
    - path: "src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py"
      provides: "tests for volume mount wiring and per-sandbox config writes"
      min_lines: 120
  key_links:
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/base.py"
      via: "SandboxConfig includes agent_pack_id + pack_digest"
      pattern: "agent_pack_id"
    - from: "src/infrastructure/sandbox/providers/daytona.py"
      to: "daytona.CreateSandboxFromSnapshotParams"
      via: "daytona.create(params)"
      pattern: "CreateSandboxFromSnapshotParams"
    - from: "src/infrastructure/sandbox/providers/daytona.py"
      to: "sandbox.fs"
      via: "get_file_info/create_folder/upload_file"
      pattern: "fs\.get_file_info|fs\.create_folder|fs\.upload_file"
---

<objective>
Close GAP-03 and GAP-04 by wiring real Daytona Volume mounts and sandbox file API operations in the Daytona provider.

Purpose: Enforce the Picoclaw runtime invariant that static identity files are mounted at sandbox creation, and ensure each sandbox gets its own runtime config outside the shared pack volume.
Output: Provider mounts digest-selected volume, verifies identity files via `sandbox.fs`, and writes `~/.picoclaw/config.json` via file API.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-CONTEXT.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-VERIFICATION.md

@src/services/sandbox_orchestrator_service.py
@src/services/daytona_pack_volume_service.py
@src/infrastructure/sandbox/providers/base.py
@src/infrastructure/sandbox/providers/daytona.py
</context>

<tasks>

<task type="auto">
  <name>task 1: Normalize volume naming and use SDK-correct pack upload primitives</name>
  <files>
src/services/daytona_pack_volume_service.py
src/tests/services/test_phase3_2_pack_volume_sync.py
  </files>
  <action>
- Update `src/services/daytona_pack_volume_service.py` to match Daytona Async SDK semantics:
  - Use official SDK types: `from daytona import CreateSandboxFromSnapshotParams, VolumeMount, FileUpload` (no custom VolumeMount class).
  - Use `await daytona.create(CreateSandboxFromSnapshotParams(...))` instead of `daytona.create(**kwargs)`.
  - Use `await sandbox.fs.upload_files([FileUpload(...), ...])` (per AsyncFileSystem docs).
- Update the deterministic volume naming contract to exactly: `agent-pack-{agent_pack_id}-{source_digest}` (no truncation).
  - This MUST match what the Daytona provider computes when mounting.
- Update `src/tests/services/test_phase3_2_pack_volume_sync.py` expectations accordingly:
  - Volume name assertions expect full UUID + full digest.
  - Upload assertions expect `upload_files()` called with a list of `FileUpload` objects whose `destination` paths are under `/workspace/pack/...`.
  </action>
  <verify>
uv run pytest -q src/tests/services/test_phase3_2_pack_volume_sync.py
  </verify>
  <done>
- Pack sync service uses SDK-correct `CreateSandboxFromSnapshotParams` + `FileUpload` list upload.
- Volume naming contract matches the required `agent-pack-{pack_id}-{digest}`.
  </done>
</task>

<task type="auto">
  <name>task 2: Pass agent_pack_id into SandboxConfig and mount digest volume on Daytona provisioning</name>
  <files>
src/infrastructure/sandbox/providers/base.py
src/services/sandbox_orchestrator_service.py
src/infrastructure/sandbox/providers/daytona.py
  </files>
  <action>
- Extend `SandboxConfig` in `src/infrastructure/sandbox/providers/base.py` with an optional `agent_pack_id: UUID | None`.
- Update `src/services/sandbox_orchestrator_service.py` to populate `agent_pack_id` when provisioning (it already has `agent_pack_id` in scope).
- Update Daytona provisioning in `src/infrastructure/sandbox/providers/daytona.py`:
  - Create sandboxes from the configured snapshot using `CreateSandboxFromSnapshotParams(snapshot=DAYTONA_PICOCLAW_SNAPSHOT_NAME, ...)`.
  - When `agent_pack_id` and `pack_digest` are present, compute volume name: `agent-pack-{agent_pack_id}-{pack_digest}` and mount it at `/workspace/pack` via `VolumeMount`.
  - Fail closed if pack binding is required but either `agent_pack_id` or `pack_digest` is missing.
  </action>
  <verify>
uv run pytest -q src/tests/services/test_sandbox_provider_adapters.py
  </verify>
  <done>
- Daytona provider provisioning uses snapshot-based create params and includes a `/workspace/pack` volume mount derived from `agent_pack_id + pack_digest`.
  </done>
</task>

<task type="auto">
  <name>task 3: Replace simulated identity verification and write per-sandbox config.json via sandbox file API</name>
  <files>
src/infrastructure/sandbox/providers/daytona.py
src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
  </files>
  <action>
- Replace simulated identity verification in `verify_identity_files()` with real `sandbox.fs.get_file_info()` checks:
  - Verify files exist under `/workspace/pack/`: `AGENT.md`, `SOUL.md`, `IDENTITY.md`.
  - Verify directory exists: `/workspace/pack/skills` and `is_dir == True`.
  - Implement polling until `timeout` is reached (small sleep between attempts) to handle mount readiness.
- In `provision_sandbox()`, write per-sandbox Picoclaw config using Daytona Async file API:
  - Compute config destination outside the shared pack volume: `~/.picoclaw/config.json` (use an absolute path like `/home/daytona/.picoclaw/config.json`).
  - Call `await sandbox.fs.create_folder("/home/daytona/.picoclaw", "700")` (idempotent: ignore "already exists" errors).
  - Call `await sandbox.fs.upload_file(config_bytes, "/home/daytona/.picoclaw/config.json")`.
  - Assert in code (fail-fast) that the config path does not start with `/workspace/pack`.
- Add `src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py` with SDK-mocked tests:
  - Provider passes a `CreateSandboxFromSnapshotParams` with a `volumes` entry mounting `/workspace/pack`.
  - `verify_identity_files()` calls `sandbox.fs.get_file_info` for required paths.
  - `provision_sandbox()` calls `sandbox.fs.create_folder` and `sandbox.fs.upload_file` for `/home/daytona/.picoclaw/config.json`.
  - Ensure test asserts config path is outside `/workspace/pack`.
  </action>
  <verify>
uv run pytest -q src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
  </verify>
  <done>
- Provider no longer reports identity-ready without real file checks.
- Per-sandbox config is materialized via file API outside the shared pack volume.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest -q src/tests/services/test_phase3_2_pack_volume_sync.py` passes.
- `uv run pytest -q src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py` passes.
</verification>

<success_criteria>
- GAP-03 closed: Daytona provider mounts digest-selected pack volume and verifies identity files via file API.
- GAP-04 closed: Daytona provider writes `~/.picoclaw/config.json` via sandbox file API outside the shared volume.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-oss-agent-server-mvp/03.2-08-SUMMARY.md`
</output>
