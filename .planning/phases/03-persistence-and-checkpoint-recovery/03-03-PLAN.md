---
phase: 03-persistence-and-checkpoint-recovery
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/db/repositories/run_session_repository.py
  - src/db/repositories/runtime_event_repository.py
  - src/db/repositories/workspace_checkpoint_repository.py
  - src/db/repositories/audit_event_repository.py
  - src/db/repositories/__init__.py
  - src/services/runtime_persistence_service.py
  - src/services/workspace_checkpoint_service.py
  - src/services/run_service.py
  - src/tests/integration/test_phase3_persistence_writes.py
autonomous: true
must_haves:
  truths:
    - "Non-guest run execution writes run/session metadata and runtime events to Postgres."
    - "Milestone checkpoint writes persist archive metadata and auto-advance the active checkpoint pointer to newest successful revision."
    - "Checkpoint and runtime persistence operations append audit events without mutating existing audit rows."
  artifacts:
    - path: "src/services/runtime_persistence_service.py"
      provides: "Run/session/event persistence workflow for non-guest runs"
      contains: "class RuntimePersistenceService"
    - path: "src/services/workspace_checkpoint_service.py"
      provides: "Checkpoint write flow with manifest metadata and active pointer auto-advance"
      contains: "class WorkspaceCheckpointService"
    - path: "src/services/run_service.py"
      provides: "Run lifecycle integration with persistence/audit/checkpoint hooks"
      contains: "execute_with_routing"
    - path: "src/tests/integration/test_phase3_persistence_writes.py"
      provides: "Integration proof for non-guest persistence and guest non-persistence behavior"
      contains: "test_"
  key_links:
    - from: "src/services/run_service.py"
      to: "src/services/runtime_persistence_service.py"
      via: "Run lifecycle emits queued/running/completed-or-failed events"
      pattern: "runtime_event"
    - from: "src/services/workspace_checkpoint_service.py"
      to: "src/infrastructure/checkpoints/s3_checkpoint_store.py"
      via: "Checkpoint milestone writes archive and manifest objects to S3"
      pattern: "put_"
    - from: "src/services/workspace_checkpoint_service.py"
      to: "src/db/repositories/workspace_checkpoint_repository.py"
      via: "Completed checkpoint updates active pointer and checkpoint status"
      pattern: "active"
---

<objective>
Wire runtime persistence and checkpoint write paths into real run execution.

Purpose: This plan delivers PERS-01 and the write half of PERS-02/PERS-03 by making successful non-guest runs durable and checkpoint-capable.
Output: Repositories plus services that persist run/session/events, write checkpoint metadata, advance active pointer, and append audit records.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-CONTEXT.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-RESEARCH.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-13-SUMMARY.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-03-SUMMARY.md
@src/services/run_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement repositories for run sessions, runtime events, checkpoints, and audit events</name>
  <files>src/db/repositories/run_session_repository.py, src/db/repositories/runtime_event_repository.py, src/db/repositories/workspace_checkpoint_repository.py, src/db/repositories/audit_event_repository.py, src/db/repositories/__init__.py</files>
  <action>Add repository modules with explicit query methods for write and read patterns used by run timeline, checkpoint history, and active pointer resolution. Include append-only audit insert APIs (no update/delete paths exposed). Keep method naming consistent with existing repository style for low-friction service integration.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_persistence_writes.py -k "repository" -q`</verify>
  <done>Repository layer can create/read run events/checkpoints and append audit entries without mutating audit history.</done>
</task>

<task type="auto">
  <name>Task 2: Build runtime persistence and checkpoint write services</name>
  <files>src/services/runtime_persistence_service.py, src/services/workspace_checkpoint_service.py</files>
  <action>Create services that (a) persist run/session metadata and runtime events for non-guest executions, and (b) execute milestone checkpoint writes via archive + S3 store + DB registry updates. Ensure checkpoint completion auto-advances active pointer to newest successful revision, and that every checkpoint lifecycle transition appends an audit event. Keep guest behavior explicitly non-persistent.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_persistence_writes.py -k "checkpoint or audit or non_guest" -q`</verify>
  <done>Service layer performs durable writes for non-guest runs, milestone checkpoint persistence, pointer advancement, and append-only audit logging.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate services into run execution and add end-to-end persistence tests</name>
  <files>src/services/run_service.py, src/tests/integration/test_phase3_persistence_writes.py</files>
  <action>Wire `RunService` to emit lifecycle persistence events (queued/running/completed/failed), persist run/session metadata, and trigger checkpoint writes at configured milestones for non-guest workspaces. Add integration coverage proving: non-guest runs create durable DB records, guests skip persistence, checkpoint metadata rows are created, and audit event rows are append-only across operations.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_persistence_writes.py -q`</verify>
  <done>Executing a non-guest run now leaves queryable run/session/events/checkpoint/audit records in Postgres-backed persistence paths.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase3_persistence_writes.py -q`; confirm durable non-guest persistence, milestone checkpoint metadata writes, pointer advancement, and append-only audit inserts.
</verification>

<success_criteria>
PERS-01 is satisfied and checkpoint write plumbing for PERS-02/PERS-03 is active in runtime execution for non-guest workspaces.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-and-checkpoint-recovery/03-03-SUMMARY.md`
</output>
