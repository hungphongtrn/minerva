---
phase: 03.3-close-pack-mount-isolation-and-identity-collision-gaps
verified: 2026-03-02T18:42:56Z
status: passed
score: 14/14 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 11/14
  gaps_closed:
    - "Server refuses to start if MINERVA_WORKSPACE_ID is not set"
    - "Pack volume mounted read-only at /workspace/pack and never receives dynamic runtime data"
    - "Per-user sandbox keying via external_user_id"
  gaps_remaining: []
  regressions: []
---

# Phase 03.3: Close pack-mount isolation and identity-collision gaps Verification Report

**Phase Goal:** Fix two structural issues that break multi-tenant safety: identity collision between developer and end-user identities sharing the `users` table, and mount isolation where static pack files and dynamic runtime data share the same volume mount path.
**Verified:** 2026-03-02T18:42:56Z
**Status:** passed
**Re-verification:** Yes - after gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | ExternalIdentity model with composite PK (workspace_id, external_user_id) | ✓ VERIFIED | `src/db/models.py:147` defines `ExternalIdentity` with composite PK fields at `src/db/models.py:156` and `src/db/models.py:159`; migration creates table/PK at `src/db/migrations/versions/0007_external_identities_and_sandbox_user_key.py:30` and `src/db/migrations/versions/0007_external_identities_and_sandbox_user_key.py:42`. |
| 2 | End-users never create rows in developer `users` table | ✓ VERIFIED | Resolver uses `ExternalIdentity` only (`src/api/dependencies/external_identity.py:19`, `src/api/dependencies/external_identity.py:110`); no `User` model import/query in this dependency path. |
| 3 | End-user identity resolves to developer workspace via MINERVA_WORKSPACE_ID | ✓ VERIFIED | Resolver requires workspace setting at `src/api/dependencies/external_identity.py:70` and returns that workspace at `src/api/dependencies/external_identity.py:128`. |
| 4 | Server refuses to start if MINERVA_WORKSPACE_ID is not set | ✓ VERIFIED | `minerva serve` now calls workspace gate at `src/cli/commands/serve.py:70`; check fails closed in preflight (`src/services/preflight_service.py:301`-`src/services/preflight_service.py:309`); regression tests pass in `src/tests/cli/test_serve_preflight_workspace_gate.py`. |
| 5 | Guest requests get no external_identities row and no session continuity | ✓ VERIFIED | Guest short-circuit before DB ops in `src/api/dependencies/external_identity.py:91`; run-session continuity disabled for guests via run-id continuity key at `src/services/run_service.py:920`. |
| 6 | Pack volume mounted read-only at /workspace/pack, never receives dynamic data | ✓ VERIFIED | Daytona mount includes RO contract at `src/infrastructure/sandbox/providers/daytona.py:936`-`src/infrastructure/sandbox/providers/daytona.py:939`; config path is outside pack mount (`src/infrastructure/sandbox/providers/daytona.py:1042` and `src/infrastructure/sandbox/providers/local_compose.py:251`) with fail-fast guards (`src/infrastructure/sandbox/providers/daytona.py:1045`, `src/infrastructure/sandbox/providers/local_compose.py:347`). |
| 7 | Picoclaw workspace directory is /home/daytona/workspace (writable, isolated) | ✓ VERIFIED | Workspace constant defined at `src/infrastructure/sandbox/providers/base.py:21`; used in provider config generation (`src/infrastructure/sandbox/providers/daytona.py:710`, `src/infrastructure/sandbox/providers/local_compose.py:177`). |
| 8 | Identity files symlinked from pack volume into workspace | ✓ VERIFIED | Daytona symlink setup runs via `ln -sf` at `src/infrastructure/sandbox/providers/daytona.py:773` and `src/infrastructure/sandbox/providers/daytona.py:779`; local compose parity metadata set in `src/infrastructure/sandbox/providers/local_compose.py:134`-`src/infrastructure/sandbox/providers/local_compose.py:146`. |
| 9 | Per-user sandbox keying via external_user_id | ✓ VERIFIED | Run path plumbs external user into routing (`src/services/run_service.py:398`, `src/services/run_service.py:431`), lifecycle forwards it (`src/services/workspace_lifecycle_service.py:216`, `src/services/workspace_lifecycle_service.py:377`), orchestrator filters and provisions with it (`src/services/sandbox_orchestrator_service.py:227`, `src/services/sandbox_orchestrator_service.py:713`, `src/services/sandbox_orchestrator_service.py:722`), repository persists/filters it (`src/db/repositories/sandbox_instance_repository.py:42`, `src/db/repositories/sandbox_instance_repository.py:145`, `src/db/repositories/sandbox_instance_repository.py:167`). |
| 10 | Bridge sends raw external_user_id as sender_id (not hardcoded `minerva`) | ✓ VERIFIED | Bridge payload uses `sender_id` parameter with fallback only (`src/services/picoclaw_bridge_service.py:323`); run service sets sender from principal (`src/services/run_service.py:777`-`src/services/run_service.py:781`). |
| 11 | Bridge forwards session_id alongside sender_id | ✓ VERIFIED | Bridge payload includes `session_id` at `src/services/picoclaw_bridge_service.py:327`; run bridge execution forwards `session_id` at `src/services/run_service.py:1008`. |
| 12 | Guest requests use `guest` as sender_id | ✓ VERIFIED | Guest sender is explicitly set in run execution path (`src/services/run_service.py:778`-`src/services/run_service.py:780`). |
| 13 | OSS `/runs` route passes external_user_id and session_id to bridge | ✓ VERIFIED | Route passes `principal` + `session_id` into run service (`src/api/oss/routes/runs.py:110`, `src/api/oss/routes/runs.py:118`); run service forwards principal external user and session to bridge (`src/services/run_service.py:780`, `src/services/run_service.py:1007`, `src/services/run_service.py:1008`). |
| 14 | local_compose provider has Daytona-parity mount isolation pattern | ✓ VERIFIED | local compose uses shared constants (`src/infrastructure/sandbox/providers/local_compose.py:23`-`src/infrastructure/sandbox/providers/local_compose.py:27`), workspace path config (`src/infrastructure/sandbox/providers/local_compose.py:177`), symlink parity before READY (`src/infrastructure/sandbox/providers/local_compose.py:373`), and metadata parity (`src/infrastructure/sandbox/providers/local_compose.py:88`-`src/infrastructure/sandbox/providers/local_compose.py:96`). |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/api/dependencies/external_identity.py` | Workspace-scoped external identity resolution | ✓ VERIFIED | Uses `ExternalIdentity`, guest bypass, and MINERVA workspace resolution. |
| `src/services/preflight_service.py` | Workspace preflight checks + CLI-exposed method | ✓ VERIFIED | `_check_workspace_configured` implemented and public `check_workspace_configured()` exposed. |
| `src/cli/commands/serve.py` | Serve startup gate wires workspace check | ✓ VERIFIED | `handle()` calls `service.check_workspace_configured()` and returns `1` on failure. |
| `src/infrastructure/sandbox/providers/daytona.py` | Read-only pack mount + symlink/workspace isolation + per-user refs | ✓ VERIFIED | RO volume mount, symlink setup, workspace config path, external_user_id ref support all present. |
| `src/infrastructure/sandbox/providers/local_compose.py` | Isolation parity + read-only contract metadata + per-user refs | ✓ VERIFIED | Uses shared isolation constants, enforces path guard, exposes `pack_mount_read_only`, supports user refs. |
| `src/services/sandbox_orchestrator_service.py` | End-to-end per-user routing/provision wiring | ✓ VERIFIED | Filters candidates by `external_user_id`, persists key, passes into `SandboxConfig`. |
| `src/db/repositories/sandbox_instance_repository.py` | Persist/filter by `external_user_id` | ✓ VERIFIED | `create`, `list_by_workspace`, and `list_active_healthy_by_workspace` accept/filter key. |
| `src/services/run_service.py` | OSS principal workspace+external user routing and bridge forwarding | ✓ VERIFIED | Fail-closed workspace resolution for OSS principal and sender/session pass-through to bridge. |
| `src/tests/cli/test_serve_preflight_workspace_gate.py` | Regression coverage for serve workspace gate | ✓ VERIFIED | Added fail-closed tests for missing/invalid/no-pack workspace scenarios. |
| `src/tests/services/test_sandbox_instance_repository.py` | Repository coverage for external_user_id | ✓ VERIFIED | Tests assert persistence/filtering by external user key. |
| `src/tests/services/test_sandbox_routing_service.py` | Orchestrator coverage for external-user routing | ✓ VERIFIED | Tests route by `external_user_id` and assert provider config receives it. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `src/cli/commands/serve.py` | `src/services/preflight_service.py` | workspace preflight gate | ✓ WIRED | `check_workspace_configured()` is called in serve startup flow before server start. |
| `src/services/preflight_service.py` | `src/config/settings.py` | MINERVA_WORKSPACE_ID validation | ✓ WIRED | Check enforces env set, workspace existence, and active pack presence. |
| `src/services/sandbox_orchestrator_service.py` | `src/db/repositories/sandbox_instance_repository.py` | active sandbox lookup by external user | ✓ WIRED | `list_active_healthy_by_workspace(... external_user_id=...)` now used for routing candidates. |
| `src/services/sandbox_orchestrator_service.py` | `src/infrastructure/sandbox/providers/base.py` | `SandboxConfig.external_user_id` provisioning | ✓ WIRED | `SandboxConfig(... external_user_id=external_user_id)` passed to provider provision call. |
| `src/services/run_service.py` | `src/services/workspace_lifecycle_service.py` | OSS principal forwarding | ✓ WIRED | OSS path passes explicit workspace + external_user_id into lifecycle resolve_target. |
| `src/services/picoclaw_bridge_service.py` | Picoclaw payload | sender/session forwarding | ✓ WIRED | `_transform_to_picoclaw_request` includes `sender_id` and `session_id`; execute path passes both. |
| `src/infrastructure/sandbox/providers/local_compose.py` | `src/infrastructure/sandbox/providers/base.py` | shared mount-isolation constants | ✓ WIRED | Imports and uses `WORKSPACE_PATH`, `PACK_MOUNT_PATH`, `CONFIG_PATH`, `IDENTITY_*`. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| AUTH-03 | 03.3-01, 03.3-02, 03.3-03, 03.3-04, 03.3-06 | User can access only their own workspace resources | ✓ SATISFIED | End-user identity decoupled to `external_identities`, serve startup fails closed on bad workspace config, and sandbox routing now keys by `(workspace_id, external_user_id)` end-to-end. |
| AGNT-02 | 03.3-01, 03.3-03, 03.3-04 | User can register an agent pack folder and run it without manual infrastructure wiring | ✓ SATISFIED | Workspace preflight now enforced in `serve`, OSS run path resolves to configured developer workspace, and bridge session/sender wiring remains operational. |
| AGNT-03 | 03.3-02, 03.3-03, 03.3-05, 03.3-06 | User can run same pack in local compose and BYOC profiles | ✓ SATISFIED | Both providers implement isolation parity, pack read-only contract is present, and per-user routing key is wired through orchestrator/repository/provider flow. |

Requirement cross-reference check: all IDs declared in phase plan frontmatter (`AUTH-03`, `AGNT-02`, `AGNT-03`) are present in `REQUIREMENTS.md`; no additional Phase 03.3-scoped orphaned IDs were found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `src/services/run_service.py` | 293 | "placeholder" in execution-flow docstring | ⚠️ Warning | Documentation wording suggests temporary framing but does not block verified phase behaviors. |

### Human Verification Required

None. Automated/static checks and targeted tests were sufficient for this phase contract.

### Gaps Summary

All previously failed gaps are closed: workspace preflight is now wired into `minerva serve`, read-only pack mount contract is enforced/exposed across providers, and per-user sandbox routing is wired end-to-end using `external_user_id`.

Targeted verification tests executed and passing:
- `uv run pytest src/tests/cli/test_serve_preflight_workspace_gate.py src/tests/services/test_sandbox_instance_repository.py src/tests/services/test_sandbox_routing_service.py src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py::TestVolumeMountWiring::test_provision_mounts_volume_at_workspace_pack src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_local_compose src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_daytona -q`

---

_Verified: 2026-03-02T18:42:56Z_
_Verifier: OpenCode (gsd-verifier)_
