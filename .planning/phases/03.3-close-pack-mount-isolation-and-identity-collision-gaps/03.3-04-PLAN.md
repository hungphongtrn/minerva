---
phase: 03.3-close-pack-mount-isolation-and-identity-collision-gaps
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/serve.py
  - src/services/preflight_service.py
  - src/tests/cli/test_serve_preflight_workspace_gate.py
autonomous: true
requirements:
  - AUTH-03
  - AGNT-02

must_haves:
  truths:
    - "Server refuses to start if MINERVA_WORKSPACE_ID is not set"
    - "Server refuses to start if MINERVA_WORKSPACE_ID points to a workspace that does not exist"
    - "Server refuses to start if MINERVA_WORKSPACE_ID points to a workspace with no active agent packs"
  artifacts:
    - path: "src/cli/commands/serve.py"
      provides: "Serve startup gates include workspace-config validation"
      contains: "WORKSPACE_CONFIGURED"
    - path: "src/services/preflight_service.py"
      provides: "Workspace-config check exposed for CLI startup"
      contains: "check_workspace_configured"
    - path: "src/tests/cli/test_serve_preflight_workspace_gate.py"
      provides: "Regression tests for serve failing closed on missing/invalid workspace"
      contains: "test_serve_preflight"
  key_links:
    - from: "src/cli/commands/serve.py"
      to: "src/services/preflight_service.py"
      via: "workspace preflight gate"
      pattern: "check_workspace_configured\(\)"
---

<objective>
Wire workspace-config preflight into `minerva serve` startup so OSS deployments fail closed when MINERVA_WORKSPACE_ID is missing/invalid/no packs.

Purpose: Phase 03.3 requires that the agent server refuses to start when it cannot deterministically resolve end-user requests to the developer workspace and its registered agent pack(s).

Output: Updated serve startup gates + regression tests proving fail-closed behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-close-pack-mount-isolation-and-identity-collision-gaps/03.3-CONTEXT.md
@.planning/phases/03.3-close-pack-mount-isolation-and-identity-collision-gaps/03.3-VERIFICATION.md
@src/cli/commands/serve.py
@src/services/preflight_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for serve workspace preflight gate</name>
  <files>
    src/tests/cli/test_serve_preflight_workspace_gate.py
  </files>
  <action>
Create `src/tests/cli/test_serve_preflight_workspace_gate.py` to prove `minerva serve` fails closed when the workspace-config check fails.

Test cases (use monkeypatch/patching; do NOT start a real uvicorn server):
1. `test_serve_preflight_fails_when_workspace_check_fails`:
   - Patch `src.cli.commands.serve.PreflightService.check_database_schema_current` to return PASS.
   - Patch `src.cli.commands.serve.PreflightService.check_workspace_configured` to return FAIL.
   - Patch `src.cli.commands.serve.PreflightService._get_picoclaw_snapshot_name` to return None (skip snapshot).
   - Patch `src.cli.commands.serve.uvicorn.run` so it cannot start the server.
   - Assert `serve.handle(args)` returns 1 and `uvicorn.run` was not called.

2. `test_serve_preflight_calls_workspace_check_on_success_path`:
   - Patch schema check to PASS.
   - Patch workspace check to PASS.
   - Patch snapshot name to None.
   - Patch `uvicorn.run` and assert it is called once after preflight passes.

Construct `args` via `argparse.Namespace(host=..., port=..., reload=False, skip_preflight=False)`.
  </action>
  <verify>
    <automated>cd /Users/phong/Workspace/minerva && uv run pytest src/tests/cli/test_serve_preflight_workspace_gate.py -q</automated>
  </verify>
  <done>
    - Tests fail prior to wiring and pass once the workspace preflight gate is implemented
    - Tests assert serve fails closed and does not start uvicorn on workspace preflight failure
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire workspace-config gate into `minerva serve` startup</name>
  <files>
    src/cli/commands/serve.py
    src/services/preflight_service.py
  </files>
  <action>
1. In `src/services/preflight_service.py`, expose a non-private CLI-friendly method:
   - Add `def check_workspace_configured(self) -> PreflightCheck:` that returns `self._check_workspace_configured()`.
   - Do not change the semantics of `_check_workspace_configured()`; it already enforces:
     - MINERVA_WORKSPACE_ID present
     - workspace exists
     - at least one active agent pack

2. In `src/cli/commands/serve.py`, add a third preflight gate (BLOCKING) after schema check and before starting the server:
   - Call `service.check_workspace_configured()`.
   - If the returned check has `status == CheckStatus.FAIL`, print message + remediation and return 1.
   - If PASS, print the success message.
   - Leave `--skip-preflight` behavior unchanged (still bypasses all gates).
   - Update the docstring "Gates" list to include the new workspace gate.

3. Keep behavior fail-closed: missing/invalid/no-packs workspace blocks startup.
  </action>
  <verify>
    <automated>cd /Users/phong/Workspace/minerva && uv run pytest src/tests/cli/test_serve_preflight_workspace_gate.py -q</automated>
  </verify>
  <done>
    - `minerva serve` runs workspace-config preflight when `--skip-preflight` is not set
    - Startup returns exit code 1 when workspace-config check fails
    - Startup proceeds to uvicorn only when schema + (optional) snapshot + workspace gates pass
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest src/tests/cli/test_serve_preflight_workspace_gate.py -q` passes
2. `uv run python -c "from src.services.preflight_service import PreflightService; assert hasattr(PreflightService, 'check_workspace_configured')"` passes
</verification>

<success_criteria>
- `minerva serve` refuses to start when MINERVA_WORKSPACE_ID is missing/invalid/no packs
- Serve startup flow explicitly invokes the workspace-config preflight check
- Regression tests prevent the preflight wiring from regressing again
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-close-pack-mount-isolation-and-identity-collision-gaps/03.3-04-SUMMARY.md`
</output>
