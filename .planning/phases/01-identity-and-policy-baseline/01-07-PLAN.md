---
phase: 01-identity-and-policy-baseline
plan: 07
type: execute
wave: 2
depends_on: ["01-06"]
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/0002_api_key_user_binding.py
  - src/identity/key_material.py
  - src/identity/repository.py
  - src/identity/service.py
  - src/api/routes/api_keys.py
  - src/authorization/guards.py
  - src/api/routes/workspace_resources.py
  - src/tests/integration/conftest.py
  - src/tests/integration/test_membership_role_behavior.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Authorization principal role is derived from real membership records, not hardcoded owner stubs."
    - "Owner and member produce different API outcomes for the same workspace resource actions."
    - "Requests without a matching workspace membership are denied deterministically."
  artifacts:
    - path: "src/identity/key_material.py"
      provides: "Principal includes stable user identity required for membership resolution"
      contains: ["user_id", "workspace_id", "key_id"]
    - path: "src/authorization/guards.py"
      provides: "Membership-backed principal resolution dependency"
      contains: ["Membership", "Role", "resolve_auth_principal_dep"]
    - path: "src/api/routes/workspace_resources.py"
      provides: "Workspace route authorization based on resolved membership role"
      contains: ["_resolve_auth_principal_with_role", "authorize_action"]
    - path: "src/tests/integration/test_membership_role_behavior.py"
      provides: "Integration proof of owner/member divergence"
      contains: ["owner", "member", "403", "201"]
  key_links:
    - from: "src/identity/service.py"
      to: "src/authorization/guards.py"
      via: "validated API key principal carries user_id used for membership lookup"
      pattern: "principal\.user_id|resolve_auth_principal_dep"
    - from: "src/authorization/guards.py"
      to: "src/db/models.py"
      via: "membership query by user_id + workspace_id"
      pattern: "Membership"
    - from: "src/api/routes/workspace_resources.py"
      to: "src/authorization/policy.py"
      via: "role-aware authorize_action with membership-derived role"
      pattern: "authorize_action"
---

<objective>
Close Gap 2 by replacing owner-role stubs with real membership-backed role resolution.

Purpose: AUTH-05 requires observable owner/member behavior differences driven by actual membership data, not placeholders.
Output: API keys bound to users, membership-backed principal resolution in guards/routes, and integration tests proving owner/member divergence.
Scope rationale: This plan intentionally stays cohesive across identity, guard, and route files because key-to-user binding and membership-backed authorization are one requirement thread with a shared principal contract.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-identity-and-policy-baseline-VERIFICATION.md
@.planning/phases/01-identity-and-policy-baseline/01-02-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-03-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind API keys to users so principals carry membership identity</name>
  <files>src/db/models.py, src/db/migrations/versions/0002_api_key_user_binding.py, src/identity/key_material.py, src/identity/repository.py, src/identity/service.py, src/api/routes/api_keys.py</files>
  <action>Add persistent key-to-user binding (`api_keys.user_id` foreign key to `users.id`) and propagate it through key creation/validation so `Principal` includes `user_id`. Backfill existing keys safely in migration (for existing records, bind to workspace owner where deterministic). Keep auth behavior backward-compatible for existing callers except for explicit new `user_id` propagation.</action>
  <verify>
Run `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "ApiKeyAuth or KeyRotateRevoke" -q`.
  </verify>
  <done>
Validated principals include user identity required to resolve workspace membership roles from DB.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded owner stubs with membership-backed role lookup</name>
  <files>src/authorization/guards.py, src/api/routes/workspace_resources.py</files>
  <action>Implement DB-backed membership lookup using `(principal.user_id, target_workspace_id)` and map membership role to authorization `Role`. Remove all hardcoded owner/user placeholders from `_resolve_auth_principal_with_role` and `resolve_auth_principal_dep`. Deny requests with missing membership using explicit 403 responses (no silent fallback to owner/member defaults).</action>
  <verify>
Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -k "role" -q`.
  </verify>
  <done>
Role used in authorization checks is always derived from actual membership state for the caller and workspace.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add integration proof for owner/member divergence and non-member denial</name>
  <files>src/tests/integration/conftest.py, src/tests/integration/test_membership_role_behavior.py</files>
  <action>Update fixtures so owner/member keys are bound to distinct users with memberships in the same workspace, then add API-level tests proving divergence: owner can perform privileged resource action, member is denied that action, both can perform allowed read actions, and users without membership receive 403.</action>
  <verify>
Run `uv run pytest src/tests/integration/test_membership_role_behavior.py -q`, then run `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "RoleBehavior" -q`.
  </verify>
  <done>
Owner/member differences are observable through real API outcomes and backed by membership data.
  </done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_membership_role_behavior.py -q` and `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "RoleBehavior" -q`.
</verification>

<success_criteria>
Authorization no longer relies on role stubs; API responses demonstrate distinct owner/member behavior based on real workspace memberships.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-07-SUMMARY.md`
</output>
