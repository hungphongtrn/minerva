---
phase: 01-identity-and-policy-baseline
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/guest/identity.py
  - src/api/dependencies/auth.py
  - src/runtime_policy/models.py
  - src/runtime_policy/engine.py
  - src/runtime_policy/enforcer.py
  - src/services/run_service.py
  - src/api/routes/runs.py
  - src/tests/runtime_policy/test_guest_and_runtime_policy.py
autonomous: true
must_haves:
  truths:
    - "Requests without explicit user identity are assigned a random guest identity."
    - "Guest-mode requests do not create persistent run/session/checkpoint records."
    - "Runtime policy decisions default to deny for egress, tools, and secret access unless explicitly allowed."
    - "Only run-scoped secrets allowed by policy are injected during execution."
  artifacts:
    - path: "src/guest/identity.py"
      provides: "Ephemeral guest principal generation"
      exports: ["create_guest_principal"]
    - path: "src/runtime_policy/engine.py"
      provides: "Default-deny policy evaluation for network/tool/secret actions"
      exports: ["evaluate_runtime_action"]
    - path: "src/services/run_service.py"
      provides: "Run execution path with guest persistence guard and policy hooks"
      contains: ["is_guest", "policy_decision", "secret_scope"]
  key_links:
    - from: "src/api/dependencies/auth.py"
      to: "src/guest/identity.py"
      via: "fallback principal path when x-api-key missing"
      pattern: "create_guest_principal"
    - from: "src/services/run_service.py"
      to: "src/runtime_policy/enforcer.py"
      via: "policy gate before action execution"
      pattern: "authorize_egress|authorize_tool|authorize_secret"
    - from: "src/services/run_service.py"
      to: "src/api/routes/runs.py"
      via: "run request handling"
      pattern: "start_run"
---

<objective>
Implement guest identity behavior and runtime policy gates for egress, tool usage, and secrets.

Purpose: AUTH-06, SECU-01, SECU-02, and SECU-03 define the default-deny runtime boundary for Phase 1.
Output: Guest principal fallback, runtime policy engine/enforcer, run route/service wiring, and policy regression tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-RESEARCH.md
@.planning/phases/01-identity-and-policy-baseline/01-01-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guest identity fallback with ephemeral principal generation</name>
  <files>src/guest/identity.py, src/api/dependencies/auth.py</files>
  <action>Implement guest principal generation using cryptographically strong random IDs and mark principal mode as guest. Update auth dependency so missing API key takes this guest path instead of failing auth, while malformed/invalid provided keys still fail. Ensure each guest request gets a fresh identity and no persistent API key record is created.</action>
  <verify>uv run pytest src/tests/runtime_policy/test_guest_and_runtime_policy.py -k "guest" -q</verify>
  <done>Anonymous requests resolve to guest principals with random IDs and do not authenticate as any persisted user.</done>
</task>

<task type="auto">
  <name>Task 2: Implement default-deny runtime policy engine and enforcement hooks</name>
  <files>src/runtime_policy/models.py, src/runtime_policy/engine.py, src/runtime_policy/enforcer.py, src/services/run_service.py</files>
  <action>Define runtime policy models for egress destinations, tool IDs, and secret scopes. Implement policy evaluation that denies by default and allows only explicit matches from user/workspace/agent context. Add enforcer helpers used by run execution and every tool/secret access callsite so no bypass path exists.</action>
  <verify>uv run pytest src/tests/runtime_policy/test_guest_and_runtime_policy.py -k "policy" -q</verify>
  <done>Egress/tool/secret requests are blocked unless allowlisted, with policy outcomes enforced through a single shared enforcer path.</done>
</task>

<task type="auto">
  <name>Task 3: Wire run API flow with guest persistence guard and scoped secrets</name>
  <files>src/api/routes/runs.py, src/services/run_service.py, src/tests/runtime_policy/test_guest_and_runtime_policy.py</files>
  <action>Add run endpoint flow that calls runtime enforcers before network/tool/secret actions and applies a hard guard: guest-mode runs cannot write persistent run/session/checkpoint records. Inject only explicitly authorized secrets into the run context and return denials with clear error contracts.</action>
  <verify>uv run pytest src/tests/runtime_policy/test_guest_and_runtime_policy.py -q</verify>
  <done>Guest requests run in non-persistent mode and runtime policy controls are enforced consistently for egress, tools, and secret injection.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/runtime_policy/test_guest_and_runtime_policy.py -q`.
</verification>

<success_criteria>
Anonymous calls execute in guest mode without persistence, and runtime action controls are default-deny with explicit allowlist behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-04-SUMMARY.md`
</output>
