---
phase: 01-identity-and-policy-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/main.py
  - src/api/router.py
  - src/config/settings.py
  - src/db/session.py
  - src/db/models.py
  - src/db/migrations/env.py
  - src/db/migrations/versions/0001_identity_policy_baseline.py
  - src/tests/smoke/test_bootstrap.py
autonomous: true
must_haves:
  truths:
    - "The API boots with FastAPI and database session wiring available."
    - "Identity, workspace, membership, and API key tables exist under migration control."
    - "Tenant tables enforce RLS with owner bypass disabled (FORCE RLS)."
  artifacts:
    - path: "src/main.py"
      provides: "FastAPI app entrypoint"
      exports: ["app"]
    - path: "src/db/models.py"
      provides: "Core ORM models for Phase 1"
      contains: ["User", "Workspace", "Membership", "ApiKey", "WorkspaceResource"]
    - path: "src/db/migrations/versions/0001_identity_policy_baseline.py"
      provides: "Initial schema + RLS baseline migration"
      contains: ["ENABLE ROW LEVEL SECURITY", "FORCE ROW LEVEL SECURITY"]
  key_links:
    - from: "src/main.py"
      to: "src/api/router.py"
      via: "app.include_router(api_router)"
      pattern: "include_router\\(api_router\\)"
    - from: "src/db/session.py"
      to: "src/config/settings.py"
      via: "database URL settings"
      pattern: "DATABASE_URL"
    - from: "src/db/migrations/versions/0001_identity_policy_baseline.py"
      to: "src/db/models.py"
      via: "Alembic schema aligned to ORM"
      pattern: "workspace_resources|api_keys|memberships"
---

<objective>
Create the runnable backend foundation and schema boundary for identity and policy work.

Purpose: Every later Phase 1 plan depends on stable app wiring and DB tables with enforced isolation primitives.
Output: FastAPI entrypoint, SQLAlchemy model set, Alembic migration pipeline, and baseline RLS-enabled schema.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Python service skeleton with uv, FastAPI, and config wiring</name>
  <files>pyproject.toml, src/main.py, src/api/router.py, src/config/settings.py, src/db/session.py</files>
  <action>Initialize the Python project with uv-managed dependencies (FastAPI, SQLAlchemy, psycopg, Alembic, pydantic-settings, pytest). Create `src/main.py` with app factory/instance, `src/api/router.py` with router registration hook, settings loader with environment-backed defaults, and SQLAlchemy engine/session helpers. Keep all runtime implementation code under `src/`.</action>
  <verify>uv sync && uv run python -c "from src.main import app; print(bool(app))"</verify>
  <done>`uv sync` succeeds and importing `src.main:app` works without runtime errors.</done>
</task>

<task type="auto">
  <name>Task 2: Define ORM models and Alembic environment for identity baseline</name>
  <files>src/db/models.py, src/db/migrations/env.py</files>
  <action>Create SQLAlchemy models for `users`, `workspaces`, `memberships`, `api_keys`, and `workspace_resources` with UUID primary keys, role fields, guest flags where needed, created/updated timestamps, and tenant foreign keys. Wire Alembic env to SQLAlchemy metadata so migrations are generated/applied via ORM tooling (not manual SQL shell commands).</action>
  <verify>uv run alembic revision --autogenerate -m "check_identity_baseline" --rev-id temp_check && uv run alembic downgrade base && uv run alembic upgrade head</verify>
  <done>Model metadata is discoverable by Alembic and migration pipeline can apply from base to head cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Create initial migration with explicit RLS + FORCE RLS protections</name>
  <files>src/db/migrations/versions/0001_identity_policy_baseline.py, src/tests/smoke/test_bootstrap.py</files>
  <action>Create the first migration that materializes the baseline tables and applies row-level security policy setup on tenant tables with both `ENABLE ROW LEVEL SECURITY` and `FORCE ROW LEVEL SECURITY`. Add a smoke test that verifies app startup and basic DB session creation path so later plans have a stable baseline.</action>
  <verify>uv run alembic upgrade head && uv run pytest src/tests/smoke/test_bootstrap.py -q</verify>
  <done>Schema is created via Alembic, RLS force statements are present in migration, and smoke test passes.</done>
</task>

</tasks>

<verification>
Run `uv run alembic upgrade head` and `uv run pytest src/tests/smoke/test_bootstrap.py -q`.
</verification>

<success_criteria>
Execution environment is runnable, core identity tables exist, and tenant tables are initialized with enforceable RLS boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-01-SUMMARY.md`
</output>
