---
phase: 01-identity-and-policy-baseline
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/rls_context.py
  - src/db/migrations/versions/0001_identity_policy_baseline.py
  - src/tests/authorization/test_workspace_isolation.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Workspace-scoped requests execute without RLS context SQL runtime errors."
    - "Cross-workspace reads and writes are denied by tenant predicates, not placeholder allow-all policies."
    - "Workspace isolation acceptance scenarios pass when executed against the supported runtime database."
  artifacts:
    - path: "src/db/rls_context.py"
      provides: "Executable transaction-local app context setter for RLS"
      contains: ["set_config", "app.workspace_id", "app.user_id", "app.role"]
    - path: "src/db/migrations/versions/0001_identity_policy_baseline.py"
      provides: "Tenant-constraining RLS policies for workspace tables"
      contains: ["current_setting('app.workspace_id'", "WITH CHECK", "workspace_resource_isolation"]
    - path: "src/tests/authorization/test_workspace_isolation.py"
      provides: "Regression coverage for RLS context SQL and tenant isolation behavior"
      contains: ["rls", "workspace isolation"]
  key_links:
    - from: "src/db/rls_context.py"
      to: "src/db/migrations/versions/0001_identity_policy_baseline.py"
      via: "matching app.* context keys consumed by current_setting"
      pattern: "app.workspace_id|app.user_id|app.role"
    - from: "src/api/routes/workspace_resources.py"
      to: "src/db/rls_context.py"
      via: "with_rls_context before workspace-resource queries"
      pattern: "with_rls_context"
---

<objective>
Close Gap 1 by making RLS context execution valid and enforcing real tenant predicates at the database boundary.

Purpose: AUTH-03 requires strict workspace isolation that survives handler mistakes and does not rely on placeholder policy SQL.
Output: Fixed RLS context SQL path, real RLS policy predicates, and regression coverage proving workspace isolation flows are green.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-RESEARCH.md
@.planning/phases/01-identity-and-policy-baseline/01-identity-and-policy-baseline-VERIFICATION.md
@.planning/phases/01-identity-and-policy-baseline/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace invalid RLS context SQL with executable transaction-local context setting</name>
  <files>src/db/rls_context.py</files>
  <action>Replace `SET CONFIG` statements with executable SQLAlchemy-safe calls to `SELECT set_config(:key, :value, true)` for transaction-local settings. Keep all three keys (`app.workspace_id`, `app.user_id`, `app.role`) and make clear/reset behavior deterministic. Add dialect-aware handling so non-PostgreSQL test backends do not throw SQL syntax errors while preserving the same function contract (do not remove context calls from routes).</action>
  <verify>
Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -k "rls_context" -q`.
  </verify>
  <done>
RLS context set/clear executes without SQL syntax failures and keeps transaction-scoped semantics for PostgreSQL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder allow-all RLS policies with tenant predicates</name>
  <files>src/db/migrations/versions/0001_identity_policy_baseline.py</files>
  <action>Update RLS policy SQL from `USING (true)` placeholders to real tenant predicates using `current_setting('app.workspace_id', true)`, `current_setting('app.user_id', true)`, and role context where needed. Add `WITH CHECK` predicates for write paths so inserts/updates cannot escape workspace boundaries. Keep policy names stable to avoid route-level wiring churn.</action>
  <verify>
Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -k "workspace" -q`.
  </verify>
  <done>
Tenant tables have non-placeholder RLS policies that deny cross-workspace access by default.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests that fail on placeholder or broken RLS context behavior</name>
  <files>src/tests/authorization/test_workspace_isolation.py</files>
  <action>Add tests that explicitly fail if RLS context SQL is invalid or if policy expressions regress to allow-all behavior. Include a route-level isolation regression that verifies cross-workspace requests are denied (not 500) and same-workspace requests remain allowed.</action>
  <verify>
Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -q`, then run `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "TestWorkspaceIsolation" -q`.
  </verify>
  <done>
Workspace isolation checks are green and guard against future regression to placeholder or broken RLS context SQL.
  </done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -q` and `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "TestWorkspaceIsolation" -q`.
</verification>

<success_criteria>
Workspace isolation flows no longer fail with SQL syntax errors, and tenant isolation is enforced by concrete RLS predicates instead of allow-all placeholders.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-06-SUMMARY.md`
</output>
