---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/0003_workspace_lifecycle_and_agent_pack_foundation.py
  - src/db/repositories/__init__.py
  - src/db/repositories/workspace_lease_repository.py
  - src/db/repositories/sandbox_instance_repository.py
  - src/db/repositories/agent_pack_repository.py
  - src/tests/smoke/test_phase2_schema_bootstrap.py
autonomous: true
must_haves:
  truths:
    - "Workspace operations that require write ownership resolve to one active lease holder at a time, and conflicting ownership attempts fail deterministically."
    - "Agent pack registration/scaffold flows reject duplicate active registrations for the same workspace path instead of creating ambiguous records."
    - "Workspace sandbox resolution can return a deterministic candidate by workspace profile and health state so routing behavior is stable for callers."
  artifacts:
    - path: "src/db/models.py"
      provides: "ORM entities for workspace_leases, sandbox_instances, agent_packs, and agent_pack_revisions"
      contains: "class WorkspaceLease"
    - path: "src/db/migrations/versions/0003_workspace_lifecycle_and_agent_pack_foundation.py"
      provides: "Alembic migration for new Phase 2 lifecycle tables, indexes, and constraints"
      contains: "revision: str = \"0003\""
    - path: "src/db/repositories/workspace_lease_repository.py"
      provides: "DB access methods for lease acquisition and release semantics"
      contains: "def acquire_active_lease"
  key_links:
    - from: "src/db/migrations/versions/0003_workspace_lifecycle_and_agent_pack_foundation.py"
      to: "src/db/models.py"
      via: "table and column alignment"
      pattern: "workspace_leases|sandbox_instances|agent_packs"
    - from: "src/db/repositories/sandbox_instance_repository.py"
      to: "src/db/models.py"
      via: "typed query filters for active and healthy sandbox candidates"
      pattern: "health_status|state|profile"
---

<objective>
Create the Phase 2 persistence foundation for workspace lifecycle, sandbox state, and path-linked agent packs.

Purpose: Phase 2 orchestration and portability behaviors cannot be implemented safely without durable lifecycle state and lockable workspace write ownership.
Output: New ORM entities, Alembic migration, and repository modules that expose lifecycle-safe query primitives.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-CONTEXT.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-RESEARCH.md
@src/db/models.py
@src/db/migrations/versions/0002_api_key_user_binding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ORM with workspace lifecycle and pack entities</name>
  <files>src/db/models.py</files>
  <action>Add SQLAlchemy models for `workspace_leases`, `sandbox_instances`, `agent_packs`, and `agent_pack_revisions` with UUID PKs, workspace-scoped FKs, timestamps, lifecycle/status columns, and explicit enums/constraints needed by routing and serialization logic. Keep one durable workspace-per-user assumption by modeling leases and sandboxes as workspace-scoped records (do not introduce org-level tenancy abstractions in this phase).</action>
  <verify>`uv run python -c "from src.db.models import WorkspaceLease, SandboxInstance, AgentPack, AgentPackRevision; print('ok')"`</verify>
  <done>All four entities import successfully and include the minimum fields required by WORK-01/02/03/04/05/06 and AGNT-02/03.</done>
</task>

<task type="auto">
  <name>Task 2: Add Alembic migration for new lifecycle tables and indexes</name>
  <files>src/db/migrations/versions/0003_workspace_lifecycle_and_agent_pack_foundation.py</files>
  <action>Create revision `0003` that adds the new tables, foreign keys, and routing/locking indexes. Include uniqueness and conflict-prevention constraints for active lease ownership and workspace+source_path pack registration. Add downgrade logic that drops new objects cleanly in reverse dependency order.</action>
  <verify>`uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head`</verify>
  <done>Migration applies and rolls back locally without manual SQL edits, and schema invariants for lease and pack uniqueness are encoded in DDL.</done>
</task>

<task type="auto">
  <name>Task 3: Add repositories and schema smoke coverage</name>
  <files>src/db/repositories/__init__.py, src/db/repositories/workspace_lease_repository.py, src/db/repositories/sandbox_instance_repository.py, src/db/repositories/agent_pack_repository.py, src/tests/smoke/test_phase2_schema_bootstrap.py</files>
  <action>Create repository modules exposing focused query methods for lease, sandbox, and pack entities (selection, upsert/update, active-state filtering). Add a smoke test that validates model metadata includes new tables and verifies migration chain now includes revision `0003`.</action>
  <verify>`uv run pytest src/tests/smoke/test_phase2_schema_bootstrap.py -q`</verify>
  <done>Repositories provide callable methods for downstream services and smoke suite passes with Phase 2 schema baseline assertions.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/smoke/test_phase2_schema_bootstrap.py -q` and confirm Alembic can migrate to head with revision `0003` present.
</verification>

<success_criteria>
Phase 2 lifecycle data objects are durable, migration-controlled, and queryable for lease serialization and sandbox routing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-01-SUMMARY.md`
</output>
