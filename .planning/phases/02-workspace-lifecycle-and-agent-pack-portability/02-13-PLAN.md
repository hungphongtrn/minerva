---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/session.py
  - src/tests/integration/conftest.py
  - src/tests/integration/test_phase2_transaction_durability.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Pack registration that returns success is durable across the next request (register then list/get returns the same pack)."
    - "Back-to-back sandbox resolve calls for the same workspace reuse the same healthy sandbox instead of creating a new ID each call."
    - "Integration tests no longer hide production transaction behavior through test-only auto-commit shortcuts."
  artifacts:
    - path: "src/db/session.py"
      provides: "Request-scoped commit/rollback transaction boundary for FastAPI DB sessions"
      contains: "session.commit"
    - path: "src/tests/integration/conftest.py"
      provides: "Test DB dependency override aligned with production request transaction semantics"
      contains: "override_get_db"
    - path: "src/tests/integration/test_phase2_transaction_durability.py"
      provides: "Regression coverage for register/list durability and resolve reuse persistence"
      contains: "register"
  key_links:
    - from: "src/db/session.py"
      to: "src/api/routes/agent_packs.py"
      via: "FastAPI Depends(get_db) transaction lifecycle"
      pattern: "Depends\(get_db\)"
    - from: "src/db/session.py"
      to: "src/api/routes/workspaces.py"
      via: "FastAPI Depends(get_db) transaction lifecycle"
      pattern: "Depends\(get_db\)"
    - from: "src/tests/integration/test_phase2_transaction_durability.py"
      to: "src/db/session.py"
      via: "Cross-request assertions that require committed state"
      pattern: "register|resolve"
---

<objective>
Close the durability gap behind UAT Test 4 and Test 5 by making production request writes commit deterministically and by proving this behavior in production-equivalent integration coverage.

Purpose: UAT failures showed success responses could be returned without durable state, which breaks pack retrieval and active sandbox reuse semantics.
Output: Request transaction boundary fix plus focused integration regressions that fail if writes are only flushed and not committed.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-UAT.md
@src/db/session.py
@src/api/routes/agent_packs.py
@src/api/routes/workspaces.py
@src/tests/integration/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable request transaction boundaries in production DB dependency</name>
  <files>src/db/session.py</files>
  <action>Update `get_db()` to commit on successful request completion and rollback on raised exceptions before close. Keep the change centralized in the dependency (do not scatter ad-hoc commits in route handlers) so all mutating routes get consistent semantics. Preserve fail-closed behavior: if commit fails, rollback and propagate the error.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_transaction_durability.py -k "register_durable or resolve_reuses" -q`</verify>
  <done>Mutating requests persist across request boundaries in production flow; success responses no longer depend on flush-only state.</done>
</task>

<task type="auto">
  <name>Task 2: Remove test-only transaction masking and align integration request behavior</name>
  <files>src/tests/integration/conftest.py</files>
  <action>Refactor `override_get_db` so integration tests exercise the same commit/rollback lifecycle as production instead of masking gaps with fixture-specific auto-commit behavior. Keep fixture ergonomics for seeded data, but route-request persistence must come from the same dependency contract used at runtime.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_transaction_durability.py -q`</verify>
  <done>Integration tests fail when production transaction behavior regresses and pass only when request durability is real.</done>
</task>

<task type="auto">
  <name>Task 3: Add durability regressions for register->list/get and healthy sandbox reuse</name>
  <files>src/tests/integration/test_phase2_transaction_durability.py</files>
  <action>Create production-equivalent regressions that perform separate HTTP requests and assert durable outcomes: (1) successful `POST /agent-packs/register` is immediately visible via list/get; (2) two consecutive `POST /workspaces/{id}/sandbox/resolve` calls return the same `sandbox_id` when the first resolved sandbox remains healthy. Include persisted DB assertions so regressions fail if writes are discarded at session close.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_transaction_durability.py -q`</verify>
  <done>UAT Test 4 durability and UAT Test 5 reuse behavior are locked by dedicated integration coverage.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_transaction_durability.py -q` and confirm both register durability and resolve reuse scenarios are green across separate requests.
</verification>

<success_criteria>
Successful pack registration and sandbox resolve operations persist durably across requests, and dedicated regressions fail if flush-without-commit behavior returns.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-13-SUMMARY.md`
</output>
