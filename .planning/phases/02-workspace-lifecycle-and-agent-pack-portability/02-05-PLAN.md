---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - src/api/routes/workspaces.py
  - src/api/routes/agent_packs.py
  - src/api/routes/runs.py
  - src/api/router.py
  - src/services/run_service.py
  - src/tests/integration/conftest.py
  - src/tests/integration/test_phase2_acceptance.py
  - src/tests/integration/test_phase2_security_regressions.py
autonomous: true
must_haves:
  truths:
    - "A user can scaffold an agent pack folder, then validate/register it through API endpoints without manual file bootstrapping or infra wiring."
    - "Run routing resolves active healthy sandboxes or provisions/hydrates replacements while excluding unhealthy instances."
    - "Phase 2 policy and isolation boundaries are enforced by automated acceptance and security regression suites."
  artifacts:
    - path: "src/api/routes/workspaces.py"
      provides: "Workspace bootstrap and sandbox resolve endpoints"
      contains: "POST /workspaces/bootstrap"
    - path: "src/api/routes/agent_packs.py"
      provides: "Pack scaffold, register, and revalidate endpoints"
      contains: "POST /agent-packs/scaffold"
    - path: "src/tests/integration/test_phase2_security_regressions.py"
      provides: "SECU-05 regression coverage for workspace/sandbox/pack boundaries"
      contains: "test_no_cross_workspace"
  key_links:
    - from: "src/api/routes/runs.py"
      to: "src/services/workspace_lifecycle_service.py"
      via: "resolve routing target before execution"
      pattern: "resolve.*sandbox|workspace"
    - from: "src/api/routes/agent_packs.py"
      to: "src/services/agent_pack_service.py"
      via: "scaffold then register/revalidate API flow"
      pattern: "scaffold|register|validate"
---

<objective>
Expose Phase 2 capabilities through API routes and prove them with end-to-end acceptance and security boundary tests.

Purpose: This plan turns previously built services into user-observable behavior and closes SECU-05 for workspace/sandbox/pack flows.
Output: New route surfaces (including explicit scaffold entrypoint), run-path lifecycle integration, and Phase 2 integration/security test suites.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-CONTEXT.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-RESEARCH.md
@.planning/phases/01-identity-and-policy-baseline/01-03-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-05-SUMMARY.md
@src/api/routes/runs.py
@src/tests/integration/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace and agent pack scaffold/register API surfaces</name>
  <files>src/api/routes/workspaces.py, src/api/routes/agent_packs.py, src/api/router.py</files>
  <action>Create endpoints for workspace bootstrap and sandbox resolve, plus explicit agent-pack public flows: `POST /agent-packs/scaffold`, `POST /agent-packs/register`, and revalidate. `POST /agent-packs/scaffold` must create/refresh required template artifacts via service layer and return deterministic scaffold status so users can continue to validation/registration without manual filesystem setup. Route handlers must reuse existing principal/auth guard patterns from Phase 1 and return clear, machine-readable checklist errors on validation failure (do not return generic 500/opaque strings for user-fixable scaffold issues).</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -k "scaffold or register or bootstrap" -q`</verify>
  <done>API exposes scaffold -> register/revalidate -> resolve behaviors with structured response contracts and authenticated workspace scoping.</done>
</task>

<task type="auto">
  <name>Task 2: Wire run path to workspace lifecycle + sandbox routing semantics</name>
  <files>src/services/run_service.py, src/api/routes/runs.py</files>
  <action>Update run start/execute flow to call workspace lifecycle resolution before run execution, enforce lease-aware routing, and surface semantic sandbox state independent of provider internals. Keep profile semantics equivalent for local compose and daytona (only provider internals differ). Exclude unhealthy candidates and trigger hydrate/create path when none healthy/active exists.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -k "routing or sandbox" -q`</verify>
  <done>Run requests route to healthy active targets when available or provision replacement targets when needed, with no profile-specific API divergence.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 2 acceptance and security regression suites (SECU-05)</name>
  <files>src/tests/integration/conftest.py, src/tests/integration/test_phase2_acceptance.py, src/tests/integration/test_phase2_security_regressions.py</files>
  <action>Extend integration fixtures for workspace/pack/sandbox scenarios and add explicit tests mapped to Phase 2 success criteria: workspace continuity, public scaffold -> validate/register flow through `POST /agent-packs/scaffold` and register/revalidate endpoints, cross-profile semantic parity, healthy-route-or-hydrate behavior, same-workspace serialization, unhealthy exclusion, and idle TTL stop behavior. Add security regression tests for cross-workspace lease hijack, cross-workspace sandbox routing, and unauthorized pack path registration attempts.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py src/tests/integration/test_phase2_security_regressions.py -q`</verify>
  <done>Phase 2 acceptance/security suites pass and directly demonstrate AGNT-01 scaffold-to-registration user flow, AGNT-02/03, WORK-01/02/03/04/05/06, and SECU-05 behaviors.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_acceptance.py src/tests/integration/test_phase2_security_regressions.py -q` and confirm no unchecked failures for Phase 2 criteria.
</verification>

<success_criteria>
Phase 2 user-facing API behavior is observable end-to-end and protected by regression tests for policy/isolation boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-05-SUMMARY.md`
</output>
