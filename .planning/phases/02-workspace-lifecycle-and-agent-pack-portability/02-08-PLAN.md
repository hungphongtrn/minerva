---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 08
type: execute
wave: 2
depends_on: ["02-06", "02-07"]
files_modified:
  - src/tests/integration/test_phase2_acceptance.py
  - src/tests/integration/test_phase2_security_regressions.py
  - src/api/routes/workspaces.py
  - src/api/routes/agent_packs.py
  - src/services/agent_scaffold_service.py
  - src/infrastructure/sandbox/providers/__init__.py
  - src/infrastructure/sandbox/providers/factory.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Phase 2 acceptance suite passes with workspace continuity, scaffold/register lifecycle, portability parity, and sandbox resolve behavior proven through API calls."
    - "Phase 2 security regression suite passes with cross-workspace lease/sandbox/pack boundaries enforced and unauthorized pack access returning 403."
    - "Verifier gap truths for WORK-01/02/03, AGNT-01/02/03, and SECU-05 are demonstrably green in one repeatable test run."
  artifacts:
    - path: "src/tests/integration/test_phase2_acceptance.py"
      provides: "End-to-end acceptance assertions mapped to Phase 2 success criteria"
      contains: "TestWorkspaceContinuity"
    - path: "src/tests/integration/test_phase2_security_regressions.py"
      provides: "SECU-05 policy/isolation regression assertions"
      contains: "TestCrossWorkspacePackIsolation"
  key_links:
    - from: "src/tests/integration/test_phase2_acceptance.py"
      to: "/api/v1/workspaces and /api/v1/agent-packs"
      via: "integration client request flow"
      pattern: "client\.(get|post)"
    - from: "src/tests/integration/test_phase2_security_regressions.py"
      to: "src/api/routes/agent_packs.py"
      via: "cross-workspace access assertions"
      pattern: "status_code == 403"
---

<objective>
Deliver a clean Phase 2 gap-closure finish by driving acceptance and security suites to green and preserving the expected API/security contracts.

Purpose: Gap closure is only complete when verifier truths are observable end-to-end, not just patched at unit/service level.
Output: Full Phase 2 acceptance + SECU-05 suites green, with any residual contract mismatches fixed and captured in the summary evidence.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-VERIFICATION.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-UAT.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-06-SUMMARY.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-07-SUMMARY.md
@src/tests/integration/test_phase2_acceptance.py
@src/tests/integration/test_phase2_security_regressions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 2 acceptance and security suites as release gate</name>
  <files>src/tests/integration/test_phase2_acceptance.py, src/tests/integration/test_phase2_security_regressions.py</files>
  <action>Execute full integration gates for Phase 2 (`test_phase2_acceptance.py` and `test_phase2_security_regressions.py`) to establish a single source of truth for remaining failures. Capture exact failing test IDs and map each one to API contract, auth ordering, path normalization, or provider portability cause before editing code.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py src/tests/integration/test_phase2_security_regressions.py -q`</verify>
  <done>All remaining failures are explicitly identified and mapped to concrete code locations before remediation.</done>
</task>

<task type="auto">
  <name>Task 2: Close residual failures and rerun until both suites are green</name>
  <files>src/tests/integration/test_phase2_acceptance.py, src/tests/integration/test_phase2_security_regressions.py, src/api/routes/workspaces.py, src/api/routes/agent_packs.py, src/services/agent_scaffold_service.py, src/infrastructure/sandbox/providers/__init__.py, src/infrastructure/sandbox/providers/factory.py</files>
  <action>Implement minimal, contract-preserving fixes for failing tests mapped in Task 1, limited to API/auth/path-normalization/provider-contract issues in the listed files. Prioritize keeping API semantics stable (owner success, unauthorized 403, traversal fail-closed, portability parity). Rerun the full gate after each fix. Bounded remediation rule: if a rerun reveals a new failure class outside those mapped categories or requiring files outside this plan, stop implementation in this plan, document the new class in the summary, and create a follow-on plan instead of expanding scope here.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py src/tests/integration/test_phase2_security_regressions.py -q` exits 0</verify>
  <done>Both integration suites are green in a single run for in-scope failure classes, and any out-of-scope/new failure class is explicitly documented and deferred to a follow-on plan.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_acceptance.py src/tests/integration/test_phase2_security_regressions.py -q` and confirm zero failures and no xfail masking of previously failing gap tests.
</verification>

<success_criteria>
Phase 2 gap closure is complete: acceptance plus SECU-05 regression suites pass, and all verifier/UAT failures for workspace status/resolve, scaffold-register flow, portability contract, and cross-workspace pack authorization are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-08-SUMMARY.md`
</output>
