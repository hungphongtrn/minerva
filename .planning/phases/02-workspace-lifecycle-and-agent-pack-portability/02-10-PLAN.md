---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 10
type: execute
wave: 2
depends_on: ["02-09"]
files_modified:
  - src/infrastructure/sandbox/providers/local_compose.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/tests/services/test_sandbox_provider_adapters.py
  - src/tests/integration/test_phase2_acceptance.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Provisioning a sandbox with a registered pack binds the pack source path into both local_compose and daytona provider runtime metadata with equivalent semantics."
    - "An acceptance test proves register -> resolve/provision across local_compose and daytona uses the same pack-binding contract without manual rewiring."
    - "Phase 2 portability behavior remains green after adding real registered-pack runtime binding checks."
  artifacts:
    - path: "src/infrastructure/sandbox/providers/local_compose.py"
      provides: "Local profile pack bind/mount behavior during provisioning"
      contains: "pack_source_path"
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "Daytona profile pack bind/mount behavior during provisioning"
      contains: "pack_source_path"
    - path: "src/tests/services/test_sandbox_provider_adapters.py"
      provides: "Provider parity assertions for pack-binding metadata contract"
      contains: "test_pack_binding_metadata_parity"
    - path: "src/tests/integration/test_phase2_acceptance.py"
      provides: "End-to-end profile parity test for registered pack runtime binding"
      contains: "TestRegisteredPackBindingParity"
  key_links:
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/local_compose.py"
      via: "SandboxConfig.pack_source_path passed into provision_sandbox"
      pattern: 'config\.pack_source_path'
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/daytona.py"
      via: "SandboxConfig.pack_source_path passed into provision_sandbox"
      pattern: 'config\.pack_source_path'
    - from: "src/tests/integration/test_phase2_acceptance.py"
      to: "local_compose and daytona providers"
      via: "register pack then assert equivalent runtime binding semantics"
      pattern: "local_compose|daytona"
---

<objective>
Close the remaining UAT Test 4 gap by implementing provider-side registered-pack binding and proving cross-profile parity through an end-to-end acceptance test.

Purpose: After plan 02-09 wires agent pack identity into provisioning, this plan makes pack binding observable in provider runtime behavior and validates equivalent semantics for local and BYOC/Daytona profiles.
Output: Provider implementations that apply pack binding from `SandboxConfig.pack_source_path`, plus a Phase 2 acceptance test that exercises and verifies pack binding parity across profiles.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-UAT.md
@.planning/debug/phase02-pack-binding-gap.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-02-SUMMARY.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-09-SUMMARY.md
@src/infrastructure/sandbox/providers/local_compose.py
@src/infrastructure/sandbox/providers/daytona.py
@src/tests/services/test_sandbox_provider_adapters.py
@src/tests/integration/test_phase2_acceptance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement equivalent provider-side pack bind semantics for local and Daytona</name>
  <files>src/infrastructure/sandbox/providers/local_compose.py, src/infrastructure/sandbox/providers/daytona.py, src/tests/services/test_sandbox_provider_adapters.py</files>
  <action>Update `provision_sandbox()` in both providers to consume `config.pack_source_path` and apply the same semantic contract: include bound pack source path in provider metadata/state, mark bind status deterministically, and keep behavior no-op when no pack is provided. Keep profile-specific mechanics isolated behind provider internals, but expose equivalent observable fields for parity assertions. Add service parity tests named `test_pack_binding_metadata_parity_local_compose` and `test_pack_binding_metadata_parity_daytona` in `test_sandbox_provider_adapters.py`.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_local_compose src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_daytona -q`</verify>
  <done>Both providers expose equivalent, testable pack-binding semantics from `SandboxConfig.pack_source_path` without changing existing non-pack provisioning behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add acceptance coverage for register-and-run parity across local_compose and daytona</name>
  <files>src/tests/integration/test_phase2_acceptance.py</files>
  <action>Add an acceptance test class (`TestRegisteredPackBindingParity`) that: scaffolds and registers a real pack, resolves/provisions execution path with that pack under local_compose and daytona provider contexts, and asserts equivalent runtime binding semantics (pack path surfaced, bind state present, no manual infra rewiring). Keep assertions semantic and provider-agnostic where possible.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -k "RegisteredPackBindingParity or pack_binding" -q`</verify>
  <done>Phase 2 acceptance suite now contains an explicit end-to-end proof that registered-pack runtime binding works equivalently across local_compose and daytona.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run portability and acceptance gates to close UAT truth</name>
  <files>src/tests/integration/test_phase2_acceptance.py</files>
  <action>Run the targeted new acceptance test first, then re-run the full Phase 2 acceptance suite to ensure no regression. If parity assertion fails due to contract mismatch, fix only in-scope pack binding semantics and rerun until green.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -q`</verify>
  <done>Phase 2 acceptance passes with explicit registered-pack local/daytona parity coverage, closing the diagnosed UAT gap truth for Test 4.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_acceptance.py -q` and confirm the new registered-pack portability scenario passes alongside existing Phase 2 acceptance tests.
</verification>

<success_criteria>
Local and Daytona providers both bind registered pack source path during provisioning with equivalent semantics, and acceptance evidence demonstrates end-to-end parity for registered-pack execution without manual infra rewiring.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-10-SUMMARY.md`
</output>
