---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/agent_scaffold_service.py
  - src/api/routes/agent_packs.py
  - src/infrastructure/sandbox/providers/__init__.py
  - src/infrastructure/sandbox/providers/factory.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Users can scaffold to common absolute temp paths and continue scaffold -> register -> validate lifecycle via API."
    - "Cross-workspace pack access paths return HTTP 403 for unauthorized principals instead of upstream 400 contract breaks."
    - "Local compose and Daytona provider symbols/contracts are exposed consistently so portability acceptance tests can import and execute parity paths."
  artifacts:
    - path: "src/services/agent_scaffold_service.py"
      provides: "Safe path normalization that allows expected absolute workflow paths without traversal escape"
      contains: "_normalize_and_validate_path"
    - path: "src/api/routes/agent_packs.py"
      provides: "Pack endpoint workspace authorization and consistent status-code contract"
      contains: "register_agent_pack"
    - path: "src/infrastructure/sandbox/providers/__init__.py"
      provides: "Stable provider exports/aliases for profile portability imports"
      contains: "DaytonaSandboxProvider"
  key_links:
    - from: "src/api/routes/agent_packs.py"
      to: "src/services/agent_pack_service.py"
      via: "authorize workspace first, then execute register/validate/get/stale/list operations"
      pattern: "workspace_id|UUID\("
    - from: "src/infrastructure/sandbox/providers/factory.py"
      to: "provider registry"
      via: "profile key to provider class mapping"
      pattern: "_PROVIDER_REGISTRY"
---

<objective>
Close scaffold/register and profile portability contract gaps that are preventing end-to-end Phase 2 acceptance from going green.

Purpose: Verification identified route-level/API-contract mismatches around scaffold path handling, pack authorization semantics, and provider contract exposure.
Output: Safe absolute-path scaffold behavior, consistent 403 authorization semantics for cross-workspace pack access, and stable provider export/alias contract for portability checks.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-VERIFICATION.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-UAT.md
@src/services/agent_scaffold_service.py
@src/api/routes/agent_packs.py
@src/infrastructure/sandbox/providers/factory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Allow safe absolute scaffold paths without weakening traversal protection</name>
  <files>src/services/agent_scaffold_service.py</files>
  <action>Refactor scaffold path normalization so product-expected absolute paths (for example temporary directories used by API/integration flows) are accepted when safe, while traversal and explicit base-path escape attacks remain blocked. Keep relative path validation fail-closed and preserve deterministic `PathTraversalError` messaging used by security tests.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -k "scaffold_creates_required_files or scaffold_is_idempotent or register_validates_scaffold" -q`</verify>
  <done>Scaffold endpoint succeeds for safe absolute paths in acceptance flow, and traversal protections still fail closed.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce pack endpoint authorization ordering and status-code contract</name>
  <files>src/api/routes/agent_packs.py</files>
  <action>Centralize workspace UUID extraction from principal and apply it consistently across register/validate/stale/get/list handlers. Ensure ownership checks happen before pack operations that can raise unrelated request-validation errors so cross-workspace access attempts deterministically return 403. Preserve existing behavior for guest restrictions and not-found resources in owner scope.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_security_regressions.py -k "cannot_register_pack_in_other_workspace or cannot_validate_pack_in_other_workspace" -q`</verify>
  <done>Cross-workspace pack operations no longer leak 400 from upstream contract issues and instead return explicit 403 authorization failures.</done>
</task>

<task type="auto">
  <name>Task 3: Align provider export/import contract for portability acceptance</name>
  <files>src/infrastructure/sandbox/providers/__init__.py, src/infrastructure/sandbox/providers/factory.py</files>
  <action>Expose stable provider symbols and aliases used by acceptance parity scenarios and ensure factory profile mapping remains explicit and deterministic for `local_compose` and `daytona`. Do not add route-level profile branching; keep portability behind provider/factory contract only.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -k "ProfileSemanticParity or provider_factory_returns_configured_provider" -q`</verify>
  <done>Acceptance parity imports and provider selection run without import/API mismatch failures across local compose and Daytona profiles.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_acceptance.py -k "scaffold or register or ProfileSemanticParity" -q` and `uv run pytest src/tests/integration/test_phase2_security_regressions.py -k "CrossWorkspacePackIsolation" -q`.
</verification>

<success_criteria>
Scaffold/register API lifecycle and profile portability checks are executable end-to-end, and cross-workspace pack authorization now matches the expected 403 security contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-07-SUMMARY.md`
</output>
