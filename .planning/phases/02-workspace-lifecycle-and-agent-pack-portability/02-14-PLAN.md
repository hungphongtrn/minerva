---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/run_service.py
  - src/api/routes/runs.py
  - src/scripts/phase2_profile_parity_harness.py
  - src/tests/integration/test_phase2_run_routing_errors.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Pack-based runs fail fast when lifecycle routing fails, instead of continuing with null/invalid sandbox targets."
    - "API responses expose pack-specific routing failures with deterministic error types, not generic 503 sandbox-unavailable responses."
    - "Cross-profile parity can be executed deterministically by automation without manual profile rewiring steps."
  artifacts:
    - path: "src/services/run_service.py"
      provides: "Strict routing success contract for non-guest runs"
      contains: "resolve_routing_target"
    - path: "src/api/routes/runs.py"
      provides: "Structured mapping for pack-routing failure types"
      contains: "error_type"
    - path: "src/scripts/phase2_profile_parity_harness.py"
      provides: "Scripted local/daytona parity verification workflow"
      contains: "SANDBOX_PROFILE"
    - path: "src/tests/integration/test_phase2_run_routing_errors.py"
      provides: "Regression coverage for fail-fast routing and pack-specific API errors"
      contains: "agent_pack_id"
  key_links:
    - from: "src/services/run_service.py"
      to: "src/services/workspace_lifecycle_service.py"
      via: "Routing result gate before run execution"
      pattern: "routing_result"
    - from: "src/api/routes/runs.py"
      to: "src/services/run_service.py"
      via: "HTTP status and detail mapping from routing errors"
      pattern: "result.error"
    - from: "src/scripts/phase2_profile_parity_harness.py"
      to: "src/infrastructure/sandbox/providers/factory.py"
      via: "Per-profile execution with SANDBOX_PROFILE switching"
      pattern: "SANDBOX_PROFILE"
---

<objective>
Close the UAT Test 4 routing/error-contract gap by enforcing fail-fast run routing semantics and exposing actionable pack-specific API errors, while adding an automated parity harness for local/daytona profile checks.

Purpose: Users need deterministic failure behavior and reproducible parity verification; silent routing fallthrough plus generic 503 responses hide real remediation paths.
Output: Hardened run routing contract, structured API errors, and an executable profile-parity harness with integration evidence.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-UAT.md
@src/services/run_service.py
@src/api/routes/runs.py
@src/services/workspace_lifecycle_service.py
@src/infrastructure/sandbox/providers/factory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce fail-fast routing contract in RunService</name>
  <files>src/services/run_service.py</files>
  <action>Harden `resolve_routing_target` and `execute_with_routing` so non-guest execution only returns success when lifecycle routing is explicitly successful and provides a valid sandbox target. Treat lifecycle/routing failure branches (including pack validation failures) as terminal routing errors; do not let run execution continue with `success=True` + null sandbox. Keep guest behavior unchanged.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "fail_fast" -q`</verify>
  <done>Run execution cannot proceed when routing is unresolved or invalid for authenticated requests.</done>
</task>

<task type="auto">
  <name>Task 2: Return pack-specific routing errors from /runs endpoint</name>
  <files>src/api/routes/runs.py</files>
  <action>Map routing failures to deterministic API error contracts: include `error_type` and remediation-oriented detail for pack-not-found, pack-workspace-mismatch, pack-invalid/stale, lease-conflict, and provider-unavailable branches. Keep 503 only for true sandbox unavailability, not for validation/ownership failures that should return 4xx.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "error_contract" -q`</verify>
  <done>Pack-specific routing failures are observable and distinguishable from infrastructure unavailability.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated cross-profile parity harness and regression coverage</name>
  <files>src/scripts/phase2_profile_parity_harness.py, src/tests/integration/test_phase2_run_routing_errors.py</files>
  <action>Create a scriptable parity harness that executes the same routing scenario under `SANDBOX_PROFILE=local_compose` and `SANDBOX_PROFILE=daytona` (sequential profile runs, no manual rewiring). Add integration tests proving both profiles return equivalent semantic outcomes for pack-bound runs and equivalent failure typing for pack-routing errors.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -q && uv run python src/scripts/phase2_profile_parity_harness.py --mode ci`</verify>
  <done>Parity and error-contract checks are reproducible in automation for both profiles.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -q` and `uv run python src/scripts/phase2_profile_parity_harness.py --mode ci`; confirm fail-fast routing and profile parity checks pass without manual environment rewiring.
</verification>

<success_criteria>
Pack-bound run routing fails closed with deterministic 4xx/5xx error typing, and parity checks for local/daytona execution paths are fully automatable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-14-SUMMARY.md`
</output>
