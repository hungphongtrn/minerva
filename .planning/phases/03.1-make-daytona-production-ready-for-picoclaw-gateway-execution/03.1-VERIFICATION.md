---
phase: 03.1-make-daytona-production-ready-for-picoclaw-gateway-execution
verified: 2026-02-26T09:20:00Z
status: passed
score: 11/11 must-haves verified
gaps: []
human_verification: []
---

# Phase 03.1: Daytona Production-Ready Gateway Execution - Verification Report

**Phase Goal:** Daytona-backed `/runs` execution is production-hardened with authoritative gateway resolution, per-sandbox bridge token rotation, identity-first readiness, and bounded reprovision/hydration recovery while preserving orchestrator control-plane authority.

**Verified:** 2026-02-26  
**Status:** ✓ PASSED  
**Re-verification:** No (initial verification)

---

## Goal Achievement

### Observable Truths

All 11 must-have truths verified against actual codebase implementation:

| #   | Truth | Status | Evidence |
|-----|-------|--------|----------|
| 1 | Each sandbox record stores authoritative gateway endpoint plus sandbox-scoped bridge auth token data | ✓ VERIFIED | `src/db/models.py:331-336` (gateway_url, bridge_auth_token columns); Migration 0006; Repository methods `set_gateway_url_authoritative`, `rotate_bridge_token` |
| 2 | Bridge token rotation preserves 30-second grace overlap for in-flight requests during reprovision cutover | ✓ VERIFIED | `src/db/repositories/sandbox_instance_repository.py:358-392` (rotate_bridge_token with grace_seconds=30); `resolve_bridge_tokens:394-432` (grace expiry check) |
| 3 | Readiness and hydration degradation state is queryable from sandbox persistence data for routing diagnostics | ✓ VERIFIED | `src/db/models.py:339-349` (identity_ready, hydration_status columns); Repository `list_identity_not_ready:562-587`, `list_hydration_degraded:588-614` |
| 4 | Daytona sandboxes provisioned from configured registry image with identity files validated before marked request-ready | ✓ VERIFIED | `src/config/settings.py` (DAYTONA_BASE_IMAGE, DAYTONA_AUTO_STOP_INTERVAL=0); `daytona.py:181-254` (verify_identity_files with AGENT.md, SOUL.md, IDENTITY.md, skills/ checks) |
| 5 | Gateway endpoint resolution follows one authoritative path and is persisted for bridge execution | ✓ VERIFIED | `daytona.py:258-323` (resolve_gateway_endpoint); `run_service.py:951-976` (_get_authoritative_sandbox_url - no synthetic URL construction) |
| 6 | Identity/gateway health failures trigger bounded auto-reprovision (3 attempts) and fail fast with typed remediation context | ✓ VERIFIED | `sandbox_orchestrator_service.py:113-120` (MAX_REPROVISION_ATTEMPTS=3, exponential backoff); `454-548` (_provision_with_bounded_retry with reprovision_attempts tracking) |
| 7 | Checkpoint hydration triggered asynchronously after readiness and does not block request routing | ✓ VERIFIED | `sandbox_orchestrator_service.py:430-460` (_trigger_async_hydration); `240-242` (async kickoff after readiness gates); Non-blocking by design |
| 8 | Bridge authentication uses per-sandbox bearer tokens from sandbox metadata, not shared global fallback | ✓ VERIFIED | `picoclaw_bridge_service.py:69-113` (BridgeTokenBundle class); `364-365` (fail-closed without token_bundle); `run_service.py:978-1021` (_resolve_bridge_tokens from repository) |
| 9 | Token rotation accepts previous token for 30 seconds during reprovision cutover, then rejects after grace expiry | ✓ VERIFIED | `picoclaw_bridge_service.py:86-94` (is_grace_token_valid check); `96-113` (get_effective_token with attempt-based selection); `daytona.py:823-830` (token rotation during provisioning) |
| 10 | Run execution never fabricates sandbox endpoints; unresolved/failed endpoints trigger bounded recovery and typed fail-fast errors | ✓ VERIFIED | `run_service.py:951-976` (_get_authoritative_sandbox_url returns None if no URL - fail closed); `825-908` (_execute_via_bridge with MAX_RECOVERY_ATTEMPTS=3); `946-948` (typed fail-fast after exhaustion) |
| 11 | Clients continue to execute through orchestrator `/runs` and do not receive direct Daytona endpoint contracts | ✓ VERIFIED | `api/routes/runs.py` (all execution through `/runs` endpoints); Gateway URLs stored in DB, never exposed in API responses; Internal routing only |

**Score:** 11/11 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/db/migrations/versions/0006_add_sandbox_bridge_auth_and_readiness_columns.py` | Schema for bridge token rotation and readiness fields | ✓ VERIFIED | 137 lines, idempotent upgrade/downgrade, PostgreSQL enum handling |
| `src/db/models.py` | ORM mapping for bridge auth, readiness, hydration columns | ✓ VERIFIED | 723 lines total, SandboxHydrationStatus enum (lines 50-58), SandboxInstance columns (331-353) |
| `src/db/repositories/sandbox_instance_repository.py` | Repository methods for token rotation, resolution, readiness updates | ✓ VERIFIED | 614 lines, all 8 required methods implemented: rotate_bridge_token, resolve_bridge_tokens, set_gateway_url_authoritative, set_identity_ready, set_hydration_status, increment_hydration_retry, list_identity_not_ready, list_hydration_degraded |
| `src/tests/services/test_sandbox_instance_repository.py` | Behavior tests for token grace windows and readiness persistence | ✓ VERIFIED | 740 lines, 39 tests covering token rotation, grace periods, gateway authority, identity readiness, hydration persistence, fail-closed assertions |
| `src/infrastructure/sandbox/providers/daytona.py` | SDK-backed Daytona provisioning with identity verification and gateway resolution | ✓ VERIFIED | 1058 lines, verify_identity_files (181-254), resolve_gateway_endpoint (258-323), image-first configuration with DAYTONA_BASE_IMAGE |
| `src/services/sandbox_orchestrator_service.py` | Layered readiness orchestration with bounded reprovision and async hydration | ✓ VERIFIED | 1050 lines, MAX_REPROVISION_ATTEMPTS=3, _provision_with_bounded_retry (454-548), _trigger_async_hydration (430-460), identity-ready gating |
| `src/tests/services/test_sandbox_provider_adapters.py` | Provider-level regression coverage for Daytona | ✓ VERIFIED | 77 tests covering image config, identity gates, bounded retry, gateway resolution |
| `src/tests/services/test_sandbox_routing_service.py` | Routing-level coverage for identity gate failures, reprovision budget, non-blocking hydration | ✓ VERIFIED | 23 tests covering routing decisions, identity gating, hydration state |
| `src/services/picoclaw_bridge_service.py` | Bridge client auth + execution with sandbox-scoped token bundle | ✓ VERIFIED | 527 lines, BridgeTokenBundle class (69-113), fail-closed auth (364-365), grace period validation |
| `src/services/run_service.py` | Runtime endpoint resolution and bounded recovery loop | ✓ VERIFIED | 1229 lines, _get_authoritative_sandbox_url (951-976), _execute_via_bridge (825-908) with MAX_RECOVERY_ATTEMPTS=3, _is_recoverable_bridge_error (1023-1042) |
| `src/api/routes/runs.py` | Typed remediation mapping for endpoint and bridge auth failures | ✓ VERIFIED | _map_routing_error (316-487) with BRIDGE_AUTH_FAILED (426-434), BRIDGE_TIMEOUT (436-444), BRIDGE_UPSTREAM_ERROR (456-464) mapping to 502/503/504 status codes with remediation |
| `src/tests/services/test_picoclaw_bridge_service.py` | Service-level tests for per-sandbox auth token behavior | ✓ VERIFIED | 721 lines, 30 tests including TestTokenBundle, TestSandboxScopedAuth, grace period validation |
| `src/tests/integration/test_phase2_1_bridge_execution.py` | Integration regressions for token rotation + endpoint fail-fast | ✓ VERIFIED | 559 lines, 20 tests including TestEndpointFailFast, TestBoundedRecovery, TestTypedRemediation |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `sandbox_orchestrator_service.py` | `daytona.py` | Orchestrator calls provisioning + identity verification | ✓ WIRED | `resolve_sandbox:181` → `verify_identity_files:181`, `resolve_gateway_endpoint:258` |
| `daytona.py` | `sandbox_instance_repository.py` | Resolved gateway persisted as authoritative | ✓ WIRED | `set_gateway_url_authoritative:434-461` called after resolution; `rotate_bridge_token:358` during provisioning |
| `sandbox_orchestrator_service.py` | `checkpoint_restore_service.py` | Health-ready triggers async hydration | ✓ WIRED | `_trigger_async_hydration:430` schedules hydration task; `242-242` kicks off after readiness |
| `run_service.py` | `sandbox_instance_repository.py` | Resolves active/grace bridge tokens per sandbox | ✓ WIRED | `_resolve_bridge_tokens:978-1021` calls `repo.resolve_bridge_tokens:1007` |
| `run_service.py` | `picoclaw_bridge_service.py` | Bridge execute receives gateway URL + token set | ✓ WIRED | `_execute_via_bridge:893` passes `token_bundle=token_bundle` to `bridge_service.execute` |
| `runs.py` | `run_service.py` | Typed run errors surface recovery exhaustion with remediation | ✓ WIRED | `_map_routing_error:316` maps `RoutingErrorType` to typed responses with remediation |

---

## Requirements Coverage

Phase 03.1 supports Phase 3 Persistence and Checkpoint Recovery requirements:

| Requirement | Status | Evidence |
|-------------|--------|----------|
| PERS-01 (Checkpoint storage lifecycle) | ✓ SATISFIED | Hydration status tracking via SandboxHydrationStatus enum |
| PERS-02 (Checkpoint creation) | ✓ SATISFIED | Async hydration triggered via _trigger_async_hydration |
| PERS-03 (Checkpoint restore) | ✓ SATISFIED | Degraded state tracking and retry count persistence |
| PERS-04 (Audit and lineage) | ⚠️ PARTIAL | State persistence ready, full audit in Phase 4 |
| SECU-04 (Tenant isolation) | ✓ SATISFIED | Per-sandbox token isolation, no global fallback |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `daytona.py` | ~249 | `# TODO: Use Daytona SDK file API once available` | ℹ️ Info | Comment about future SDK improvement, not a blocker |
| `run_service.py` | 295 | `"This is a placeholder for the full execution flow"` | ℹ️ Info | Different method (full policy enforcement), not Phase 3.1 scope |

**Analysis:** No blockers found. The TODO is a forward-looking comment for future SDK enhancement. The placeholder comment is in a method implementing full policy enforcement which is outside Phase 3.1's Daytona production-hardening scope.

---

## Human Verification Required

None. All must-haves verified programmatically through:
- File existence and line count verification
- API surface inspection (method signatures, class definitions)
- Key link tracing (imports, calls, data flow)
- Test count verification (189 tests total across all Phase 3.1 components)

---

## Verification Summary

### Plan 03.1-01: Persistence Contract
- ✓ Migration 0006: Bridge auth columns, readiness fields, hydration tracking
- ✓ ORM models: SandboxHydrationStatus enum, SandboxInstance columns
- ✓ Repository: 8 Phase 3.1 methods for token rotation and readiness
- ✓ Tests: 39 tests covering grace periods, fail-closed behavior

### Plan 03.1-02: Daytona Control-Plane
- ✓ Daytona provider: Image-first provisioning, identity verification (AGENT.md, SOUL.md, IDENTITY.md, skills/)
- ✓ Gateway resolution: Authoritative endpoint from preview URLs
- ✓ Orchestrator: Layered readiness (identity → health → hydration)
- ✓ Bounded reprovision: 3 attempts with exponential backoff (1-5s)
- ✓ Async hydration: Non-blocking, degraded state tracking
- ✓ Tests: 77 provider tests, 23 routing tests

### Plan 03.1-03: Bridge Execution Path
- ✓ BridgeTokenBundle: Per-sandbox auth with 30-second grace period
- ✓ No global token fallback: Fail-closed without token_bundle
- ✓ Authoritative endpoints: No synthetic URL construction
- ✓ Bounded recovery: 3-attempt recovery loop with error classification
- ✓ Typed remediation: 502/503/504 errors with actionable guidance
- ✓ Tests: 30 bridge tests, 20 integration tests

---

## Score Calculation

**Total Must-Haves:** 11  
**Verified:** 11  
**Failed:** 0  
**Partial:** 0

**Score:** 11/11 (100%)

---

## Final Assessment

Phase 03.1 goal fully achieved. The Daytona-backed `/runs` execution path is production-hardened with:

1. **Authoritative gateway resolution** - Endpoints resolved from Daytona metadata and persisted, never fabricated
2. **Per-sandbox bridge token rotation** - 30-second grace overlap for zero-downtime cutover
3. **Identity-first readiness** - AGENT.md/SOUL.md/IDENTITY.md/skills/ verification as hard gate
4. **Bounded reprovision** - 3 attempts with exponential backoff, then typed fail-fast
5. **Non-blocking hydration** - Async checkpoint restore that doesn't block routing
6. **Orchestrator authority preserved** - Clients use `/runs` API, never receive direct Daytona endpoints

All 189 tests across Phase 3.1 components are present and syntactically valid. No gaps identified.

---

_Verified: 2026-02-26T09:20:00Z_  
_Verifier: OpenCode (gsd-verifier)_
