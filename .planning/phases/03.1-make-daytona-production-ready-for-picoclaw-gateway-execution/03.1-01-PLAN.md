---
phase: 03.1-make-daytona-production-ready-for-picoclaw-gateway-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/models.py
  - src/db/repositories/sandbox_instance_repository.py
  - src/db/migrations/versions/0006_add_sandbox_bridge_auth_and_readiness_columns.py
  - src/tests/services/test_sandbox_instance_repository.py
autonomous: true
must_haves:
  truths:
    - "Each sandbox record stores an authoritative gateway endpoint plus sandbox-scoped bridge auth token data required by orchestrated bridge execution."
    - "Bridge token rotation preserves a 30-second grace overlap so in-flight requests do not fail during reprovision cutover."
    - "Readiness and hydration degradation state is queryable from sandbox persistence data for routing diagnostics."
  artifacts:
    - path: "src/db/migrations/versions/0006_add_sandbox_bridge_auth_and_readiness_columns.py"
      provides: "Schema for bridge token rotation fields and readiness/hydration status fields on sandbox_instances"
      contains: "upgrade"
    - path: "src/db/models.py"
      provides: "ORM mapping for sandbox bridge auth, readiness, and hydration tracking columns"
      contains: "class SandboxInstance"
    - path: "src/db/repositories/sandbox_instance_repository.py"
      provides: "Repository methods for token rotation, active token resolution, and readiness status updates"
      contains: "rotate_bridge_token"
    - path: "src/tests/services/test_sandbox_instance_repository.py"
      provides: "Behavior tests for token grace windows and readiness state persistence"
      contains: "test_"
  key_links:
    - from: "src/db/repositories/sandbox_instance_repository.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "Orchestrator consumes repository APIs to persist authoritative gateway endpoint and rotate sandbox bridge tokens during provision/reprovision"
      pattern: "rotate_bridge_token"
    - from: "src/db/repositories/sandbox_instance_repository.py"
      to: "src/services/run_service.py"
      via: "Run path consumes repository token-resolution API to accept active and grace-period bridge tokens per sandbox"
      pattern: "resolve_bridge_tokens"
    - from: "src/db/repositories/sandbox_instance_repository.py"
      to: "src/db/models.py"
      via: "Repository writes readiness and hydration status columns used by routing"
      pattern: "hydration"
---

<objective>
Add the persistence contract needed for Daytona production-hardening: authoritative endpoint storage, per-sandbox bridge token rotation, and readiness/hydration status tracking.

Purpose: Phase 3.1 execution reliability depends on durable sandbox metadata that survives process restarts and allows deterministic token and readiness decisions.
Output: New Alembic migration, updated sandbox ORM/repository fields, and repository-level tests for token overlap and readiness state behavior.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/3.1-CONTEXT.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-RESEARCH.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-03-SUMMARY.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-UAT-GAPS-SUMMARY.md
@src/db/models.py
@src/db/repositories/sandbox_instance_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sandbox bridge-auth and readiness columns via Alembic + ORM</name>
  <files>
    src/db/migrations/versions/0006_add_sandbox_bridge_auth_and_readiness_columns.py
    src/db/models.py
  </files>
  <action>Create migration `0006` to extend `sandbox_instances` with: (a) authoritative bridge auth fields (`bridge_auth_token`, `bridge_auth_token_prev`, `bridge_auth_token_prev_expires_at`) and (b) readiness/hydration observability fields (`identity_ready`, `hydration_status`, `hydration_retry_count`, `hydration_last_error`). Keep upgrade/downgrade idempotent and nullable-safe for existing rows. Mirror the same fields in `SandboxInstance` ORM with deterministic defaults that fail closed (identity_ready false, hydration status pending/degraded states explicit).</action>
  <verify>`uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head`</verify>
  <done>Schema and ORM support bridge token overlap + readiness tracking without breaking existing sandbox rows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement repository APIs for token rotation and readiness state updates</name>
  <files>
    src/db/repositories/sandbox_instance_repository.py
  </files>
  <action>Add focused repository methods for Phase 3.1 orchestration: `rotate_bridge_token(..., grace_seconds=30)`, `resolve_bridge_tokens(...)`, `set_gateway_url_authoritative(...)`, and readiness helpers (`set_identity_ready`, `set_hydration_status`, `increment_hydration_retry`). Ensure rotation preserves old token validity for exactly 30 seconds, then expires deterministically. Avoid global token fallbacks and avoid deriving gateway URLs in consumers once authoritative value is set.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_instance_repository.py -k "token or hydration or gateway" -q`</verify>
  <done>Control-plane and bridge layers can read/write authoritative endpoint, active token, grace token, and hydration state through repository APIs.</done>
</task>

<task type="auto">
  <name>Task 3: Add repository behavior tests for grace overlap and readiness persistence</name>
  <files>
    src/tests/services/test_sandbox_instance_repository.py
  </files>
  <action>Create service/repository tests that prove: (1) rotating token keeps old token valid until grace expiry and invalid afterward, (2) authoritative gateway URL updates are persisted and replace placeholders, and (3) hydration status/retry counters persist across updates. Include at least one negative assertion that missing token/gateway state fails closed.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_instance_repository.py -q`</verify>
  <done>Token rotation, endpoint authority, and readiness persistence contracts are protected by automated tests.</done>
</task>

</tasks>

<verification>
Run `uv run alembic upgrade head && uv run pytest src/tests/services/test_sandbox_instance_repository.py -q`; confirm 0006 schema is reversible and repository behaviors enforce token grace + readiness persistence.
</verification>

<success_criteria>
Sandbox persistence layer now provides durable bridge-token rotation and readiness/hydration metadata that later Phase 3.1 execution paths can consume directly.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-01-SUMMARY.md`
</output>
