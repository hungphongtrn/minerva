---
phase: 03.1-make-daytona-production-ready-for-picoclaw-gateway-execution
plan: 04
type: execute
wave: 3
depends_on: ["03.1-02", "03.1-03"]
files_modified:
  - src/config/settings.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/scripts/daytona_base_image_preflight.py
  - src/infrastructure/sandbox/providers/daytona_base_image_prep.md
  - src/tests/services/test_sandbox_provider_adapters.py
  - src/tests/scripts/test_daytona_base_image_preflight.py
autonomous: true
gap_closure: true
user_setup:
  - service: container-registry
    why: "Publish and pin the Picoclaw base image used by Daytona sandboxes"
    env_vars:
      - name: DAYTONA_BASE_IMAGE
        source: "Registry digest reference (repo/image@sha256:...)"
      - name: DAYTONA_BASE_IMAGE_PREP_IMAGE
        source: "Candidate digest/image to validate in preflight before rollout"
    dashboard_config:
      - task: "Create pull credential/token for Daytona to access the private runtime image"
        location: "Your container registry access settings"

must_haves:
  truths:
    - "Operators can prepare a Picoclaw sandbox base image with deterministic, repeatable steps and promote it by digest."
    - "Daytona provisioning fails fast when base-image configuration violates the runtime contract instead of silently drifting."
    - "A single automated preflight command validates identity files and gateway readiness for a candidate base image before rollout."
    - "Preflight failures return actionable remediation details and do not leave disposable validation sandboxes behind."
  artifacts:
    - path: "src/config/settings.py"
      provides: "Explicit base-image contract toggles and preflight defaults"
      contains: "DAYTONA_BASE_IMAGE"
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "Provider-level base-image contract validation before sandbox create"
      contains: "_build_create_params"
    - path: "src/scripts/daytona_base_image_preflight.py"
      provides: "AsyncDaytona preflight command for disposable base-image validation"
      contains: "AsyncDaytona"
    - path: "src/infrastructure/sandbox/providers/daytona_base_image_prep.md"
      provides: "Progressive-disclosure runbook for build, publish, preflight, rollout, and rollback"
      contains: "Quick Start"
    - path: "src/tests/scripts/test_daytona_base_image_preflight.py"
      provides: "Regression tests for preflight pass/fail, cleanup, and report output"
      contains: "test_"
  key_links:
    - from: "src/config/settings.py"
      to: "src/infrastructure/sandbox/providers/daytona.py"
      via: "Provider reads strict base-image contract settings before create"
      pattern: "DAYTONA_BASE_IMAGE"
    - from: "src/scripts/daytona_base_image_preflight.py"
      to: "src/infrastructure/sandbox/providers/daytona.py"
      via: "Preflight reuses provider identity/gateway contract rules to prevent drift"
      pattern: "REQUIRED_IDENTITY_FILES|verify_identity"
    - from: "src/scripts/daytona_base_image_preflight.py"
      to: "daytona.AsyncDaytona"
      via: "Disposable sandbox create/get/delete cycle validates candidate image safely"
      pattern: "create|delete"
---

<objective>
Add deterministic base-image preparation guardrails so Daytona sandbox startup remains operationally repeatable and production-safe.

Purpose: Phase 03.1 runtime hardening is complete at code-path level, but operators still need explicit image preparation rules and a repeatable preflight gate to prevent rollout drift.
Output: Strict base-image contract validation in provider/config, an AsyncDaytona preflight CLI, and an operator runbook for build/publish/pin/rollback workflows.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/3.1-CONTEXT.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-RESEARCH.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-02-SUMMARY.md
@src/config/settings.py
@src/infrastructure/sandbox/providers/daytona.py
@src/tests/services/test_sandbox_provider_adapters.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce deterministic base-image contract at provider configuration boundary</name>
  <files>
    src/config/settings.py
    src/infrastructure/sandbox/providers/daytona.py
    src/tests/services/test_sandbox_provider_adapters.py
  </files>
  <action>Add explicit settings and provider validation for production image determinism: require digest-pinned image references when strict mode is enabled, fail fast on empty/unsafe image refs, and stamp image contract labels/env metadata during sandbox create params assembly. Reuse existing typed provider/configuration errors (do not introduce ad-hoc exception classes) so routing/error mapping stays consistent. Avoid permissive fallback behavior that silently swaps to mutable tags in production because that reintroduces rollout drift.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_provider_adapters.py -k "daytona and (base_image or digest or config)" -q`</verify>
  <done>Provider rejects non-deterministic base-image configuration before create() and accepts compliant digest-pinned configuration with explicit contract metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AsyncDaytona disposable preflight CLI for candidate base images</name>
  <files>
    src/scripts/daytona_base_image_preflight.py
    src/tests/scripts/test_daytona_base_image_preflight.py
  </files>
  <action>Create `daytona_base_image_preflight.py` as an operator-facing CLI (invoked via `uv run python ...`) that provisions a disposable sandbox from a candidate image using Daytona Python Async SDK, validates identity contract (`AGENT.md`, `SOUL.md`, `IDENTITY.md`, `skills/`), checks gateway readiness via existing provider resolution/health semantics, emits structured pass/fail output, and always tears down the disposable sandbox in `finally`. Support non-interactive exit codes (0 pass, non-zero fail) and machine-readable JSON output for CI gates.</action>
  <verify>`uv run pytest src/tests/scripts/test_daytona_base_image_preflight.py -q`</verify>
  <done>Operators can run one command to validate a candidate base image and receive deterministic pass/fail output with guaranteed sandbox cleanup.</done>
</task>

<task type="auto">
  <name>Task 3: Publish progressive-disclosure base-image prep runbook aligned to automation</name>
  <files>
    src/infrastructure/sandbox/providers/daytona_base_image_prep.md
    src/scripts/daytona_base_image_preflight.py
    src/tests/scripts/test_daytona_base_image_preflight.py
  </files>
  <action>Author a concise progressive-disclosure runbook covering Quick Start first, then detailed sections for build, publish, digest pinning, preflight command usage, rollout, and rollback. Align every runbook step to executable CLI commands from Task 2 (no hand-wavy/manual-only guidance except unavoidable registry credential setup). Add test coverage that validates CLI help and JSON schema fields referenced by the runbook so guidance and automation cannot drift.</action>
  <verify>`uv run python src/scripts/daytona_base_image_preflight.py --help && uv run pytest src/tests/scripts/test_daytona_base_image_preflight.py -k "help or json" -q`</verify>
  <done>Base-image preparation guidance is explicit, versionable, and mechanically aligned with automated preflight behavior.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_sandbox_provider_adapters.py -k "daytona and (base_image or digest or config)" -q && uv run pytest src/tests/scripts/test_daytona_base_image_preflight.py -q`; then run `uv run python src/scripts/daytona_base_image_preflight.py --help` to confirm operator guidance and deterministic preflight tooling are both present.
</verification>

<success_criteria>
Daytona base-image preparation is operationally repeatable: image refs are deterministic and validated, candidate images can be preflighted with disposable sandboxes, and rollout/rollback guidance is codified alongside executable automation.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-04-SUMMARY.md`
</output>
