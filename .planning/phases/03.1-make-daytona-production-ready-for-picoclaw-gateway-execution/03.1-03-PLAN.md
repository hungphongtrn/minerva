---
phase: 03.1-make-daytona-production-ready-for-picoclaw-gateway-execution
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/services/picoclaw_bridge_service.py
  - src/services/run_service.py
  - src/api/routes/runs.py
  - src/tests/services/test_picoclaw_bridge_service.py
  - src/tests/integration/test_phase2_1_bridge_execution.py
  - src/tests/integration/test_phase2_run_routing_errors.py
autonomous: true
must_haves:
  truths:
    - "Bridge authentication uses per-sandbox bearer tokens from sandbox metadata, not a shared global fallback token."
    - "Token rotation accepts previous token for 30 seconds during reprovision cutover, then rejects it after grace expiry."
    - "Run execution never fabricates sandbox endpoints; unresolved/failed endpoints trigger bounded recovery attempts and then typed fail-fast errors."
    - "Clients continue to execute through orchestrator `/runs` and do not receive direct Daytona endpoint contracts."
  artifacts:
    - path: "src/services/picoclaw_bridge_service.py"
      provides: "Bridge client auth + execution flow that accepts sandbox-scoped token bundle"
      contains: "execute"
    - path: "src/services/run_service.py"
      provides: "Run-time endpoint resolution and bounded recovery loop for bridge execution failures"
      contains: "_execute_via_bridge"
    - path: "src/api/routes/runs.py"
      provides: "Typed remediation mapping for endpoint resolution and bridge auth/recovery failures"
      contains: "_map_routing_error"
    - path: "src/tests/services/test_picoclaw_bridge_service.py"
      provides: "Service-level tests for per-sandbox auth token behavior and grace-period validation"
      contains: "test_"
  key_links:
    - from: "src/services/run_service.py"
      to: "src/db/repositories/sandbox_instance_repository.py"
      via: "Run execution resolves active and grace bridge tokens per sandbox before HTTP calls"
      pattern: "resolve_bridge_tokens"
    - from: "src/services/run_service.py"
      to: "src/services/picoclaw_bridge_service.py"
      via: "Bridge execute receives authoritative gateway URL + token set from routing"
      pattern: "PicoclawBridgeService"
    - from: "src/api/routes/runs.py"
      to: "src/services/run_service.py"
      via: "Typed run errors surface bounded recovery exhaustion with remediation"
      pattern: "routing_error_type"
    - from: "src/tests/integration/test_phase2_1_bridge_execution.py"
      to: "src/services/run_service.py"
      via: "Integration regression verifies missing gateway_url never falls back to fabricated endpoint construction"
      pattern: "gateway_url"
---

<objective>
Harden the `/runs` data-plane bridge path so token usage and endpoint recovery are deterministic, fail-closed, and compatible with Daytona reprovision behavior.

Purpose: Control-plane hardening is incomplete unless bridge auth and endpoint retry semantics enforce the same per-sandbox security and bounded failure model.
Output: Refactored bridge/run execution path with sandbox-scoped token resolution, bounded endpoint recovery, and regression evidence.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/3.1-CONTEXT.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-RESEARCH.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-03-SUMMARY.md
@src/services/picoclaw_bridge_service.py
@src/services/run_service.py
@src/api/routes/runs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace global bridge token fallback with sandbox-scoped token bundle auth</name>
  <files>
    src/services/picoclaw_bridge_service.py
    src/services/run_service.py
  </files>
  <action>Refactor bridge auth flow so token material is supplied per sandbox from repository data (active token plus optional grace token metadata), not from `settings.PICOCLAW_BRIDGE_TOKEN` fallback. Keep both health and execute calls bearer-authenticated, and fail closed with `AUTH_FAILED` when token context is missing or expired. Preserve bridge service timeout/retry behavior while making token source explicit in method signatures.</action>
  <verify>`uv run pytest src/tests/services/test_picoclaw_bridge_service.py -k "token or auth or grace" -q`</verify>
  <done>Bridge service executes only with sandbox-specific token inputs and enforces grace-window token validity rules.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce authoritative endpoint resolution and bounded runtime recovery in RunService</name>
  <files>
    src/services/run_service.py
    src/api/routes/runs.py
  </files>
  <action>Remove synthetic sandbox URL construction fallback from `RunService._get_sandbox_url`; require authoritative `gateway_url` from routing/repository. Implement bounded runtime recovery loop (max 3 attempts) for endpoint/transport/health failures by forcing reprovision through existing lifecycle resolution and retrying bridge execute with refreshed endpoint+token context. On exhaustion, return typed routing error with remediation and no deferred queue fallback. Keep `/runs` as sole client entrypoint and do not expose Daytona endpoint internals in API payloads.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -k "reprovision or endpoint or bridge" -q`</verify>
  <done>Runtime endpoint failures trigger bounded automatic recovery and deterministic fail-fast API errors when recovery budget is exhausted.</done>
</task>

<task type="auto">
  <name>Task 3: Add bridge and integration regressions for token rotation + endpoint fail-fast behavior</name>
  <files>
    src/tests/services/test_picoclaw_bridge_service.py
    src/tests/integration/test_phase2_1_bridge_execution.py
    src/tests/integration/test_phase2_run_routing_errors.py
  </files>
  <action>Add tests to prove: (1) active token works, grace token works only before expiry, expired grace token fails auth; (2) run execution does not fabricate URLs when gateway URL is missing; (3) endpoint failures trigger bounded recovery attempts and surface typed remediation after exhaustion; and (4) valid-pack routing regression remains intact (no false pack 4xx on runtime infrastructure failures).</action>
  <verify>`uv run pytest src/tests/services/test_picoclaw_bridge_service.py -q && uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -q && uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "valid_pack_never_returns_pack_client_errors" -q`</verify>
  <done>Bridge auth/token and endpoint recovery semantics are fully covered by service + integration regressions.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_picoclaw_bridge_service.py -q && uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -q`; confirm sandbox-scoped token auth and bounded endpoint recovery/fail-fast behavior.
</verification>

<success_criteria>
`/runs` bridge execution path is production-safe: no global token fallback, no fabricated endpoints, bounded recovery loops, and typed fail-fast errors under persistent failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-03-SUMMARY.md`
</output>
