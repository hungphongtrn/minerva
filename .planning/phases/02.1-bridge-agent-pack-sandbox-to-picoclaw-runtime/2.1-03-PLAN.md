---
phase: 02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime
plan: 03
type: execute
wave: 2
depends_on: ["2.1-01", "2.1-02"]
files_modified:
  - src/services/run_service.py
  - src/api/routes/runs.py
  - src/tests/integration/test_phase2_1_bridge_execution.py
  - src/tests/integration/test_phase2_run_routing_errors.py
autonomous: true
must_haves:
  truths:
    - "A user can invoke Picoclaw runtime through `/runs` and receive final execution output in the API response."
    - "Execution requests use workspace+pack scoped session keys for continuity, while guest requests remain per-request ephemeral."
    - "Runtime bridge failures return typed fail-closed API errors with remediation guidance (not silent success or ambiguous 400s)."
  artifacts:
    - path: "src/services/run_service.py"
      provides: "Routing + policy + bridge execution flow with session key generation and typed bridge failure propagation"
      contains: "execute_with_routing"
    - path: "src/api/routes/runs.py"
      provides: "Run API contract exposing final output and deterministic runtime bridge error mapping"
      contains: "start_run"
    - path: "src/tests/integration/test_phase2_1_bridge_execution.py"
      provides: "Integration coverage for bridge success path, session scoping, and fail-closed runtime errors"
      contains: "test_"
  key_links:
    - from: "src/api/routes/runs.py"
      to: "src/services/run_service.py"
      via: "Route delegates execution to RunService and maps typed bridge errors to HTTP responses"
      pattern: "routing_error_type"
    - from: "src/services/run_service.py"
      to: "src/services/picoclaw_bridge_service.py"
      via: "Run execution calls bridge service after routing/policy checks"
      pattern: "PicoclawBridgeService"
    - from: "src/services/run_service.py"
      to: "sandbox routing metadata"
      via: "Session key and bridge target are derived from workspace+pack scoped routing context"
      pattern: "session"
---

<objective>
Integrate bridge execution into the `/runs` path so authenticated users execute against in-sandbox Picoclaw gateway with scoped session continuity and typed fail-closed API errors.

Purpose: This is the user-visible phase outcome: routing no longer stops at sandbox selection and now completes runtime invocation with final response delivery.
Output: End-to-end `/runs` bridge invocation path with final assistant output, session scoping, and integration evidence.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-CONTEXT.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-RESEARCH.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-14-SUMMARY.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-17-SUMMARY.md
@src/services/run_service.py
@src/api/routes/runs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire run execution from routing target into Picoclaw bridge invocation</name>
  <files>src/services/run_service.py</files>
  <action>After successful routing and policy checks, call `PicoclawBridgeService` with sandbox bridge metadata from lifecycle/orchestrator outputs. Generate deterministic session keys scoped to `workspace + agent_pack` for authenticated runs (and per-request ephemeral keys for guests), enforce stale-pack fail-closed checks before execute, and include final assistant output plus runtime metadata in `RunResult.outputs`.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -k "session or stale or bridge" -q`</verify>
  <done>RunService executes real bridge path (not placeholder-only behavior), and session/stale rules are enforced before runtime calls.</done>
</task>

<task type="auto">
  <name>Task 2: Update run API contract and typed error mapping for bridge execution</name>
  <files>src/api/routes/runs.py</files>
  <action>Update `/runs` response contract to return final runtime output text on success, while preserving existing routing metadata. Map bridge failures (auth failure, health failure, timeout, upstream runtime error, stale pack) to deterministic typed HTTP errors with remediation guidance; keep existing pack validation/ownership 4xx mapping semantics intact.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -k "error_mapping or success_payload" -q`</verify>
  <done>API responses expose final bridge output on success and deterministic typed fail-closed errors on runtime bridge failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration coverage for Phase 2.1 execution truths</name>
  <files>src/tests/integration/test_phase2_1_bridge_execution.py, src/tests/integration/test_phase2_run_routing_errors.py</files>
  <action>Create integration tests that verify: (1) successful `/runs` bridge invocation returns final output, (2) health polling is required before execute, (3) bearer-auth/runtime failures fail closed with typed errors, (4) timeout/retry behavior is observable, and (5) session key scope is workspace+pack with guest ephemerality. Keep one regression assertion in `test_phase2_run_routing_errors.py` to ensure valid-pack routing continues to avoid pack-specific false 4xx errors after bridge wiring.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -q && uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "valid_pack_never_returns_pack_client_errors" -q`</verify>
  <done>Integration tests prove all Phase 2.1 user-observable execution behaviors and remain green with existing routing-regression coverage.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -q && uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "valid_pack_never_returns_pack_client_errors" -q`; confirm final output, session scope, health/auth fail-closed behavior, and timeout/retry typing are covered and passing.
</verification>

<success_criteria>
`/runs` now bridges into in-sandbox Picoclaw runtime and returns final output with workspace+pack session continuity semantics, while runtime failures remain typed and fail closed.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-03-SUMMARY.md`
</output>
