---
phase: 02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/picoclaw_bridge_service.py
  - src/config/settings.py
  - src/tests/services/test_picoclaw_bridge_service.py
autonomous: true
must_haves:
  truths:
    - "Bridge calls authenticate with bearer token and always poll sandbox /health before execution."
    - "Bridge fails closed with typed errors when health, auth, timeout, or transport checks fail."
    - "Retry and timeout behavior is deterministic and observable in bridge results."
  artifacts:
    - path: "src/services/picoclaw_bridge_service.py"
      provides: "Synchronous HTTP bridge client with health polling, auth headers, timeout/retry policy, and typed error mapping"
      contains: "class PicoclawBridgeService"
    - path: "src/config/settings.py"
      provides: "Bridge timeout/retry/auth configuration settings"
      contains: "PICOCLAW_BRIDGE"
    - path: "src/tests/services/test_picoclaw_bridge_service.py"
      provides: "Service-level regression coverage for health/auth/timeout/retry/fail-closed semantics"
      contains: "test_bridge"
  key_links:
    - from: "src/services/picoclaw_bridge_service.py"
      to: "src/config/settings.py"
      via: "Bridge timeout/retry/auth config is loaded from settings"
      pattern: "settings\.PICOCLAW_BRIDGE"
    - from: "src/services/picoclaw_bridge_service.py"
      to: "sandbox gateway /health"
      via: "pre-execution readiness poll"
      pattern: "health"
    - from: "src/services/picoclaw_bridge_service.py"
      to: "sandbox gateway execute endpoint"
      via: "Bearer-authenticated HTTP POST"
      pattern: "Authorization"
---

<objective>
Create the runtime bridge client used by `/runs` to call Picoclaw inside a provisioned sandbox with explicit health, auth, timeout, retry, and fail-closed behavior.

Purpose: Phase 2.1 requires deterministic synchronous HTTP invocation to Picoclaw gateway, and this bridge service is the core integration boundary.
Output: A reusable `PicoclawBridgeService` with typed failure modes plus tests proving health polling and bearer-authenticated execution.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-CONTEXT.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-RESEARCH.md
@src/config/settings.py
@src/services/run_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Picoclaw bridge service with health-first fail-closed flow</name>
  <files>src/services/picoclaw_bridge_service.py</files>
  <action>Create `PicoclawBridgeService` for synchronous HTTP execution against in-sandbox Picoclaw gateway. Require pre-execution `/health` polling before any execute request, include bearer token auth on both health and execute calls, and return typed errors for health failure, auth failure, timeout, upstream non-2xx responses, and malformed payloads. Keep external contract in Minerva envelope terms (do not leak raw Picoclaw payload shape to callers).</action>
  <verify>`uv run pytest src/tests/services/test_picoclaw_bridge_service.py -k "health or auth or fail_closed" -q`</verify>
  <done>Service exposes deterministic success/error results and never attempts execution when health polling fails.</done>
</task>

<task type="auto">
  <name>Task 2: Add bridge configuration knobs in settings</name>
  <files>src/config/settings.py</files>
  <action>Add explicit bridge settings for health timeout, health retries/backoff, execution timeout, and execute retry budget. Keep defaults conservative (bounded retries, bounded timeout), and ensure values are environment-overridable without code edits. Do not add secrets to code defaults; secrets remain environment-provided.</action>
  <verify>`uv run python -c "from src.config.settings import settings; print(bool(settings))"`</verify>
  <done>Bridge service can read timeout/retry/auth-related runtime config from environment-backed settings.</done>
</task>

<task type="auto">
  <name>Task 3: Lock behavior with service tests</name>
  <files>src/tests/services/test_picoclaw_bridge_service.py</files>
  <action>Add focused tests for: (1) health is polled before execution, (2) bearer token is attached, (3) health/auth failures short-circuit execute path, (4) timeout and retry behavior is bounded, and (5) error typing is stable for API mapping. Use mocked HTTP transport; do not require live sandbox runtime for this plan.</action>
  <verify>`uv run pytest src/tests/services/test_picoclaw_bridge_service.py -q`</verify>
  <done>Tests prove health/auth/timeout/retry/fail-closed semantics and pass green.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_picoclaw_bridge_service.py -q`; confirm all bridge service tests pass and execution calls are blocked when health/auth checks fail.
</verification>

<success_criteria>
Picoclaw bridge client exists, enforces health-first bearer-authenticated execution, and emits deterministic typed failures for timeout/retry and transport errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-01-SUMMARY.md`
</output>
