services:
  postgres:
    image: postgres:16-alpine
    container_name: picoclaw-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-picoclaw}
      POSTGRES_USER: ${POSTGRES_USER:-picoclaw}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-picoclaw_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-picoclaw} -d ${POSTGRES_DB:-picoclaw}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres_data:
    driver: local
