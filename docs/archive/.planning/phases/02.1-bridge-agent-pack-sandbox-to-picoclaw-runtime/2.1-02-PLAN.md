---
phase: 02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/sandbox/providers/base.py
  - src/services/sandbox_orchestrator_service.py
  - src/infrastructure/sandbox/providers/local_compose.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/tests/services/test_sandbox_provider_adapters.py
  - src/tests/services/test_sandbox_routing_service.py
autonomous: true
must_haves:
  truths:
    - "Each sandbox gets deterministic Picoclaw `config.json` with bridge-only channels and credentials sourced from environment variables."
    - "Agent pack materialization uses snapshot copy/sync behavior, not live host bind semantics."
    - "Source changes after materialization are detected as stale and execution path can fail closed."
  artifacts:
    - path: "src/services/sandbox_orchestrator_service.py"
      provides: "Provisioning flow that resolves pack digests and passes runtime bridge/config metadata to providers"
      contains: "_provision_sandbox"
    - path: "src/infrastructure/sandbox/providers/local_compose.py"
      provides: "Local profile snapshot materialization + runtime config generation metadata"
      contains: "provision_sandbox"
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "Daytona profile snapshot materialization + runtime config generation metadata"
      contains: "provision_sandbox"
    - path: "src/tests/services/test_sandbox_provider_adapters.py"
      provides: "Parity tests for materialization/config behavior across providers"
      contains: "pack"
  key_links:
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/base.py"
      via: "Extended SandboxConfig passes pack digest and runtime config inputs"
      pattern: "SandboxConfig"
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/local_compose.py"
      via: "Provisioning sends snapshot materialization contract to local provider"
      pattern: "pack_source_path"
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/daytona.py"
      via: "Provisioning sends snapshot materialization contract to daytona provider"
      pattern: "pack_source_path"
---

<objective>
Wire provisioning-time runtime surface setup: deterministic bridge-ready config generation, snapshot pack materialization, and stale detection metadata for fail-closed routing.

Purpose: Phase 2.1 cannot execute safely unless each sandbox is provisioned with isolated runtime config and a deterministic pack snapshot.
Output: Provider-parity provisioning contract that materializes packs, generates runtime config, and exposes stale-detection inputs.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-CONTEXT.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-RESEARCH.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-09-SUMMARY.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-10-SUMMARY.md
@src/services/sandbox_orchestrator_service.py
@src/infrastructure/sandbox/providers/base.py
@src/infrastructure/sandbox/providers/local_compose.py
@src/infrastructure/sandbox/providers/daytona.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend provisioning contract for bridge runtime config and digest-aware pack metadata</name>
  <files>src/infrastructure/sandbox/providers/base.py, src/services/sandbox_orchestrator_service.py</files>
  <action>Extend `SandboxConfig` and orchestrator provisioning inputs to carry runtime bridge/config generation parameters and pack digest metadata needed for stale detection. Reuse existing pack validation digest flow (do not invent a second digest algorithm). Fail closed when digest/materialization prerequisites are missing or inconsistent for pack-bound provisioning.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_routing_service.py -k "pack and provision" -q`</verify>
  <done>Orchestrator passes deterministic runtime/pack metadata into provider provisioning and rejects invalid or stale pack states before execution routing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement provider-specific snapshot materialization and config generation parity</name>
  <files>src/infrastructure/sandbox/providers/local_compose.py, src/infrastructure/sandbox/providers/daytona.py</files>
  <action>Implement provider-specific pack materialization as snapshot copy/sync semantics (no live bind semantics) and generate per-sandbox Picoclaw `config.json` with bridge-only channels enabled and public channels disabled. Ensure sensitive credentials remain env-var sourced and are not persisted in pack files or DB. Expose materialized pack digest/config metadata needed by run execution for stale checks and bridge target resolution.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_provider_adapters.py -k "pack_binding or materialization or config" -q`</verify>
  <done>Both providers expose equivalent bridge-ready metadata and materialized pack behavior without live source coupling.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity and fail-closed regression coverage for provisioning behavior</name>
  <files>src/tests/services/test_sandbox_provider_adapters.py, src/tests/services/test_sandbox_routing_service.py</files>
  <action>Add tests proving: (1) generated config is sandbox-scoped and bridge-only, (2) provider metadata includes materialized digest/config references, and (3) stale or inconsistent pack metadata causes fail-closed routing/provision behavior. Keep assertions profile-portable across local_compose and daytona where semantics should match.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_provider_adapters.py -k "config or pack" -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -k "stale or fail_closed" -q`</verify>
  <done>Service and provider tests demonstrate config/materialization/stale-detection contract with fail-closed guarantees.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_sandbox_provider_adapters.py -k "config or pack" -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -k "stale or fail_closed" -q`; confirm provider parity and fail-closed provisioning checks pass.
</verification>

<success_criteria>
Provisioning now emits bridge-ready sandbox config and snapshot materialization metadata for both profiles, and stale pack states are rejected deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-02-SUMMARY.md`
</output>
