---
phase: 03.3-close-pack-mount-isolation-and-identity-collision-gaps
plan: 04
subsystem: cli
 tags:
  - cli
  - preflight
  - workspace
  - oss
  - fail-closed

# Dependency graph
requires:
  - phase: 03.3-01
    provides: ExternalIdentity model and workspace validation logic
  - phase: 03.3-03
    provides: Identity forwarding infrastructure for OSS mode
provides:
  - Workspace-config preflight gate in minerva serve
  - Regression tests preventing preflight wiring regressions
  - Fail-closed behavior for invalid workspace configuration
affects:
  - 03.3-05
  - 03.3-06
  - serve-command
  - oss-deployments

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Preflight gate pattern: explicit validation before server startup"
    - "Fail-closed security: deny access when configuration is uncertain"

key-files:
  created:
    - src/tests/cli/test_serve_preflight_workspace_gate.py
  modified:
    - src/cli/commands/serve.py
    - src/services/preflight_service.py

key-decisions:
  - "Workspace check is Gate 2 (BLOCKING) after schema, before snapshot"
  - "Public check_workspace_configured() wrapper exposes private _check_workspace_configured()"
  - "Tests use MagicMock with CheckStatus enum values for proper comparisons"

patterns-established:
  - "Preflight gate: Each gate returns 1 immediately on failure, preventing server start"
  - "Regression test pattern: Mock service and assert uvicorn.run is never called on failure"

requirements-completed:
  - AUTH-03
  - AGNT-02

# Metrics
duration: 2min
completed: 2026-03-02
---

# Phase 03.3 Plan 04: Wire Workspace-Config Preflight into Serve Startup

**Workspace-config preflight gate wired into `minerva serve` startup, ensuring OSS deployments fail closed when MINERVA_WORKSPACE_ID is missing/invalid/points to workspace with no active agent packs.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-02T18:23:29Z
- **Completed:** 2026-03-02T18:26:21Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added public `check_workspace_configured()` method to PreflightService as CLI-friendly wrapper
- Implemented workspace-config as Gate 2 (BLOCKING) in serve startup sequence
- Created comprehensive regression tests proving fail-closed behavior
- Updated serve docstring to document all 3 preflight gates

## Task Commits

Each task was committed atomically:

1. **Task 1: Add regression tests** - `cc9526c` (test)
2. **Task 2: Wire workspace-config gate** - `dab40a5` (feat)

**Plan metadata:** (this summary)

## Files Created/Modified

- `src/tests/cli/test_serve_preflight_workspace_gate.py` - 5 regression tests covering fail-closed scenarios
- `src/cli/commands/serve.py` - Added Gate 2 workspace check, updated docstring
- `src/services/preflight_service.py` - Added public `check_workspace_configured()` method

## Decisions Made

1. **Gate ordering:** Workspace check positioned after schema (data integrity) and before snapshot (optional infrastructure) — workspace is required for OSS identity resolution
2. **Public wrapper pattern:** Exposed `_check_workspace_configured()` via `check_workspace_configured()` to maintain private implementation details while enabling CLI usage
3. **Test mocking strategy:** Used `MagicMock` with direct `CheckStatus` enum assignment to match the actual comparison logic in serve.py

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - tests initially failed as expected (proving they work), then passed after wiring implementation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Workspace preflight gate is active and tested
- Server will refuse to start without valid workspace configuration
- Ready for integration testing of full OSS deployment flow

---
*Phase: 03.3-close-pack-mount-isolation-and-identity-collision-gaps*
*Completed: 2026-03-02*
