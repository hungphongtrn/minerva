---
phase: 03.3-close-pack-mount-isolation-and-identity-collision-gaps
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/sandbox/providers/daytona.py
  - src/infrastructure/sandbox/providers/local_compose.py
  - src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
  - src/tests/services/test_sandbox_provider_adapters.py
autonomous: true
requirements:
  - AGNT-03

must_haves:
  truths:
    - "Pack volume mounted read-only at /workspace/pack and never receives dynamic runtime data"
  artifacts:
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "Daytona VolumeMount pack mount is read-only"
      contains: "VolumeMount"
    - path: "src/infrastructure/sandbox/providers/local_compose.py"
      provides: "Local compose exposes and enforces pack mount read-only contract"
      contains: "pack_mount_read_only"
    - path: "src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py"
      provides: "Test asserts read-only flag on Daytona pack mount"
      contains: "read_only"
    - path: "src/tests/services/test_sandbox_provider_adapters.py"
      provides: "Parity tests assert pack mount read-only contract metadata"
      contains: "pack_mount_read_only"
  key_links:
    - from: "src/infrastructure/sandbox/providers/daytona.py"
      to: "daytona.VolumeMount"
      via: "CreateSandboxFromSnapshotParams.volumes"
      pattern: "VolumeMount\(.*mount_path=\"/workspace/pack\""
---

<objective>
Enforce the pack mount read-only contract across Daytona and local_compose providers.

Purpose: Pack volume is immutable identity+skills content mounted at `/workspace/pack`. Dynamic runtime data must live in `/home/daytona/workspace`. Read-only enforcement prevents accidental writes and makes the isolation contract verifiable.

Output: Daytona VolumeMount uses a read-only flag; local_compose exposes/enforces a read-only contract with parity tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-close-pack-mount-isolation-and-identity-collision-gaps/03.3-CONTEXT.md
@.planning/phases/03.3-close-pack-mount-isolation-and-identity-collision-gaps/03.3-VERIFICATION.md
@src/infrastructure/sandbox/providers/daytona.py
@src/infrastructure/sandbox/providers/local_compose.py
@src/infrastructure/sandbox/providers/base.py
@src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
@src/tests/services/test_sandbox_provider_adapters.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for read-only pack mount contract (Daytona + local_compose)</name>
  <files>
    src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
    src/tests/services/test_sandbox_provider_adapters.py
  </files>
  <action>
1. Update `src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py`:
   - In `TestVolumeMountWiring.test_provision_mounts_volume_at_workspace_pack`, assert the pack mount is marked read-only.
   - Determine the correct attribute name based on the installed `daytona` SDK object:
     - Use `getattr(volume_mount, 'read_only', None)` or `getattr(volume_mount, 'readonly', None)`.
     - Assert the resolved value is `True`.
   - Keep the existing mount_path and volume_id assertions.

2. Update `src/tests/services/test_sandbox_provider_adapters.py`:
   - In `test_pack_binding_metadata_parity_local_compose`, assert:
     - `info.ref.metadata.get('pack_mount_path') == '/workspace/pack'`
     - `info.ref.metadata.get('pack_mount_read_only') is True` when pack_bound
   - In `test_pack_binding_metadata_parity_daytona`, assert the same metadata contract on `info.ref.metadata`.
   - These are contract tests: they must not require real Daytona credentials.
  </action>
  <verify>
    <automated>cd /Users/phong/Workspace/minerva && uv run pytest src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py::TestVolumeMountWiring::test_provision_mounts_volume_at_workspace_pack src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_local_compose src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_daytona -q</automated>
  </verify>
  <done>
    - Tests assert Daytona volume mount includes a read-only flag
    - Parity tests assert both providers expose pack_mount_read_only metadata when pack is bound
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement read-only enforcement in provider mount configuration</name>
  <files>
    src/infrastructure/sandbox/providers/daytona.py
    src/infrastructure/sandbox/providers/local_compose.py
  </files>
  <action>
1. In `src/infrastructure/sandbox/providers/daytona.py` within `_build_create_params()`:
   - Update the `VolumeMount(...)` for the pack volume to include the SDK-supported read-only flag.
   - Before coding, confirm the correct parameter name with:
     - `uv run python -c "import inspect; from daytona import VolumeMount; print(inspect.signature(VolumeMount))"`
   - Use the discovered parameter name to set the mount read-only (expected: `read_only=True`).
   - Keep mount_path as `/workspace/pack` and keep the deterministic volume_id.

2. In `src/infrastructure/sandbox/providers/local_compose.py`:
   - Treat pack mount as read-only contractually for pack-bound sandboxes:
     - Add `pack_mount_read_only: bool` to sandbox metadata returned from `_to_info` (when pack_bound=True, set True).
     - Ensure metadata always includes `pack_mount_path` equal to `PACK_MOUNT_PATH`.
   - Add a fail-fast contract guard in provisioning:
     - If any provider-generated dynamic paths (config path, workspace path) are under `PACK_MOUNT_PATH`, raise `SandboxConfigurationError` (fail closed).
     - This keeps local_compose parity verifiable even though it is a simulation.

3. Do not move dynamic data back onto `/workspace/pack`; only enforce read-only semantics.
  </action>
  <verify>
    <automated>cd /Users/phong/Workspace/minerva && uv run pytest src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py::TestVolumeMountWiring::test_provision_mounts_volume_at_workspace_pack src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_local_compose src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_daytona -q</automated>
  </verify>
  <done>
    - Daytona provider mounts pack volume read-only at `/workspace/pack`
    - local_compose provider exposes pack_mount_read_only=True in metadata for pack-bound sandboxes
    - local_compose fails closed if any dynamic path would be placed under `/workspace/pack`
  </done>
</task>

</tasks>

<verification>
1. `uv run python -c "import inspect; from daytona import VolumeMount; print(inspect.signature(VolumeMount))"` shows a read-only option (or confirms attribute name)
2. `uv run pytest src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py::TestVolumeMountWiring::test_provision_mounts_volume_at_workspace_pack -q` passes
3. `uv run pytest src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_local_compose -q` passes
4. `uv run pytest src/tests/services/test_sandbox_provider_adapters.py::TestSemanticParityLifecycle::test_pack_binding_metadata_parity_daytona -q` passes
</verification>

<success_criteria>
- Pack volume mount is read-only in Daytona create parameters
- Provider parity tests prove pack mount read-only contract is exposed and enforced
- No dynamic runtime writes target `/workspace/pack`
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-close-pack-mount-isolation-and-identity-collision-gaps/03.3-05-SUMMARY.md`
</output>
