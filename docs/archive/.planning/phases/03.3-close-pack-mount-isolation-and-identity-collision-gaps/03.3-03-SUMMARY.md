---
phase: 03.3-close-pack-mount-isolation-and-identity-collision-gaps
plan: 03
subsystem: bridge

tags: [picoclaw, bridge, sender_id, session_id, identity-forwarding, local_compose, mount-isolation]

requires:
  - phase: 03.3-01
    provides: ExternalIdentity model with external_user_id for sender_id resolution
  - phase: 03.3-02
    provides: Mount isolation constants (WORKSPACE_PATH, PACK_MOUNT_PATH, CONFIG_PATH)

provides:
  - Bridge service with sender_id and session_id forwarding to Picoclaw
  - RunService wiring external_user_id through to bridge execution
  - Guest sender_id handling (uses "guest" for guest requests)
  - local_compose provider with full mount isolation parity with Daytona
  - Per-user sandbox keying in local_compose via _generate_user_ref
  - Workspace symlink creation in local_compose for isolation

affects:
  - OSS /runs endpoint
  - Picoclaw conversation scoping
  - Multi-user sandbox isolation

tech-stack:
  added: []
  patterns:
    - "Identity forwarding: external_user_id → sender_id in Picoclaw payload"
    - "Guest handling: principal.is_guest → 'guest' sender_id"
    - "Mount isolation parity: local_compose uses same paths as Daytona"
    - "Per-user sandbox keying: SHA-256 hash of (workspace_id, external_user_id)"

key-files:
  created: []
  modified:
    - src/services/picoclaw_bridge_service.py - Added sender_id/session_id parameters and forwarding
    - src/services/run_service.py - Wired sender_id from principal to bridge
    - src/infrastructure/sandbox/providers/local_compose.py - Full isolation parity with Daytona

key-decisions:
  - "Forward raw external_user_id (from X-User-ID) as sender_id to Picoclaw - no hashing or transformation"
  - "Guest requests use generic 'guest' as sender_id - no unique identifier for guests"
  - "Full mount isolation parity: local_compose uses same WORKSPACE_PATH as Daytona in this phase"
  - "config.json written outside pack volume (CONFIG_PATH) for isolation"

patterns-established:
  - "Identity forwarding: Bridge accepts sender_id/session_id and forwards to Picoclaw payload"
  - "Guest detection: RunService checks principal.is_guest to set 'guest' sender_id"
  - "Isolation parity: local_compose mirrors Daytona mount isolation pattern"

requirements-completed:
  - AUTH-03
  - AGNT-02
  - AGNT-03

duration: 5min
completed: 2026-03-02
---

# Phase 03.3 Plan 03: Wire Identity Forwarding and Local Compose Parity Summary

**Bridge service forwarding raw external_user_id as sender_id and session_id to Picoclaw, with local_compose provider achieving full mount isolation parity with Daytona.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-02T17:57:28Z
- **Completed:** 2026-03-02T18:03:19Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- **Bridge sender_id forwarding** - `_transform_to_picoclaw_request` now accepts `sender_id` parameter (replacing hardcoded "minerva") and `session_id` for thread continuity
- **Bridge execute methods updated** - `execute()` and `execute_via_bridge()` both accept and forward sender_id/session_id
- **RunService identity wiring** - `_execute_via_bridge` now receives sender_id from principal.external_user_id (or "guest" for guests)
- **Guest handling** - Guest requests (principal.is_guest) use "guest" as sender_id per user decision
- **local_compose parity** - Full mount isolation parity with Daytona:
  - Config workspace set to WORKSPACE_PATH (/home/daytona/workspace)
  - Config path outside pack volume (CONFIG_PATH)
  - Per-user sandbox keying via _generate_user_ref
  - Workspace symlink creation via _create_workspace_symlinks

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix bridge sender_id forwarding and update OSS /runs route** - `e6d1a38` (feat)
2. **Task 2: Implement local_compose mount isolation parity with Daytona** - `6fe971a` (feat)

**Plan metadata:** [pending] (docs: complete plan)

## Files Created/Modified

- `src/services/picoclaw_bridge_service.py` - Added sender_id/session_id to _transform_to_picoclaw_request, execute(), execute_via_bridge()
- `src/services/run_service.py` - Wired sender_id from principal through to _execute_via_bridge
- `src/infrastructure/sandbox/providers/local_compose.py` - Full isolation parity: imports constants, updated config, _generate_user_ref, _create_workspace_symlinks, per-user sandbox keying

## Decisions Made

- **Raw external_user_id forwarding** - Forward the raw external_user_id (from X-User-ID) as sender_id to Picoclaw via the bridge. No hashing or transformation.
- **Guest sender_id** - Guest requests (X-User-ID matches GUEST_ID) use generic 'guest' as sender_id - the agent knows it's a guest but has no unique identifier.
- **Full isolation parity now** - local_compose uses the same isolation pattern as Daytona in this phase (not a future enhancement).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - all changes implemented smoothly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- ✅ Bridge forwards raw external_user_id as sender_id (not hardcoded "minerva")
- ✅ Bridge forwards session_id in payload
- ✅ Guest requests use "guest" as sender_id
- ✅ OSS /runs route wires identity through to bridge execution
- ✅ local_compose has full mount isolation parity with Daytona
- ✅ local_compose config.json no longer written inside /workspace/pack
- ✅ Both providers support per-user sandbox keying

**Phase 03.3 complete!** All three plans finished:
- Plan 01: External Identity Infrastructure (identity collision fix)
- Plan 02: Mount Isolation and Per-User Routing
- Plan 03: Wire Identity Forwarding and Local Compose Parity

---

*Phase: 03.3-close-pack-mount-isolation-and-identity-collision-gaps*
*Completed: 2026-03-02*

## Self-Check: PASSED

- [x] SUMMARY.md created at `.planning/phases/03.3-close-pack-mount-isolation-and-identity-collision-gaps/03.3-03-SUMMARY.md`
- [x] All task commits present (e6d1a38, 6fe971a)
- [x] Bridge sender_id forwarding verified programmatically
- [x] local_compose parity constants verified
- [x] All imports successful
- [x] No hardcoded "minerva" as sole sender_id path
