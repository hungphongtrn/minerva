---
status: testing
phase: 03.1-make-daytona-production-ready-for-picoclaw-gateway-execution
source:
  - 03.1-01-SUMMARY.md
  - 03.1-02-SUMMARY.md
  - 03.1-03-SUMMARY.md
  - 03.1-04-SUMMARY.md
started: 2026-02-26T16:20:19Z
updated: 2026-03-01T16:56:43Z
---

## Current Test

number: 3
name: Bridge Token Rotation Grace Window
expected: |
  During reprovision token cutover, requests signed with previous token still work for about 30 seconds, then old token is rejected while current token continues to work.
awaiting: user response

## Tests

### 1. Runs Execute via Authoritative Gateway
expected: With a ready Daytona sandbox (identity_ready true, gateway_url present, bridge token available), a `/runs` request executes through the bridge and returns normal run output instead of a routing failure.
result: skipped
reason: Need environment bootstrap first (server/sandbox/agent setup not ready yet)

### 2. Missing Gateway Fails Closed
expected: If sandbox `gateway_url` is missing or unresolved, `/runs` fails fast with typed bridge endpoint error and remediation guidance; no fabricated fallback URL is used.
result: issue
reported: "I dont think this is a good idea. The reason is that everyboday will then can access to DAYTONA_BASE_IMAGE. This is also a gap. We need a mechanism to prepare the base image during deployment process."
severity: major

### 3. Bridge Token Rotation Grace Window
expected: During reprovision token cutover, requests signed with previous token still work for about 30 seconds, then old token is rejected while current token continues to work.
result: pending

### 4. Missing Bridge Token Fails Closed
expected: If bridge token bundle is unavailable, `/runs` returns a typed auth failure with remediation and does not forward an unauthenticated bridge request.
result: pending

### 5. Recoverable Bridge Failures Use Bounded Retry
expected: Transport/timeout/health bridge failures trigger bounded recovery attempts (max 3) with reprovision and then return typed 503/504 errors with remediation if still failing.
result: pending

### 6. Identity Readiness Is a Hard Gate
expected: A sandbox missing required identity files (AGENT.md, SOUL.md, IDENTITY.md, skills/) is not treated as ready; request path blocks execution until reprovision or readiness is restored.
result: pending

### 7. Base Image Guardrails and Preflight CLI
expected: Strict mode rejects mutable base image tags, and `uv run python src/scripts/daytona_base_image_preflight.py --image <digest> --json` returns deterministic validation results with remediation and cleanup.
result: pending

## Summary

total: 7
passed: 0
issues: 1
pending: 5
skipped: 1

## Gaps

- truth: "If sandbox `gateway_url` is missing or unresolved, `/runs` fails fast with typed bridge endpoint error and remediation guidance; no fabricated fallback URL is used."
  status: failed
  reason: "User reported: I dont think this is a good idea. The reason is that everyboday will then can access to DAYTONA_BASE_IMAGE. This is also a gap. We need a mechanism to prepare the base image during deployment process."
  severity: major
  test: 2
  artifacts: []
  missing: []
