---
phase: 03.1-make-daytona-production-ready-for-picoclaw-gateway-execution
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/config/settings.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/services/sandbox_orchestrator_service.py
  - src/tests/services/test_sandbox_provider_adapters.py
  - src/tests/services/test_sandbox_routing_service.py
autonomous: true
must_haves:
  truths:
    - "Daytona sandboxes are provisioned from a configured registry image with identity files validated before being marked request-ready."
    - "Gateway endpoint resolution for Daytona follows one authoritative path and is persisted for bridge execution instead of ad-hoc URL construction."
    - "Identity/gateway health failures trigger bounded auto-reprovision (3 attempts) and then fail fast with typed remediation context."
    - "Checkpoint hydration is triggered asynchronously after readiness and does not block request routing."
  artifacts:
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "SDK-backed Daytona provisioning with image params, identity verification, and gateway URL extraction"
      contains: "provision_sandbox"
    - path: "src/services/sandbox_orchestrator_service.py"
      provides: "Layered readiness orchestration with bounded reprovision and async hydration kickoff"
      contains: "resolve_sandbox"
    - path: "src/tests/services/test_sandbox_provider_adapters.py"
      provides: "Provider-level regression coverage for Daytona create parameters and gateway resolution"
      contains: "test_daytona"
    - path: "src/tests/services/test_sandbox_routing_service.py"
      provides: "Routing-level coverage for identity gate failures, reprovision budget, and non-blocking hydration"
      contains: "test_"
  key_links:
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/daytona.py"
      via: "Orchestrator calls Daytona provisioning + identity verification before ACTIVE routing"
      pattern: "verify_identity"
    - from: "src/infrastructure/sandbox/providers/daytona.py"
      to: "src/db/repositories/sandbox_instance_repository.py"
      via: "Resolved Daytona gateway endpoint is persisted as authoritative gateway_url"
      pattern: "gateway_url"
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/services/checkpoint_restore_service.py"
      via: "Health-ready sandbox triggers async hydration with degraded retry state updates"
      pattern: "restore_workspace"
    - from: "src/tests/services/test_sandbox_routing_service.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "Regression test verifies hydration kickoff is async and does not block request-ready routing"
      pattern: "non_blocking.*hydration|hydration.*non_blocking"
---

<objective>
Harden Daytona control-plane provisioning and readiness orchestration so `/runs` receives deterministic, production-safe sandbox targets.

Purpose: Phase 3.1 requires authoritative gateway resolution, identity-first readiness, and bounded recovery loops before any bridge execution can be trusted in production.
Output: Production-grade Daytona provider flow, orchestrator-level readiness/reprovision behavior, and service tests that lock these behaviors.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/3.1-CONTEXT.md
@.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-RESEARCH.md
@.planning/phases/02.1-bridge-agent-pack-sandbox-to-picoclaw-runtime/2.1-02-SUMMARY.md
@src/infrastructure/sandbox/providers/daytona.py
@src/services/sandbox_orchestrator_service.py
@src/services/checkpoint_restore_service.py
@src/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Daytona placeholder provisioning with image-first, identity-verified startup</name>
  <files>
    src/config/settings.py
    src/infrastructure/sandbox/providers/daytona.py
  </files>
  <action>Implement real SDK provisioning using Daytona image parameters (configured base image, labels, env vars, and `auto_stop_interval=0` for runtime continuity). Remove placeholder/simulation-only branches in Daytona provisioning/materialization code. Add provider helpers to verify required identity mount completeness (`AGENT.md`, `SOUL.md`, `IDENTITY.md`, `skills/`) and to resolve gateway endpoint from Daytona sandbox metadata/preview URLs in one canonical method. Return typed provider errors when identity or endpoint resolution is incomplete.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_provider_adapters.py -k "daytona and (provision or identity or gateway)" -q`</verify>
  <done>Daytona provisioning is SDK-backed, image-first, identity-gated, and produces authoritative gateway endpoint metadata without placeholder behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add orchestrator layered readiness with bounded auto-reprovision and async hydration</name>
  <files>
    src/services/sandbox_orchestrator_service.py
  </files>
  <action>Update `resolve_sandbox`/provision flow to enforce layered gates: identity completeness (hard gate) -> gateway health (ready gate) -> async checkpoint hydration (non-blocking). On identity or endpoint/health failures, stop/reprovision with bounded retry budget of 3 and exponential backoff; on budget exhaustion return typed fail-fast `PROVISION_FAILED` messaging with remediation. Persist authoritative gateway URL and rotate per-sandbox bridge token through repository APIs from plan 03.1-01. Kick off hydration in background task and mark degraded retry state when hydration fails, without blocking successful routing.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_routing_service.py -k "reprovision or identity" -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -k "hydration and non_blocking" -q`</verify>
  <done>Routing returns request-ready sandboxes only after identity+health gates, retries are bounded, and hydration is asynchronous with degraded tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Add provider and orchestration regressions for readiness and bounded recovery</name>
  <files>
    src/tests/services/test_sandbox_provider_adapters.py
    src/tests/services/test_sandbox_routing_service.py
  </files>
  <action>Add tests that prove: (1) Daytona create call uses explicit image/runtime config, (2) identity mount failure triggers bounded reprovision attempts and eventually fails closed, (3) authoritative gateway URL is persisted once and reused, and (4) hydration failures mark degraded + retries without blocking routing success. Include a dedicated non-blocking hydration assertion/test name so async hydration behavior is explicitly validated. Keep assertions deterministic and avoid real Daytona network calls (SDK mocks only).</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_provider_adapters.py -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -k "reprovision or identity" -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -k "hydration and non_blocking" -q`</verify>
  <done>Daytona control-plane hardening behaviors are pinned by automated tests and safe against regression.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_sandbox_provider_adapters.py -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -k "reprovision or identity" -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -k "hydration and non_blocking" -q`; confirm identity-first readiness, authoritative endpoint persistence, bounded reprovision, and explicitly non-blocking async hydration behavior.
</verification>

<success_criteria>
Daytona control-plane flow is production-hardened: image-based startup, identity hard gate, authoritative gateway endpoint, bounded reprovision, and non-blocking hydration.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-make-daytona-production-ready-for-picoclaw-gateway-execution/03.1-02-SUMMARY.md`
</output>
