---
phase: 03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 03.4-02
files_modified:
  - src/integrations/zeroclaw/spec.py
  - src/integrations/zeroclaw/spec.schema.json
  - src/integrations/zeroclaw/spec.template.json
  - src/tests/integrations/test_zeroclaw_spec.py
autonomous: false
requirements:
  - MIGRATE-01

must_haves:
  truths:
    - "If we decide to migrate, Zeroclaw's contract is captured in-repo in a validated, machine-readable spec file."
  artifacts:
    - path: src/integrations/zeroclaw/spec.schema.json
      provides: "Schema defining required fields for Zeroclaw integration"
    - path: src/integrations/zeroclaw/spec.py
      provides: "Loader/validator used by runtime integration"
    - path: src/integrations/zeroclaw/spec.template.json
      provides: "Copy/paste template for the spec"
  key_links:
    - from: src/integrations/zeroclaw/spec.py
      to: src/services/zeroclaw_gateway_service.py
      via: "Spec-driven endpoints/auth configuration"
      pattern: "load_zeroclaw_spec"
---

<objective>
Gate migration on an explicit decision (based on the audit report) and capture Zeroclaw's API/runtime contract in a validated in-repo spec file so implementation can proceed deterministically.

Purpose: Remove ambiguity and avoid hand-waving migration work without a contract.
Output: Spec schema + loader + template + validation test.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-RESEARCH.md

@src/services/run_service.py
@src/services/picoclaw_bridge_service.py
@src/infrastructure/sandbox/providers/daytona.py
</context>

<tasks>

<task type="auto">
  <name>task 1: add Zeroclaw spec schema + loader + template</name>
  <files>
src/integrations/zeroclaw/spec.schema.json
src/integrations/zeroclaw/spec.template.json
src/integrations/zeroclaw/spec.py
  </files>
  <action>
Create a small Zeroclaw spec system under `src/integrations/zeroclaw/`:

- `spec.schema.json`: JSON Schema describing the required fields for migration. Minimum required keys:
  - `version` (string)
  - `gateway.port` (int)
  - `gateway.health_path` (string)
  - `gateway.execute_path` (string)
  - `gateway.stream_mode` (enum: `none`, `sse`, `ws`)
  - `auth.mode` (enum: `bearer`, `none`)
  - `runtime.config_path` (string, absolute path in sandbox)
  - `runtime.start_command` (string, shell command executed in sandbox)
  - `examples.execute_request` (object)
  - `examples.execute_response` (object)

- `spec.template.json`: A template that satisfies the schema structurally and uses placeholder values.

- `spec.py`: Implement `load_zeroclaw_spec(path: str = "src/integrations/zeroclaw/spec.json") -> ZeroclawSpec` using Pydantic v2 models. Validation rules:
  - paths must start with `/`
  - `runtime.config_path` must not live under `/workspace/pack` (mount isolation)
  - `gateway.port` must be 1..65535

Do not add any new dependency; use existing pydantic.
  </action>
  <verify>
uv run python -c "from src.integrations.zeroclaw.spec import load_zeroclaw_spec; import pathlib; p=pathlib.Path('src/integrations/zeroclaw/spec.template.json'); print(p.exists())"
  </verify>
  <done>
Zeroclaw spec schema, template, and loader exist and enforce deterministic validation rules.
  </done>
</task>

<task type="auto">
  <name>task 2: add validation tests for spec loader</name>
  <files>
src/tests/integrations/test_zeroclaw_spec.py
  </files>
  <action>
Create `src/tests/integrations/test_zeroclaw_spec.py` validating:
- Loading `spec.template.json` succeeds.
- Invalid `gateway.port` fails.
- `runtime.config_path` under `/workspace/pack` fails.

Keep tests fast/offline.
  </action>
  <verify>
uv run pytest src/tests/integrations/test_zeroclaw_spec.py -q
  </verify>
  <done>
Spec loader tests pass and establish deterministic validation for the migration contract.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>task 3: decision gate - proceed with Zeroclaw migration?</name>
  <files>
src/integrations/zeroclaw/spec.json
.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md
  </files>
  <action>
Use `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md` as the source of truth.

Decision:
- Option A (keep Picoclaw): Audit verdict is PASS and we stop migration. Do NOT execute Plans 03.4-04 and 03.4-05.
- Option B (migrate Zeroclaw): Audit verdict is FAIL (or we choose replacement anyway). Before proceeding, create `src/integrations/zeroclaw/spec.json` by copying `src/integrations/zeroclaw/spec.template.json` and filling real values per Zeroclaw contract.

Notes:
- Zeroclaw spec must be machine-readable and pass `load_zeroclaw_spec()` validation.
- This phase does not add new capabilities beyond migrating the gateway integration; keep OSS SSE semantics stable.
  </action>
  <verify>
uv run python -c "from pathlib import Path; assert Path('.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-AUDIT-REPORT.md').exists(); print('audit report present')"
  </verify>
  <done>
If migrating, `src/integrations/zeroclaw/spec.json` exists and `uv run python -c "from src.integrations.zeroclaw.spec import load_zeroclaw_spec; load_zeroclaw_spec()"` succeeds.
If keeping Picoclaw, executor stops here and marks remaining plans skipped.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/tests/integrations/test_zeroclaw_spec.py -q` passes
- If migrating: `src/integrations/zeroclaw/spec.json` exists and `load_zeroclaw_spec()` succeeds
</verification>

<success_criteria>
- Migration is unblocked by an explicit, validated spec file if chosen
- If keeping Picoclaw, executor stops here (plans 04-05 skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-03-SUMMARY.md`
</output>
