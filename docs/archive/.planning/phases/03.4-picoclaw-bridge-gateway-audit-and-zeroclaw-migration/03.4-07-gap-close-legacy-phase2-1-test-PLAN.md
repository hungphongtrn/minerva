---
phase: 03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/integration/test_phase2_1_bridge_execution.py
autonomous: true
requirements:
  - CLEANUP-02

must_haves:
  truths:
    - "Running or collecting src/tests/integration/test_phase2_1_bridge_execution.py never fails with ImportError after Picoclaw module removal."
  artifacts:
    - path: src/tests/integration/test_phase2_1_bridge_execution.py
      provides: "Legacy Phase 2.1 integration tests are explicitly deprecated or updated to current gateway integration"
  key_links:
    - from: src/tests/integration/test_phase2_1_bridge_execution.py
      to: pytest
      via: "module-level skip for deprecated Phase 2.1 Picoclaw bridge tests"
      pattern: "pytest\\.skip\\(.*allow_module_level=True"
---

<objective>
Close VERIFICATION Gap 2 by making the legacy Phase 2.1 bridge integration test file compatible with the post-migration codebase (Picoclaw bridge module deleted).

Purpose: Keep the repository test suite import-safe and avoid latent ImportErrors.
Output: Updated legacy test file with explicit deprecation handling.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-VERIFICATION.md

@src/tests/integration/test_phase2_1_bridge_execution.py
@src/tests/smoke/test_gateway_cutover.py
</context>

<tasks>

<task type="auto">
  <name>task 1: deprecate legacy Phase 2.1 bridge test file with module-level skip (import-safe)</name>
  <files>
src/tests/integration/test_phase2_1_bridge_execution.py
  </files>
  <action>
Update `src/tests/integration/test_phase2_1_bridge_execution.py` so it can no longer raise ImportError due to deleted Picoclaw modules.

Recommended implementation (fastest, preserves repo hygiene):
- Add a module-level skip immediately after the module docstring, before any imports except `pytest`:
  - `import pytest`
  - `pytest.skip("Deprecated: Phase 2.1 Picoclaw bridge tests superseded by Zeroclaw gateway cutover tests (Phase 03.4)", allow_module_level=True)`

Notes:
- This file is Phase 2.1-specific and Picoclaw-specific; Phase 03.4 introduced `src/tests/smoke/test_gateway_cutover.py` and `src/tests/services/test_zeroclaw_gateway_service.py` as the active regression suite.
- Keeping a clear skip message preserves history while preventing accidental execution failures.

Why:
- VERIFICATION Gap 2: 6 inline imports reference `src.services.picoclaw_bridge_service` which no longer exists.
  </action>
  <verify>
uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -q
  </verify>
  <done>
The legacy test file imports successfully and is skipped with a clear deprecation message (no ImportError).
  </done>
</task>

<task type="auto">
  <name>task 2: prove no remaining Picoclaw-bridge imports exist in the legacy test file</name>
  <files>
src/tests/integration/test_phase2_1_bridge_execution.py
  </files>
  <action>
After adding the module-level skip, ensure the file is still clean of any top-level imports from deleted Picoclaw modules.

If the executor prefers a stricter cleanup (optional):
- Remove or comment out Picoclaw-only inline imports that are now unreachable due to module-level skip, to reduce dead code noise.

Keep ASCII-only edits.
  </action>
  <verify>
uv run python -c "import pathlib; p=pathlib.Path('src/tests/integration/test_phase2_1_bridge_execution.py'); s=p.read_text(encoding='utf-8'); assert 'src.services.picoclaw_bridge_service' not in s; print('ok')"
  </verify>
  <done>
The legacy test file contains no references to `src.services.picoclaw_bridge_service` and remains import-safe.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/tests/integration/test_phase2_1_bridge_execution.py -q` reports the module as skipped
- `uv run pytest src/tests/smoke/test_gateway_cutover.py -q` still passes (sanity check)
</verification>

<success_criteria>
- VERIFICATION Gap 2 is closed: the legacy integration test file can be collected/executed without ImportError after Picoclaw removal
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-07-SUMMARY.md`
</output>
