---
phase: 03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration
plan: 05
type: execute
wave: 5
depends_on:
  - 03.4-04
files_modified:
  - src/services/picoclaw_bridge_service.py
  - src/tests/services/test_picoclaw_bridge_service.py
  - src/config/settings.py
  - src/cli/commands/init.py
  - .env.example
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02

must_haves:
  truths:
    - "Picoclaw integration is removed from the codebase after Zeroclaw cutover (no fallback path remains)."
    - "Repo configuration and docs no longer refer to Picoclaw bridge; Zeroclaw is the primary gateway naming."
  artifacts:
    - path: src/services/zeroclaw_gateway_service.py
      provides: "Sole gateway integration after cleanup"
  key_links:
    - from: src/config/settings.py
      to: .env.example
      via: "Documented env vars remain in sync"
      pattern: "ZER0CLAW|ZEROCLOW|ZEROC" 
---

<objective>
Delete Picoclaw integration artifacts and rename configuration/env/docs from Picoclaw/bridge to Zeroclaw, leaving the repository in a clean post-cutover state.

Purpose: Big-bang cutover means no rollback; remove obsolete code and config to prevent drift.
Output: Picoclaw removal + renamed settings + updated env example.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-CONTEXT.md
@src/config/settings.py
@src/cli/commands/init.py
@.env.example
</context>

<tasks>

<task type="auto">
  <name>task 1: remove Picoclaw bridge service + tests + any remaining references</name>
  <files>
src/services/picoclaw_bridge_service.py
src/tests/services/test_picoclaw_bridge_service.py
  </files>
  <action>
Delete Picoclaw bridge integration artifacts after cutover:
- Remove `src/services/picoclaw_bridge_service.py`.
- Remove `src/tests/services/test_picoclaw_bridge_service.py`.

Then remove or update all remaining references/imports across the repo so tests and type checking still work (the codebase must not refer to Picoclaw bridge anywhere).
  </action>
  <verify>
uv run pytest src/tests/services/ -q
  </verify>
  <done>
No Picoclaw bridge code remains and service-level tests still pass.
  </done>
</task>

<task type="auto">
  <name>task 2: rename config/env/docs to Zeroclaw naming (no Picoclaw/bridge leftovers)</name>
  <files>
src/config/settings.py
src/cli/commands/init.py
.env.example
  </files>
  <action>
Update configuration naming to Zeroclaw:
- Replace Picoclaw-specific env/settings keys with Zeroclaw equivalents where appropriate.
- Ensure `.env.example` and `src/cli/commands/init.py` template output are updated accordingly.

Keep ASCII-only `.env.example` invariant.

After edits, grep the repo to ensure no `PICOCLAW_` or `picoclaw` gateway integration naming remains except where it refers to historical snapshot naming that is truly still required.
  </action>
  <verify>
uv run pytest src/tests/ -q
  </verify>
  <done>
`.env.example` documents Zeroclaw config; repo builds/tests pass; no stale Picoclaw bridge naming remains.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/tests/ -q` passes
- Simple content search shows Picoclaw bridge naming removed from runtime integration
</verification>

<success_criteria>
- Zeroclaw is the only runtime gateway integration remaining
- Repo configuration and docs are consistent and future-proof
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-05-SUMMARY.md`
</output>
