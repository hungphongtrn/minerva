---
phase: 03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/run_service.py
  - src/tests/services/test_run_service_deprecated_bridge_helpers.py
autonomous: true
requirements:
  - CLEANUP-01

must_haves:
  truths:
    - "Calling RunService._is_recoverable_bridge_error() never raises NameError and returns a deterministic boolean based on GatewayErrorType."
  artifacts:
    - path: src/services/run_service.py
      provides: "Deprecated bridge helper delegates to gateway error typing"
    - path: src/tests/services/test_run_service_deprecated_bridge_helpers.py
      provides: "Regression coverage for deprecated bridge helper"
  key_links:
    - from: src/services/run_service.py
      to: src/services/run_service.py
      via: "_is_recoverable_bridge_error delegates to _is_recoverable_gateway_error"
      pattern: "def _is_recoverable_bridge_error\\(.*\\):[\\s\\S]*_is_recoverable_gateway_error"
---

<objective>
Close VERIFICATION Gap 1 by fixing the deprecated RunService helper that still references the deleted BridgeErrorType.

Purpose: Keep backwards-compatibility helpers safe after Picoclaw removal.
Output: Updated deprecated helper + a small regression test.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-VERIFICATION.md

@src/services/run_service.py
@src/services/zeroclaw_gateway_service.py
</context>

<tasks>

<task type="auto">
  <name>task 1: fix deprecated _is_recoverable_bridge_error to avoid deleted BridgeErrorType</name>
  <files>
src/services/run_service.py
  </files>
  <action>
Update `RunService._is_recoverable_bridge_error()` so it cannot reference `BridgeErrorType` (deleted with Picoclaw cleanup).

Implementation requirements:
- Keep the method in place (it is marked DEPRECATED but retained for backwards compatibility).
- Replace the current `recoverable_types = {BridgeErrorType...}` logic with one of:
  - `return self._is_recoverable_gateway_error(error)` (preferred: single source of truth), OR
  - a `GatewayErrorType`-based set identical to `_is_recoverable_gateway_error()`.
- Keep the signature unchanged (`error: Optional[GatewayError] -> bool`) and preserve fail-closed behavior (`None` -> `False`).

Why:
- VERIFICATION Gap 1: NameError would occur if this deprecated method is called.
  </action>
  <verify>
uv run python -c "from src.services.run_service import RunService; from src.services.zeroclaw_gateway_service import GatewayError, GatewayErrorType; s=RunService(); print(s._is_recoverable_bridge_error(GatewayError(error_type=GatewayErrorType.TIMEOUT, message='x', remediation='y')))"
  </verify>
  <done>
`RunService._is_recoverable_bridge_error()` no longer references `BridgeErrorType` and returns a boolean for valid `GatewayError` inputs.
  </done>
</task>

<task type="auto">
  <name>task 2: add regression tests for deprecated bridge recoverability helper</name>
  <files>
src/tests/services/test_run_service_deprecated_bridge_helpers.py
  </files>
  <action>
Create `src/tests/services/test_run_service_deprecated_bridge_helpers.py` to prevent regressions.

Test cases (fast/offline):
- `_is_recoverable_bridge_error(None)` returns `False`.
- For each recoverable `GatewayErrorType` (`HEALTH_CHECK_FAILED`, `TIMEOUT`, `TRANSPORT_ERROR`), `_is_recoverable_bridge_error(GatewayError(...))` returns `True`.
- For a non-recoverable `GatewayErrorType` (pick one used elsewhere in code, e.g. `AUTH_FAILED` or `UPSTREAM_ERROR`), `_is_recoverable_bridge_error(...)` returns `False`.

Constraints:
- Import `GatewayError` and `GatewayErrorType` from `src.services.zeroclaw_gateway_service` (current gateway typing).
- Do not import any Picoclaw modules.
  </action>
  <verify>
uv run pytest src/tests/services/test_run_service_deprecated_bridge_helpers.py -q
  </verify>
  <done>
Tests pass and protect the deprecated helper from reintroducing deleted type references.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest src/tests/services/test_run_service_deprecated_bridge_helpers.py -q` passes
</verification>

<success_criteria>
- VERIFICATION Gap 1 is closed: deprecated helper cannot raise NameError and is covered by a focused unit test
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-picoclaw-bridge-gateway-audit-and-zeroclaw-migration/03.4-06-SUMMARY.md`
</output>
