---
phase: 03-persistence-and-checkpoint-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/0004_phase3_persistence_and_checkpoint_recovery.py
  - src/tests/smoke/test_phase3_schema_bootstrap.py
autonomous: true
must_haves:
  truths:
    - "Non-guest run/session metadata and runtime events have durable relational tables in Postgres."
    - "Checkpoint metadata and active checkpoint pointer are represented as first-class database artifacts per workspace."
    - "Audit events are append-only, and update/delete attempts are rejected at database level."
  artifacts:
    - path: "src/db/models.py"
      provides: "ORM models for run sessions, runtime events, workspace checkpoints, active pointer, and audit events"
      contains: "class RunSession"
    - path: "src/db/migrations/versions/0004_phase3_persistence_and_checkpoint_recovery.py"
      provides: "Phase 3 schema migration with indexes, constraints, and immutable audit trigger"
      contains: "def upgrade"
    - path: "src/tests/smoke/test_phase3_schema_bootstrap.py"
      provides: "Schema and immutability smoke checks"
      contains: "test_"
  key_links:
    - from: "src/db/migrations/versions/0004_phase3_persistence_and_checkpoint_recovery.py"
      to: "src/db/models.py"
      via: "Migration table/constraint names match ORM model tables"
      pattern: "workspace_checkpoints|runtime_events|audit_events"
    - from: "src/db/migrations/versions/0004_phase3_persistence_and_checkpoint_recovery.py"
      to: "audit_events"
      via: "Trigger/function rejects UPDATE and DELETE"
      pattern: "immutable|RAISE EXCEPTION"
---

<objective>
Create the durable schema foundation for Phase 3 persistence and checkpoint recovery.

Purpose: All runtime persistence, checkpoint tracking, restore logic, and security audit guarantees depend on these data structures being correct and immutable where required.
Output: New Phase 3 models, migration, and smoke coverage validating schema shape and append-only audit enforcement.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-CONTEXT.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-RESEARCH.md
@src/db/models.py
@src/db/migrations/versions/0003_workspace_lifecycle_and_agent_pack_foundation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 3 ORM models for runtime persistence and checkpoint metadata</name>
  <files>src/db/models.py</files>
  <action>Add ORM classes and supporting enums/constants for `run_sessions`, `runtime_events`, `workspace_checkpoints`, `workspace_active_checkpoint`, and `audit_events`. Include required foreign keys, status fields, manifest/version metadata, and query indexes that match expected read patterns (run timeline, workspace checkpoint history, active pointer lookups). Keep audit payloads structured (JSON-capable) and preserve existing Phase 1/2 model behavior unchanged.</action>
  <verify>`uv run pytest src/tests/smoke/test_phase3_schema_bootstrap.py -k "phase3_models" -q`</verify>
  <done>All Phase 3 persistence entities are represented in ORM with explicit columns/relations needed by PERS-01, PERS-03, and SECU-04.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration with immutable audit enforcement</name>
  <files>src/db/migrations/versions/0004_phase3_persistence_and_checkpoint_recovery.py</files>
  <action>Create migration `0004` that adds all Phase 3 tables and indexes, plus DB-level append-only protections on `audit_events` (trigger/function for PostgreSQL, safe compatibility path for SQLite test runs). Include active-pointer uniqueness and referential constraints for checkpoint lineage. Do not use raw ad-hoc SQL scripts outside migration files because schema evolution must remain ORM/Alembic-managed.</action>
  <verify>`uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head`</verify>
  <done>Migration applies and rolls back cleanly, and immutable audit guard exists in upgrade path.</done>
</task>

<task type="auto">
  <name>Task 3: Add smoke checks for schema availability and audit immutability</name>
  <files>src/tests/smoke/test_phase3_schema_bootstrap.py</files>
  <action>Add smoke tests that verify new tables exist, core insert paths work, and attempted mutation/deletion of audit rows is rejected (fail-closed). Include at least one assertion proving active checkpoint pointer relationship resolves to a valid checkpoint record.</action>
  <verify>`uv run pytest src/tests/smoke/test_phase3_schema_bootstrap.py -q`</verify>
  <done>Smoke suite proves schema bootstrap is complete and append-only audit constraints are enforced.</done>
</task>

</tasks>

<verification>
Run `uv run alembic upgrade head && uv run pytest src/tests/smoke/test_phase3_schema_bootstrap.py -q`; confirm all new Phase 3 tables exist and audit mutations are blocked.
</verification>

<success_criteria>
Postgres schema now contains durable run/session/event/checkpoint/audit structures with immutable audit enforcement, ready for service-layer persistence wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-and-checkpoint-recovery/03-01-SUMMARY.md`
</output>
