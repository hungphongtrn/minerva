---
phase: 03-persistence-and-checkpoint-recovery
plan: 05
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/api/routes/persistence.py
  - src/api/router.py
  - src/services/workspace_checkpoint_service.py
  - src/tests/integration/test_phase3_persistence_api.py
  - src/tests/integration/test_phase3_security_regressions.py
autonomous: true
must_haves:
  truths:
    - "Operators and clients can query run/session metadata and runtime event timelines from Postgres-backed APIs."
    - "The API exposes checkpoint manifest/version details and the active checkpoint pointer for each workspace."
    - "Operator pointer updates are auditable and fail closed on rollback-to-older-revision attempts."
  artifacts:
    - path: "src/api/routes/persistence.py"
      provides: "Phase 3 read APIs for run timeline, checkpoint manifests, active pointer, and audit timeline"
      contains: "APIRouter"
    - path: "src/services/workspace_checkpoint_service.py"
      provides: "Operator pointer update guardrails and audit append hooks"
      contains: "set_active"
    - path: "src/tests/integration/test_phase3_persistence_api.py"
      provides: "Integration coverage for PERS-01/PERS-03 query surfaces"
      contains: "test_"
    - path: "src/tests/integration/test_phase3_security_regressions.py"
      provides: "Security regressions for immutable audit and pointer restrictions"
      contains: "test_"
  key_links:
    - from: "src/api/routes/persistence.py"
      to: "src/db/repositories/runtime_event_repository.py"
      via: "Run timeline endpoints query ordered runtime events"
      pattern: "sequence"
    - from: "src/api/routes/persistence.py"
      to: "src/services/workspace_checkpoint_service.py"
      via: "Checkpoint pointer read/update endpoints call service guardrails"
      pattern: "active_checkpoint"
    - from: "src/services/workspace_checkpoint_service.py"
      to: "src/db/repositories/audit_event_repository.py"
      via: "Pointer changes append immutable audit entries"
      pattern: "pointer"
---

<objective>
Expose Phase 3 persistence/query APIs and enforce pointer-control security boundaries.

Purpose: PERS-01/PERS-03 need user-visible query surfaces, and SECU-04 requires immutable auditability for pointer operations and timeline access.
Output: New persistence API routes, router wiring, operator-only pointer update semantics, and integration/security test evidence.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-CONTEXT.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-RESEARCH.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-03-SUMMARY.md
@src/api/router.py
@src/api/dependencies/auth.py
@src/services/workspace_checkpoint_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistence read routes for run timeline and checkpoint metadata</name>
  <files>src/api/routes/persistence.py, src/api/router.py</files>
  <action>Create a new `persistence` route module and register it in API router. Expose endpoints to query run/session metadata, ordered runtime events, checkpoint manifest/version records, active checkpoint pointer, and workspace audit timeline. Keep responses typed and deterministic for downstream Phase 5 event-stream alignment.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_persistence_api.py -k "run_timeline or checkpoint_manifest or active_pointer" -q`</verify>
  <done>Persistence data is queryable via API endpoints without direct DB access.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce operator-only pointer updates with no rollback to older revisions</name>
  <files>src/services/workspace_checkpoint_service.py, src/api/routes/persistence.py</files>
  <action>Add pointer update endpoint and service guardrails so only operator-authorized principals can change active pointer, and only to the newest successful checkpoint revision (no rollback to older revisions in Phase 3). Record pointer changes in audit history and hard-reject invalid pointer transitions with explicit reason codes.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_security_regressions.py -k "pointer" -q`</verify>
  <done>Pointer control is fail-closed, auditable, and Phase 3 rollback restrictions are enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration and security regressions for persistence APIs and audit immutability</name>
  <files>src/tests/integration/test_phase3_persistence_api.py, src/tests/integration/test_phase3_security_regressions.py</files>
  <action>Add end-to-end tests that validate API queryability of run/session/events/checkpoints, verify ordered event timeline responses, verify audit timeline visibility constraints, and confirm audit entries remain immutable (update/delete attempts fail). Include negative tests for unauthorized pointer changes and rollback attempts.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_persistence_api.py -q && uv run pytest src/tests/integration/test_phase3_security_regressions.py -q`</verify>
  <done>Phase 3 persistence surfaces and SECU-04 security expectations are demonstrated by integration evidence.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase3_persistence_api.py -q && uv run pytest src/tests/integration/test_phase3_security_regressions.py -q`; confirm queryable persistence surfaces, pointer controls, and immutable audit behavior.
</verification>

<success_criteria>
Phase 3 persistence data and checkpoint pointers are externally observable through API contracts, with operator-only control and immutable audit trails.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-and-checkpoint-recovery/03-05-SUMMARY.md`
</output>
