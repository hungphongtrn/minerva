---
phase: 03-persistence-and-checkpoint-recovery
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/infrastructure/sandbox/providers/base.py
  - src/infrastructure/sandbox/providers/local_compose.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/services/checkpoint_restore_service.py
  - src/services/workspace_lifecycle_service.py
  - src/services/run_service.py
  - src/api/routes/runs.py
  - src/tests/integration/test_phase3_checkpoint_restore.py
autonomous: true
must_haves:
  truths:
    - "Cold-start restore attempts hydrate from active checkpoint revision first, then previous valid checkpoint if latest is unusable."
    - "When restore is already in progress, run requests are acknowledged as queued/restoring instead of failing open or racing duplicate restores."
    - "If restore fails twice, execution continues with a fresh workspace state (static identity mount only), with fallback decisions recorded in audit history."
  artifacts:
    - path: "src/services/checkpoint_restore_service.py"
      provides: "Restore orchestration with active-pointer lookup, fallback, single retry, and fresh-start escape hatch"
      contains: "class CheckpointRestoreService"
    - path: "src/services/workspace_lifecycle_service.py"
      provides: "Lifecycle integration of restore-before-routing semantics"
      contains: "resolve_target"
    - path: "src/api/routes/runs.py"
      provides: "Run API restore-aware acknowledgements (`queued`/`restoring`)"
      contains: "start_run"
    - path: "src/tests/integration/test_phase3_checkpoint_restore.py"
      provides: "Integration tests for fallback and restore failure policy"
      contains: "test_"
  key_links:
    - from: "src/services/workspace_lifecycle_service.py"
      to: "src/services/checkpoint_restore_service.py"
      via: "Cold-start path invokes restore flow before sandbox routing"
      pattern: "restore"
    - from: "src/api/routes/runs.py"
      to: "src/services/run_service.py"
      via: "Route maps restore-in-progress state to queued/restoring response"
      pattern: "restoring"
    - from: "src/services/checkpoint_restore_service.py"
      to: "src/db/repositories/audit_event_repository.py"
      via: "Fallback/retry/fresh-start outcomes append audit events"
      pattern: "fallback|restore"
---

<objective>
Implement cold-start restore behavior with deterministic fallback and degraded-mode safety.

Purpose: PERS-04 requires reliable hydration from checkpoints without introducing restore races or total run failures when checkpoint data is bad.
Output: Restore service and lifecycle wiring that support active->previous fallback, single retry, queued/restoring responses, and fresh-start continuation.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-CONTEXT.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-RESEARCH.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-03-SUMMARY.md
@src/services/workspace_lifecycle_service.py
@src/services/run_service.py
@src/api/routes/runs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend provider and lifecycle contracts for restore-aware sandbox hydration</name>
  <files>src/infrastructure/sandbox/providers/base.py, src/infrastructure/sandbox/providers/local_compose.py, src/infrastructure/sandbox/providers/daytona.py, src/services/workspace_lifecycle_service.py</files>
  <action>Extend provider contract and lifecycle wiring so restore flows can hydrate runtime memory/session state before normal routing proceeds. Keep semantics provider-agnostic: restore state should expose `restoring`/`ready` transitions and prevent concurrent cold-start restores for the same workspace. Avoid changing static identity mount logic because static files are mounted at provisioning time and are not checkpoint payload.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_checkpoint_restore.py -k "contract or restoring_state" -q`</verify>
  <done>Lifecycle can represent restore-in-progress status and providers can participate in restore-ready hydration flow.</done>
</task>

<task type="auto">
  <name>Task 2: Implement checkpoint restore service with fallback and retry policy</name>
  <files>src/services/checkpoint_restore_service.py</files>
  <action>Implement restore service that resolves active checkpoint pointer, validates manifest/archive checksum, restores latest checkpoint when valid, falls back to previous valid checkpoint when latest fails, retries restore once on transient failure, then returns fresh-start outcome without memory/session state if both attempts fail. Append audit events for restore start/success/fallback/failure/fresh-start outcomes.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_checkpoint_restore.py -k "fallback or retry or fresh_start" -q`</verify>
  <done>Restore flow deterministically follows active -> previous valid -> retry once -> fresh start policy with auditable transitions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire restore outcomes into run execution responses and integration tests</name>
  <files>src/services/run_service.py, src/api/routes/runs.py, src/tests/integration/test_phase3_checkpoint_restore.py</files>
  <action>Integrate restore-aware lifecycle output into run execution so requests arriving during restore receive queued acknowledgement with restoring state (not hard failure). Ensure post-restore runs resume normal execution path and fallback decisions are not silently swallowed. Add integration tests for restoring acknowledgement, fallback-to-previous success, and degraded fresh-start continuation.</action>
  <verify>`uv run pytest src/tests/integration/test_phase3_checkpoint_restore.py -q`</verify>
  <done>Cold-start restore behavior is observable, race-safe, and aligned with phase decisions on fallback and degraded continuation.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase3_checkpoint_restore.py -q`; confirm restore-in-progress responses, active-to-previous fallback, single retry behavior, and fresh-start continuation after repeated failure.
</verification>

<success_criteria>
PERS-04 is functionally satisfied: cold-start restore hydrates from checkpoint when possible and degrades safely when checkpoints are unusable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-and-checkpoint-recovery/03-04-SUMMARY.md`
</output>
