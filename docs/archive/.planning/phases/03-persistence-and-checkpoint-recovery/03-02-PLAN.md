---
phase: 03-persistence-and-checkpoint-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/config/settings.py
  - .env.example
  - src/infrastructure/checkpoints/s3_checkpoint_store.py
  - src/services/checkpoint_archive_service.py
  - src/tests/services/test_checkpoint_storage_and_archive.py
autonomous: true
user_setup:
  - service: s3-compatible-storage
    why: "Checkpoint archive persistence for non-guest workspaces"
    env_vars:
      - name: CHECKPOINT_S3_BUCKET
        source: "Object storage console or operator-provided bucket name"
      - name: CHECKPOINT_S3_ENDPOINT
        source: "Object storage endpoint URL (AWS S3, MinIO, Ceph, etc.)"
      - name: CHECKPOINT_S3_ACCESS_KEY
        source: "Object storage access key"
      - name: CHECKPOINT_S3_SECRET_KEY
        source: "Object storage secret key"
    dashboard_config:
      - task: "Create checkpoint bucket and grant put/get/head permissions"
        location: "Object storage bucket IAM/policy settings"
must_haves:
  truths:
    - "Checkpoint archives are written to deterministic S3 keys for each workspace revision."
    - "Archive manifests include checksum and version metadata before a checkpoint is considered complete."
    - "Checkpoint archive content excludes static identity files and captures only runtime memory/session state."
  artifacts:
    - path: "src/infrastructure/checkpoints/s3_checkpoint_store.py"
      provides: "S3-compatible checkpoint object write/read/head operations"
      contains: "class S3CheckpointStore"
    - path: "src/services/checkpoint_archive_service.py"
      provides: "Archive pack/unpack logic scoped to memory/session checkpoint data"
      contains: "class CheckpointArchiveService"
    - path: "src/config/settings.py"
      provides: "Checkpoint storage and milestone policy configuration"
      contains: "CHECKPOINT_"
  key_links:
    - from: "src/services/checkpoint_archive_service.py"
      to: "src/infrastructure/checkpoints/s3_checkpoint_store.py"
      via: "Archive output and checksum metadata are passed into S3 write API"
      pattern: "checksum|manifest"
    - from: "src/config/settings.py"
      to: "src/infrastructure/checkpoints/s3_checkpoint_store.py"
      via: "Storage client initialization consumes endpoint/bucket/credential settings"
      pattern: "CHECKPOINT_S3"
---

<objective>
Implement object-storage checkpoint primitives and archive tooling for Phase 3.

Purpose: PERS-02 and PERS-03 require deterministic checkpoint files and manifests in S3-compatible storage before restore and pointer logic can be built safely.
Output: Configured checkpoint storage settings, archive service, S3 store abstraction, and tests proving key layout/checksum behavior.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-CONTEXT.md
@.planning/phases/03-persistence-and-checkpoint-recovery/03-RESEARCH.md
@src/config/settings.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checkpoint storage dependencies and configuration surface</name>
  <files>pyproject.toml, src/config/settings.py, .env.example</files>
  <action>Add required Phase 3 storage dependencies (`boto3`, `s3fs`, `zstandard` when used) and introduce checkpoint config in settings (`CHECKPOINT_S3_*`, milestone triggers, interval safety checkpoint). Add `.env.example` entries for these settings. Keep defaults local-dev friendly but fail closed for missing production credentials where persistence is enabled.</action>
  <verify>`uv run python -c "from src.config.settings import settings; assert hasattr(settings, 'CHECKPOINT_S3_BUCKET')"`</verify>
  <done>Project config cleanly exposes all checkpoint storage controls and dependency resolution succeeds via `uv`.</done>
</task>

<task type="auto">
  <name>Task 2: Build S3 checkpoint store and archive service with deterministic keying</name>
  <files>src/infrastructure/checkpoints/s3_checkpoint_store.py, src/services/checkpoint_archive_service.py</files>
  <action>Create `S3CheckpointStore` with methods for put/get/head manifest/archive objects and deterministic key layout `workspaces/{workspace_id}/checkpoints/{checkpoint_id}/...`. Build `CheckpointArchiveService` that packages only dynamic runtime memory/session state into archive artifacts, computes SHA-256 on final archive bytes, and returns manifest-ready metadata. Do not include static identity files (`AGENT.md`, `SOUL.md`, `IDENTITY.md`, `skills/`) in checkpoint payloads because Phase 3 scope excludes static mounts.</action>
  <verify>`uv run pytest src/tests/services/test_checkpoint_storage_and_archive.py -k "key_layout or checksum or scope" -q`</verify>
  <done>Archive/store primitives produce deterministic keys and checksums with checkpoint scope boundaries enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Add service tests for storage correctness and failure handling</name>
  <files>src/tests/services/test_checkpoint_storage_and_archive.py</files>
  <action>Add focused service tests using mocked S3 client interactions to validate upload/download/head flow, checksum mismatch detection, and missing-object failure behavior. Include explicit test asserting static identity files are excluded from archive output.</action>
  <verify>`uv run pytest src/tests/services/test_checkpoint_storage_and_archive.py -q`</verify>
  <done>Tests prove checkpoint object operations and archive scope rules are stable enough for registry/restore integration.</done>
</task>

</tasks>

<verification>
Run `uv sync && uv run pytest src/tests/services/test_checkpoint_storage_and_archive.py -q`; confirm deterministic S3 key generation, checksum metadata, and checkpoint scope filtering.
</verification>

<success_criteria>
Phase 3 has production-shaped checkpoint object storage primitives with deterministic manifests and archive boundaries aligned to memory/session-only persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-and-checkpoint-recovery/03-02-SUMMARY.md`
</output>
