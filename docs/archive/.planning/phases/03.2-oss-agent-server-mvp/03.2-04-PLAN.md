---
phase: 03.2-oss-agent-server-mvp
plan: 4
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - src/api/oss/__init__.py
  - src/api/oss/router.py
  - src/api/oss/routes/__init__.py
  - src/api/oss/routes/health.py
  - src/api/oss/routes/metrics.py
  - src/main.py
  - src/tests/integration/test_phase3_2_health_ready.py
autonomous: true
requirements:
  - WORK-06
  - SECU-01
  - SECU-02

must_haves:
  truths:
    - "Operators can use `/health`, `/ready`, and `/metrics` for k8s-native checks"
  artifacts:
    - path: "src/api/oss/routes/health.py"
      provides: "root-level /health and /ready endpoints"
      exports: ["router"]
    - path: "src/api/oss/routes/metrics.py"
      provides: "root-level /metrics endpoint"
      exports: ["router"]
  key_links:
    - from: "src/main.py"
      to: "src/api/oss/router.py"
      via: "app.include_router"
      pattern: "include_router\\(oss_router"
---

<objective>
Expose operator endpoints at the root level: `/health`, `/ready`, `/metrics`, wired into the FastAPI app WITHOUT the `/api/v1` prefix.

Purpose: Provide k8s-native health/readiness/metrics for an OSS deployment.
Output: OSS router tree + operator endpoints + tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-oss-agent-server-mvp/03.2-CONTEXT.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-RESEARCH.md

@src/services/run_service.py
@src/services/picoclaw_bridge_service.py
@src/api/router.py
@src/main.py
</context>

<tasks>

<task type="auto">
  <name>task 1: Add OSS router tree + implement `/health` and `/ready` fail-closed readiness gate</name>
  <files>
 src/api/oss/__init__.py
 src/api/oss/router.py
 src/api/oss/routes/__init__.py
 src/api/oss/routes/health.py
 src/main.py
  </files>
  <action>
 - Create a new router tree under `src/api/oss/` that is included WITHOUT the `/api/v1` prefix.
 - Implement `/health`:
   - Returns component statuses (db, daytona, s3, llm) using the PreflightService.
   - Never raises; always 200 with a structured payload.
 - Implement `/ready`:
   - Returns 200 only if blocking dependencies are OK (db + daytona + configured snapshot exists + schema up-to-date).
   - Returns 503 otherwise (k8s readiness semantics).
 - Snapshot gate MUST use the locked decision remediation message surfaced by the CLI/server: if missing, readiness fails closed and remediation tells developer to run `minerva snapshot build`.
 - Do not remove existing `/api/v1/*` router; OSS surface is additive.
   </action>
  <verify>
 uv run pytest -q src/tests/integration/test_phase3_2_health_ready.py
  </verify>
  <done>
 - Root endpoints exist and do not collide with `/api/v1/*`.
 - `/ready` is fail-closed on blocking dependency failures.
  </done>
</task>

<task type="auto">
  <name>task 2: Implement `/metrics` root endpoint</name>
  <files>
src/api/oss/routes/metrics.py
  </files>
  <action>
- Implement `/metrics` using `prometheus-fastapi-instrumentator` expose helper, mounted without `/api/v1` prefix.
- Ensure metrics endpoint does not require auth and is safe to call frequently.
  </action>
  <verify>
uv run pytest -q src/tests/integration/test_phase3_2_health_ready.py
  </verify>
  <done>
- `/metrics` responds successfully and includes default FastAPI/HTTP metrics.
  </done>
</task>

</tasks>

<verification>
 - `uv run pytest -q src/tests/integration/test_phase3_2_health_ready.py` passes.
</verification>

<success_criteria>
- Operators can check readiness/health/metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-oss-agent-server-mvp/03.2-04-SUMMARY.md`
</output>
