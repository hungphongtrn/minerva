---
phase: 03.2-oss-agent-server-mvp
plan: 3
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - src/services/daytona_pack_volume_service.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/cli/commands/register.py
  - src/tests/services/test_phase3_2_pack_volume_sync.py
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - WORK-02
  - WORK-03
  - WORK-05
  - SECU-05

must_haves:
  truths:
    - "`minerva register` validates the agent pack and syncs it to an immutable Daytona Volume (volume-per-digest)"
    - "Daytona sandboxes mount the registered pack Volume as static identity files"
    - "Per-sandbox Picoclaw `~/.picoclaw/config.json` is written via Daytona sandbox file API (not into the shared pack volume)"
  artifacts:
    - path: "src/services/daytona_pack_volume_service.py"
      provides: "volume-per-digest sync using disposable sandbox + sandbox.fs"
      contains: "class DaytonaPackVolumeService"
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "real identity verification via file API + volume mounts"
      contains: "VolumeMount"
    - path: "src/tests/services/test_phase3_2_pack_volume_sync.py"
      provides: "mocked tests for volume sync + cleanup"
      min_lines: 120
  key_links:
    - from: "src/cli/commands/register.py"
      to: "src/services/daytona_pack_volume_service.py"
      via: "sync pack -> volume"
      pattern: "DaytonaPackVolumeService"
    - from: "src/infrastructure/sandbox/providers/daytona.py"
      to: "src/services/daytona_pack_volume_service.py"
      via: "volume naming contract"
      pattern: "agent-pack"
---

<objective>
Replace simulated Daytona pack materialization with real Volume mounts + sandbox file API: register syncs pack files into a volume-per-digest; provisioning mounts that volume and verifies identity files.

Purpose: Enforce the OSS invariant that static identity files are mounted at sandbox creation, while supporting pack updates via digest-immutable volumes.
Output: Pack volume sync service + provider mounting + register command wiring + tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-oss-agent-server-mvp/03.2-CONTEXT.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-RESEARCH.md

@src/infrastructure/sandbox/providers/daytona.py
@src/services/agent_pack_service.py
@src/services/agent_pack_validation.py
@reference_repos/picoclaw/cmd/picoclaw/main.go
</context>

<tasks>

<task type="auto">
  <name>task 1: Implement volume-per-digest pack sync using disposable sandbox</name>
  <files>
src/services/daytona_pack_volume_service.py
src/tests/services/test_phase3_2_pack_volume_sync.py
  </files>
  <action>
- Create `DaytonaPackVolumeService` that:
  - Computes a deterministic volume name: `agent-pack-{agent_pack_id}-{source_digest}`.
  - Ensures the volume exists via `await daytona.volume.get(name, create=True)`.
  - Creates a disposable sandbox from the configured Picoclaw snapshot (env from Plan 03.2-02; for tests, mock):
    - Mount the volume at `/workspace/pack` using `VolumeMount(volume_id=..., mount_path="/workspace/pack")`.
  - Uploads pack files into the mounted directory via `sandbox.fs.upload_files(...)` (bulk upload).
  - Deletes the disposable sandbox in a `finally` block.
- Sync scope:
  - Include `AGENT.md`, `SOUL.md`, `IDENTITY.md`, and `skills/**` only.
  - Do NOT upload runtime config or secrets to the shared volume.
- Add tests (mock AsyncDaytona + sandbox.fs) verifying:
  - Correct volume naming.
  - Upload destinations under `/workspace/pack/...`.
  - Cleanup runs even on upload error.
  </action>
  <verify>
uv run pytest -q src/tests/services/test_phase3_2_pack_volume_sync.py
  </verify>
  <done>
- Service is deterministic and safe (cleanup guaranteed).
- Tests cover happy path and failure cleanup.
  </done>
</task>

<task type="auto">
  <name>task 2: Update Daytona provider to mount volume + verify identity via file API + write config outside volume</name>
  <files>src/infrastructure/sandbox/providers/daytona.py</files>
  <action>
- Update provisioning to create sandboxes from a Snapshot (not raw image):
   - Use `CreateSandboxFromSnapshotParams(snapshot=DAYTONA_PICOCLAW_SNAPSHOT_NAME, volumes=[...])`.
- Mount the *digest-selected* pack volume at `/workspace/pack` and treat it as the source of static identity files:
  - Compute the deterministic volume name exactly as `agent-pack-{agent_pack_id}-{source_digest}` (same contract as `DaytonaPackVolumeService`).
  - Mount that exact volume (do not mount `agent-pack-{agent_pack_id}-latest` or any non-digest alias).
- Replace simulated identity verification with real file API checks:
   - Verify `/workspace/pack/AGENT.md`, `/workspace/pack/SOUL.md`, `/workspace/pack/IDENTITY.md` exist.
   - Verify `/workspace/pack/skills/` exists.
- Write per-sandbox Picoclaw config to `~/.picoclaw/config.json` (e.g., `/home/daytona/.picoclaw/config.json`) using `sandbox.fs.create_folder` + `sandbox.fs.upload_file`.
   - This config includes the sandbox-scoped bridge auth token and LLM settings.
   - MUST NOT write config into `/workspace/pack` (shared volume).
- Keep fail-closed behavior: if identity verification fails, treat sandbox not ready.
  </action>
  <verify>
uv run pytest -q src/tests/services/test_sandbox_provider_adapters.py
  </verify>
  <done>
- Provider no longer claims identity-ready without real file checks.
- Per-sandbox config is written outside the shared volume.
  </done>
</task>

<task type="auto">
  <name>task 3: Wire `minerva register` to validate pack, register in DB, and sync pack volume</name>
  <files>src/cli/commands/register.py</files>
  <action>
- Implement `minerva register`:
  - Validates pack structure using existing services (AGENT.md/SOUL.md/IDENTITY.md/skills).
  - Registers/updates `AgentPack` in DB using `AgentPackService.register(...)`.
  - On success, calls `DaytonaPackVolumeService.sync_pack_to_volume(pack_id, source_path, source_digest)`.
  - Prints the resulting `agent_pack_id` for use as deployment default.
- Interpret "blocks on critical failures" as:
  - Validation errors -> exit non-zero.
  - Daytona sync errors -> exit non-zero.
  </action>
  <verify>
uv run pytest -q src/tests/services/test_agent_pack_service.py
  </verify>
  <done>
- Register command ties DB registration to volume materialization.
- Pack files sync into an immutable volume-per-digest.
  </done>
</task>

</tasks>

<verification>
- Provider tests remain green.
- Pack sync tests verify cleanup and deterministic volume naming.
</verification>

<success_criteria>
- Registered packs can be mounted via Daytona Volumes.
- Identity verification uses real sandbox file API.
- Per-sandbox config is isolated from shared pack volume.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-oss-agent-server-mvp/03.2-03-SUMMARY.md`
</output>
