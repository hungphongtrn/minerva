---
phase: 03.2-oss-agent-server-mvp
plan: 8
subsystem: infra

tags:
  - daytona
  - sdk
  - volume-mount
  - file-api
  - identity-verification
  - config-write

# Dependency graph
requires:
  - phase: 03.2-01
    provides: SandboxConfig foundation
  - phase: 03.2-05
    provides: Pack volume service

provides:
  - Daytona provider with SDK-correct primitives
  - Real identity file verification via file API
  - Per-sandbox config written outside pack volume

affects:
  - src/infrastructure/sandbox/providers/daytona.py
  - src/services/sandbox_orchestrator_service.py
  - src/services/daytona_pack_volume_service.py

# Tech tracking
tech-stack:
  added:
    - daytona.CreateSandboxFromSnapshotParams
    - daytona.VolumeMount
    - daytona.FileUpload
    - sandbox.fs.get_file_info
    - sandbox.fs.create_folder
    - sandbox.fs.upload_file
  patterns:
    - SDK-correct Daytona primitives
    - Real file API verification (not simulated)
    - Per-sandbox config outside shared volume

key-files:
  created:
    - src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
  modified:
    - src/infrastructure/sandbox/providers/base.py
    - src/infrastructure/sandbox/providers/daytona.py
    - src/services/daytona_pack_volume_service.py
    - src/services/sandbox_orchestrator_service.py
    - src/tests/services/test_phase3_2_pack_volume_sync.py
    - src/tests/services/test_sandbox_provider_adapters.py

key-decisions:
  - "Use SDK-correct CreateSandboxFromSnapshotParams instead of kwargs dict"
  - "Volume naming: agent-pack-{full-uuid}-{full-digest} (no truncation)"
  - "Fail-closed validation: both agent_pack_id and pack_digest required for pack binding"
  - "Identity verification uses real sandbox.fs.get_file_info() with polling"
  - "Per-sandbox config at /home/daytona/.picoclaw/config.json (outside /workspace/pack)"

patterns-established:
  - "SDK types: Import CreateSandboxFromSnapshotParams, VolumeMount, FileUpload from daytona"
  - "Volume mounts: Always mount pack volume at /workspace/pack"
  - "File API: Use get_file_info for existence checks, create_folder for dirs, upload_file for content"
  - "Config separation: Runtime config outside shared volume, identity files inside"

requirements-completed:
  - AGNT-02
  - WORK-02
  - WORK-05

# Metrics
duration: 19 min
completed: 2026-03-02
---

# Phase 03.2 Plan 8: Daytona Volume Mount and Config Write Summary

**Daytona provider now uses SDK-correct primitives with real file API verification and per-sandbox config written outside the shared pack volume, closing GAP-03 and GAP-04.**

## Performance

- **Duration:** 19 min
- **Started:** 2026-03-02T08:44:01Z
- **Completed:** 2026-03-02T09:03:26Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- GAP-03 closed: Daytona provider mounts digest-selected pack volume and verifies identity files via file API
- GAP-04 closed: Daytona provider writes `~/.picoclaw/config.json` via sandbox file API outside the shared volume
- Pack sync service updated to use SDK-correct CreateSandboxFromSnapshotParams + FileUpload list upload
- Volume naming standardized to full UUID + full digest (no truncation)
- SandboxConfig extended with optional agent_pack_id for pack binding
- Fail-closed validation ensures both agent_pack_id and pack_digest present for pack binding
- Real identity verification via sandbox.fs.get_file_info() with polling loop
- Per-sandbox config written to /home/daytona/.picoclaw/config.json (asserted outside /workspace/pack)

## Task Commits

Each task was committed atomically:

1. **Task 1: Normalize volume naming and use SDK-correct pack upload primitives** - `d6eeed3` (feat)
2. **Task 2: Pass agent_pack_id into SandboxConfig and mount digest volume** - `cc80385` (feat)
3. **Task 3: Replace simulated identity verification and write per-sandbox config** - `e2bbc63` (feat)

## Files Created/Modified

- `src/services/daytona_pack_volume_service.py` - SDK-correct CreateSandboxFromSnapshotParams, FileUpload, full volume naming
- `src/tests/services/test_phase3_2_pack_volume_sync.py` - Updated for SDK primitives and full naming
- `src/infrastructure/sandbox/providers/base.py` - Added agent_pack_id to SandboxConfig
- `src/infrastructure/sandbox/providers/daytona.py` - Snapshot-based provisioning, real file API, config write
- `src/services/sandbox_orchestrator_service.py` - Populate agent_pack_id in config
- `src/tests/services/test_sandbox_provider_adapters.py` - Updated for CreateSandboxFromSnapshotParams
- `src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py` - New tests for Task 3

## Decisions Made

- Use SDK-correct CreateSandboxFromSnapshotParams instead of kwargs dict (proper SDK semantics)
- Volume naming uses full UUID + full digest for deterministic mapping (no truncation)
- Fail-closed validation: both agent_pack_id and pack_digest required when pack binding
- Identity verification uses real file API with polling (not simulated)
- Per-sandbox config must be outside /workspace/pack (fail-fast assertion)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated test mocks for CreateSandboxFromSnapshotParams**
- **Found during:** Task 2 test execution
- **Issue:** Tests expected kwargs dict but provider now passes CreateSandboxFromSnapshotParams object
- **Fix:** Updated test mocks to accept positional params argument
- **Files modified:** src/tests/services/test_sandbox_provider_adapters.py
- **Verification:** Tests pass with new SDK-correct primitive
- **Committed in:** cc80385 (Task 2 commit)

**2. [Rule 2 - Missing Critical] Added agent_pack_id to test configs**
- **Found during:** Task 2 test execution
- **Issue:** Tests with pack_source_path missing required agent_pack_id for fail-closed validation
- **Fix:** Added agent_pack_id and pack_digest to test SandboxConfig instances
- **Files modified:** src/tests/services/test_sandbox_provider_adapters.py
- **Verification:** Tests pass with fail-closed validation
- **Committed in:** cc80385 (Task 2 commit)

**3. [Rule 1 - Bug] Fixed enum comparison in test assertion**
- **Found during:** Task 3 test execution
- **Issue:** Test compared `result.state.value` (int) to string "READY"
- **Fix:** Changed to compare `result.state` to SandboxState.READY enum
- **Files modified:** src/tests/infrastructure/sandbox/test_daytona_volume_mount_and_config.py
- **Verification:** Test passes with correct enum comparison
- **Committed in:** e2bbc63 (Task 3 commit)

---

**Total deviations:** 3 auto-fixed (2 blocking, 1 missing critical, 1 bug)
**Impact on plan:** All auto-fixes necessary for correctness. No scope creep.

## Issues Encountered

None - plan executed successfully with test-driven verification.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Daytona provider now fully implements:
  - Snapshot-based provisioning with volume mounts
  - Real identity file verification via file API
  - Per-sandbox config written outside shared volume
- Ready for integration with runtime bridge
- All gaps GAP-03 and GAP-04 closed

---
*Phase: 03.2-oss-agent-server-mvp*
*Completed: 2026-03-02*
