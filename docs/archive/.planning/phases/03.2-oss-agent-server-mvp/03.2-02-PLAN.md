---
phase: 03.2-oss-agent-server-mvp
plan: 2
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - src/services/daytona_snapshot_build_service.py
  - src/cli/commands/snapshot_build.py
  - src/tests/services/test_phase3_2_snapshot_build.py
autonomous: true
requirements:
  - AGNT-03
  - WORK-03
  - SECU-05

must_haves:
  truths:
    - "`minerva snapshot build` creates (or reuses) a Daytona Snapshot for Picoclaw base runtime"
    - "Snapshot build streams logs and returns a non-zero exit code on failure"
  artifacts:
    - path: "src/services/daytona_snapshot_build_service.py"
      provides: "Daytona snapshot build from a repo+ref"
      contains: "class DaytonaSnapshotBuildService"
    - path: "src/cli/commands/snapshot_build.py"
      provides: "CLI command implementation for snapshot build"
      contains: "def run_snapshot_build"
    - path: "src/tests/services/test_phase3_2_snapshot_build.py"
      provides: "mocked tests for snapshot build service"
      min_lines: 80
  key_links:
    - from: "src/cli/commands/snapshot_build.py"
      to: "src/services/daytona_snapshot_build_service.py"
      via: "service invocation"
      pattern: "DaytonaSnapshotBuildService"
---

<objective>
Implement `minerva snapshot build` using Daytona Declarative Builder to produce a Picoclaw base Snapshot required for sandbox provisioning.

Purpose: Enforce a deterministic, operator-driven runtime base without requiring prebuilt public images.
Output: Snapshot build service + CLI command + tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-CONTEXT.md
@.planning/phases/03.2-oss-agent-server-mvp/03.2-RESEARCH.md

@reference_repos/picoclaw/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>task 1: Add snapshot build service (AsyncDaytona + Image builder)</name>
  <files>src/services/daytona_snapshot_build_service.py</files>
  <action>
- Create `DaytonaSnapshotBuildService` that:
  - Reads required env vars (or accepts explicit args):
    - `PICOCLAW_REPO_URL` (git URL)
    - `PICOCLAW_REPO_REF` (branch/tag/sha; default: main)
    - `DAYTONA_PICOCLAW_SNAPSHOT_NAME` (snapshot name)
  - Clones the repo into a temp dir.
  - Builds a Daytona `Image` using one of these approaches (prefer #1 if it works with the chosen Dockerfile):
    1) `Image.from_dockerfile(path_to_dockerfile)` when Dockerfile is self-contained.
    2) If Dockerfile needs build context, use `Image.dockerfile_commands([...], context_dir=repo_dir)` OR re-express minimal build steps using `add_local_dir` + `run_commands`.
  - Calls `await daytona.snapshot.create(CreateSnapshotParams(name=..., image=image), on_logs=...)`.
  - Surfaces errors with clear remediation (e.g., missing Daytona scopes: `write:snapshots`).
- Keep the service pure (no CLI parsing); return a structured result for the command layer.
  </action>
  <verify>
uv run pytest -q src/tests/services/test_phase3_2_snapshot_build.py
  </verify>
  <done>
- Service can be unit tested with AsyncDaytona mocked.
- Service exposes enough info for CLI to print progress and failures.
  </done>
</task>

<task type="auto">
  <name>task 2: Wire `minerva snapshot build` command to the service</name>
  <files>
src/cli/commands/snapshot_build.py
src/tests/services/test_phase3_2_snapshot_build.py
  </files>
  <action>
- Implement `minerva snapshot build` behavior in `src/cli/commands/snapshot_build.py`:
  - Parses flags `--repo-url`, `--ref`, `--snapshot-name` overriding env.
  - Streams build logs (pass `on_logs` callback).
  - Exits 0 on success; non-zero on failure.
- Add/extend tests to cover:
  - Missing required inputs -> non-zero exit.
  - Happy path -> snapshot.create invoked with expected params.
  </action>
  <verify>
uv run pytest -q src/tests/services/test_phase3_2_snapshot_build.py
  </verify>
  <done>
- `uv run minerva snapshot build --help` documents required env vars.
- Unit tests verify the CLI command calls the service and handles failures.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest -q src/tests/services/test_phase3_2_snapshot_build.py` passes.
</verification>

<success_criteria>
- Snapshot build command exists and is test-covered.
- Build failures return actionable errors and non-zero exit.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-oss-agent-server-mvp/03.2-02-SUMMARY.md`
</output>
