---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 16
type: execute
wave: 2
depends_on: ["02-13"]
files_modified:
  - src/services/sandbox_orchestrator_service.py
  - src/api/routes/workspaces.py
  - src/tests/integration/test_phase2_idle_ttl_enforcement.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "TTL-expired active sandboxes are auto-stopped before routing and are not reused as active healthy targets."
    - "Resolve responses provide observable TTL cleanup evidence (what was stopped and why) so users can verify behavior from API output."
    - "TTL state transitions are durable and visible in persisted sandbox records across requests."
  artifacts:
    - path: "src/services/sandbox_orchestrator_service.py"
      provides: "TTL cleanup enforcement wired into request-time routing path"
      contains: "stop_idle_sandboxes"
    - path: "src/api/routes/workspaces.py"
      provides: "Resolve response fields for TTL cleanup observability"
      contains: "ttl"
    - path: "src/tests/integration/test_phase2_idle_ttl_enforcement.py"
      provides: "Regression proving stop-and-replace behavior with persisted assertions"
      contains: "idle_ttl"
  key_links:
    - from: "src/api/routes/workspaces.py"
      to: "src/services/workspace_lifecycle_service.py"
      via: "resolve_target call returning routing metadata"
      pattern: "resolve_target"
    - from: "src/services/workspace_lifecycle_service.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "resolve_sandbox orchestration with TTL cleanup"
      pattern: "resolve_sandbox"
    - from: "src/tests/integration/test_phase2_idle_ttl_enforcement.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "Expired sandbox seeded then validated as stopped/replaced"
      pattern: "stop"
---

<objective>
Close UAT Test 9 by wiring idle TTL enforcement into observable routing behavior and proving durable stop/replacement state in API + DB evidence.

Purpose: TTL logic exists but is not reliably visible to users without request-path enforcement and persisted state assertions.
Output: Request-time TTL cleanup integration, response-level observability fields, and dedicated regression coverage.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-UAT.md
@src/services/sandbox_orchestrator_service.py
@src/services/workspace_lifecycle_service.py
@src/api/routes/workspaces.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce idle TTL cleanup before sandbox routing resolution</name>
  <files>src/services/sandbox_orchestrator_service.py</files>
  <action>Integrate TTL stop execution into the runtime routing path so expired active sandboxes are evaluated/stopped before healthy-candidate selection. Ensure stopped instances are excluded from routing and replacement provisioning path is used when needed. Keep the logic fail-closed when cleanup operations partially fail.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_idle_ttl_enforcement.py -k "cleanup_before_route" -q`</verify>
  <done>Resolve no longer routes to TTL-expired active instances, and stop/replacement behavior executes in the request path.</done>
</task>

<task type="auto">
  <name>Task 2: Expose TTL cleanup observability in workspace resolve API</name>
  <files>src/api/routes/workspaces.py</files>
  <action>Extend sandbox resolve response contract with deterministic TTL cleanup metadata (for example cleanup_applied flag, stopped sandbox IDs/count, and reason summary). Populate values from lifecycle/orchestrator outcome and keep backward-compatible defaults for cases with no cleanup.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_idle_ttl_enforcement.py -k "response_observability" -q`</verify>
  <done>Users can verify TTL enforcement directly from resolve API output instead of inferring behavior indirectly.</done>
</task>

<task type="auto">
  <name>Task 3: Add persisted TTL stop/replacement regression coverage</name>
  <files>src/tests/integration/test_phase2_idle_ttl_enforcement.py</files>
  <action>Add regression tests that seed TTL-expired active sandbox records, call resolve, and assert: expired sandbox transitions to stopped/unhealthy persisted state, routing returns replacement sandbox, and repeated calls reflect durable state instead of non-persisted ID churn. Include DB assertions from separate requests to validate persistence.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_idle_ttl_enforcement.py -q`</verify>
  <done>TTL auto-stop and replacement behavior is both observable and durably persisted, closing UAT Test 9.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_idle_ttl_enforcement.py -q` and confirm TTL-expired sandboxes are stopped/replaced with persisted DB evidence and observable API metadata.
</verification>

<success_criteria>
Idle TTL policy is enforced in live routing flow, surfaced clearly in API responses, and validated by persisted-state regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-16-SUMMARY.md`
</output>
