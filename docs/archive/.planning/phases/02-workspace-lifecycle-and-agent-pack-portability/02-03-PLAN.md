---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/config/settings.py
  - src/services/workspace_lease_service.py
  - src/services/sandbox_orchestrator_service.py
  - src/services/workspace_lifecycle_service.py
  - src/services/__init__.py
  - src/tests/services/test_workspace_lease_service.py
  - src/tests/services/test_sandbox_routing_service.py
  - src/tests/services/test_workspace_lifecycle_service.py
autonomous: true
must_haves:
  truths:
    - "A user gets one durable workspace reused across calls instead of one-off workspace creation per request."
    - "Same-workspace write paths are serialized with lease acquisition, renewal, and deterministic release."
    - "Routing prefers active healthy sandboxes and excludes unhealthy instances before provisioning replacements."
    - "Idle sandbox auto-stop uses a configured TTL value, and non-default TTL values are honored by lifecycle routing behavior."
  artifacts:
    - path: "src/config/settings.py"
      provides: "Typed sandbox idle TTL configuration with default and validation"
      contains: "SANDBOX_IDLE_TTL_SECONDS"
    - path: "src/services/workspace_lease_service.py"
      provides: "Lease acquire/renew/release orchestration with expiration-based recovery"
      contains: "class WorkspaceLeaseService"
    - path: "src/services/sandbox_orchestrator_service.py"
      provides: "Health-aware sandbox selection and replacement logic"
      contains: "class SandboxOrchestratorService"
    - path: "src/services/workspace_lifecycle_service.py"
      provides: "Durable workspace bootstrap/reuse and sandbox resolution entrypoint"
      contains: "class WorkspaceLifecycleService"
  key_links:
    - from: "src/services/workspace_lifecycle_service.py"
      to: "src/services/workspace_lease_service.py"
      via: "critical section for resolve/create path"
      pattern: "acquire.*try:.*resolve.*finally:.*release"
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/infrastructure/sandbox/providers/base.py"
      via: "provider-agnostic health/provision operations"
      pattern: "get_active|provision|get_health|stop"
    - from: "src/config/settings.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "settings-driven idle TTL injected into stop eligibility evaluation"
      pattern: "SANDBOX_IDLE_TTL_SECONDS|sandbox_idle_ttl_seconds"
    - from: "src/services/workspace_lifecycle_service.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "resolve path invokes orchestrator methods that enforce configured idle-stop policy"
      pattern: "resolve|route|idle|ttl"
---

<objective>
Implement the workspace lifecycle control-plane services that guarantee continuity, safe serialization, and health-aware sandbox routing.

Purpose: WORK-01 through WORK-06 depend on service-level orchestration before API routes can expose reliable behavior.
Output: Lease, sandbox orchestration, and workspace lifecycle services with direct test coverage for concurrency and routing edge cases.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-CONTEXT.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-RESEARCH.md
@.planning/phases/01-identity-and-policy-baseline/01-03-SUMMARY.md
@src/services/run_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DB-backed workspace lease service</name>
  <files>src/services/workspace_lease_service.py, src/tests/services/test_workspace_lease_service.py</files>
  <action>Implement lease acquisition, renewal heartbeat, release, and expiration recovery over `workspace_leases` with transaction-safe conflict behavior. Return retryable conflict outcomes for concurrent acquisition attempts on the same workspace instead of silent overrides. Keep behavior fail-closed if lease state is ambiguous.</action>
  <verify>`uv run pytest src/tests/services/test_workspace_lease_service.py -q`</verify>
  <done>Concurrent same-workspace acquire attempts serialize deterministically and expired leases can be safely reclaimed.</done>
</task>

<task type="auto">
  <name>Task 2: Add configurable idle TTL settings and wire sandbox stop enforcement</name>
  <files>src/config/settings.py, src/services/sandbox_orchestrator_service.py, src/tests/services/test_sandbox_routing_service.py</files>
  <action>Add explicit sandbox idle TTL setting(s) in `src/config/settings.py` (include default value and fail-closed validation for invalid/non-positive values), then inject that configuration into `SandboxOrchestratorService` so stop eligibility is computed from configured TTL instead of implicit constants. Keep existing health-first routing behavior (active -> healthy -> route), exclude unhealthy instances immediately, provision/hydrate replacements when needed, and preserve idempotent stop semantics.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_routing_service.py -k "ttl" -q && uv run pytest src/tests/services/test_sandbox_routing_service.py -q`</verify>
  <done>Idle-stop eligibility is driven by `SANDBOX_IDLE_TTL_SECONDS` (not hardcoded), and tests prove at least one non-default TTL value changes stop/no-stop outcomes deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Build workspace lifecycle service to auto-create/reuse and resolve targets</name>
  <files>src/services/workspace_lifecycle_service.py, src/services/__init__.py, src/tests/services/test_workspace_lifecycle_service.py</files>
  <action>Create lifecycle entrypoints that ensure durable workspace existence per authenticated user (auto-create on first use, reuse thereafter), acquire lease for write path, resolve active sandbox target via orchestrator (including the orchestrator path that enforces configured idle-stop TTL policy), and release lease in all success/failure branches. Do not add org-level lifecycle rules or queue fairness behavior in this phase.</action>
  <verify>`uv run pytest src/tests/services/test_workspace_lifecycle_service.py -q`</verify>
  <done>Service exposes a single method that returns workspace + routing target with guaranteed lease cleanup, continuity semantics, and routing behavior consistent with configured idle-stop enforcement.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_workspace_lease_service.py src/tests/services/test_sandbox_routing_service.py src/tests/services/test_workspace_lifecycle_service.py -q` and confirm sandbox routing tests include non-default TTL configuration coverage.
</verification>

<success_criteria>
Workspace continuity, lease serialization, and health-aware routing logic are deterministic and covered by focused service tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-03-SUMMARY.md`
</output>
