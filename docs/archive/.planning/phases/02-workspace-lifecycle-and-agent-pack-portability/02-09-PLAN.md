---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/run_service.py
  - src/services/workspace_lifecycle_service.py
  - src/services/sandbox_orchestrator_service.py
  - src/tests/services/test_workspace_lifecycle_service.py
  - src/tests/services/test_sandbox_routing_service.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Providing agent_pack_id in run execution reaches sandbox provisioning instead of being dropped before lifecycle resolution."
    - "Sandbox provisioning resolves registered AgentPack source_path and sets SandboxConfig.pack_source_path before provider provisioning."
    - "Missing, invalid, inactive, or cross-workspace agent_pack_id fails closed and does not provision a sandbox with ambiguous pack binding."
  artifacts:
    - path: "src/services/run_service.py"
      provides: "Run-to-lifecycle propagation of optional agent_pack_id"
      contains: "execute_with_routing"
    - path: "src/services/workspace_lifecycle_service.py"
      provides: "Lifecycle-to-orchestrator propagation of agent_pack_id"
      contains: "resolve_target"
    - path: "src/services/sandbox_orchestrator_service.py"
      provides: "Pack source-path resolution and fail-closed validation before provision"
      contains: "pack_source_path"
  key_links:
    - from: "src/services/run_service.py"
      to: "src/services/workspace_lifecycle_service.py"
      via: "resolve_target(... agent_pack_id=...)"
      pattern: "resolve_target\\(.*agent_pack_id"
    - from: "src/services/workspace_lifecycle_service.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "resolve_sandbox(... agent_pack_id=...)"
      pattern: "resolve_sandbox\\(.*agent_pack_id"
    - from: "src/services/sandbox_orchestrator_service.py"
      to: "src/db/repositories/agent_pack_repository.py"
      via: "AgentPack lookup and workspace ownership validation"
      pattern: "get_by_id"
---

<objective>
Close the core runtime wiring gap behind UAT Test 4 by making registered agent packs flow from run request into sandbox provisioning with deterministic fail-closed checks.

Purpose: Schema-level `agent_pack_id` storage already exists, but runtime pack binding is not verifiable until agent pack identity is propagated and resolved into `SandboxConfig.pack_source_path`.
Output: End-to-end service wiring from run request to orchestrator provisioning, with targeted service tests proving propagation and fail-closed behavior.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-UAT.md
@.planning/debug/phase02-pack-binding-gap.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-03-SUMMARY.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-04-SUMMARY.md
@src/services/run_service.py
@src/services/workspace_lifecycle_service.py
@src/services/sandbox_orchestrator_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate agent_pack_id through run and lifecycle orchestration</name>
  <files>src/services/run_service.py, src/services/workspace_lifecycle_service.py</files>
  <action>Wire `agent_pack_id` from `RunService.execute_with_routing()` into `resolve_routing_target()` and then into `WorkspaceLifecycleService.resolve_target()` and its internal sandbox resolution call. Parse and normalize UUIDs once at service boundary, preserve guest behavior, and keep existing non-pack execution path unchanged. Avoid route-layer branching; keep routing behavior service-driven. Ensure test coverage includes `TestSandboxRoutingIntegration::test_resolve_target_passes_agent_pack_id_to_orchestrator`.</action>
  <verify>`uv run pytest src/tests/services/test_workspace_lifecycle_service.py::TestSandboxRoutingIntegration::test_resolve_target_passes_agent_pack_id_to_orchestrator -q`</verify>
  <done>When `agent_pack_id` is supplied for authenticated runs, lifecycle resolution receives that identifier and forwards it to orchestrator without breaking existing guest/default flows.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve pack source path in orchestrator and fail closed on invalid pack binding</name>
  <files>src/services/sandbox_orchestrator_service.py</files>
  <action>Before provisioning, resolve `agent_pack_id` via repository, enforce workspace ownership and active/usable pack constraints, and map to `SandboxConfig.pack_source_path`. If lookup fails or pack is not valid for the workspace, return deterministic provisioning failure (no provider call, no ambiguous bind). Preserve existing behavior for requests without `agent_pack_id`. Ensure coverage includes `TestHealthAwareRouting::test_resolve_sandbox_populates_pack_source_path_from_agent_pack_id` and `TestHealthAwareRouting::test_resolve_sandbox_rejects_cross_workspace_agent_pack_binding`.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_routing_service.py::TestHealthAwareRouting::test_resolve_sandbox_populates_pack_source_path_from_agent_pack_id src/tests/services/test_sandbox_routing_service.py::TestHealthAwareRouting::test_resolve_sandbox_rejects_cross_workspace_agent_pack_binding -q`</verify>
  <done>Provision requests with valid packs pass `pack_source_path` into `SandboxConfig`, and invalid/cross-workspace pack IDs fail closed before provider provisioning.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused service tests for pack-id propagation and validation gates</name>
  <files>src/tests/services/test_workspace_lifecycle_service.py, src/tests/services/test_sandbox_routing_service.py</files>
  <action>Add or extend service tests to prove: (1) lifecycle forwards `agent_pack_id` to orchestrator, (2) orchestrator populates config with resolved pack source path, and (3) orchestrator blocks invalid/cross-workspace bindings. Use the exact test names referenced in Task 1 and Task 2 verify commands to keep execution deterministic. Keep tests deterministic with local fixtures/mocks and avoid profile-specific behavior in this plan (provider parity is covered in the next plan).</action>
  <verify>`uv run pytest src/tests/services/test_workspace_lifecycle_service.py src/tests/services/test_sandbox_routing_service.py -k "agent_pack_id or pack_source_path" -q`</verify>
  <done>Service-layer tests provide reproducible evidence that pack binding wiring is now present and guarded before provider-specific mount behavior.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_workspace_lifecycle_service.py src/tests/services/test_sandbox_routing_service.py -k "agent_pack_id or pack_source_path" -q` and confirm newly added pack-binding service tests pass with no regressions in existing routing semantics.
</verification>

<success_criteria>
Run/lifecycle/orchestrator path now carries `agent_pack_id` into provisioning, orchestrator resolves and validates pack source path fail-closed, and service tests demonstrate this wiring clearly enough for provider-level parity work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-09-SUMMARY.md`
</output>
