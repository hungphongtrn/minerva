---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/settings.py
  - src/infrastructure/__init__.py
  - src/infrastructure/sandbox/__init__.py
  - src/infrastructure/sandbox/providers/__init__.py
  - src/infrastructure/sandbox/providers/base.py
  - src/infrastructure/sandbox/providers/local_compose.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/infrastructure/sandbox/providers/factory.py
  - src/tests/services/test_sandbox_provider_adapters.py
autonomous: true
must_haves:
  truths:
    - "Switching between `local_compose` and `daytona` profiles preserves the same workspace runtime API behavior for callers."
    - "Equivalent sandbox conditions produce the same user-visible lifecycle states (`ready`, `hydrating`, `unhealthy`, `stopped`) across both profiles."
    - "If sandbox profile configuration is unsupported or invalid, requests fail fast with explicit errors instead of silently routing to the wrong backend."
  artifacts:
    - path: "src/infrastructure/sandbox/providers/base.py"
      provides: "Provider protocol and shared sandbox state contract"
      contains: "class SandboxProvider"
    - path: "src/infrastructure/sandbox/providers/local_compose.py"
      provides: "Local profile adapter implementation"
      contains: "class LocalComposeSandboxProvider"
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "Daytona profile adapter implementation"
      contains: "class DaytonaSandboxProvider"
  key_links:
    - from: "src/infrastructure/sandbox/providers/factory.py"
      to: "src/config/settings.py"
      via: "config-driven provider selection"
      pattern: "SANDBOX_PROFILE|DAYTONA"
    - from: "src/tests/services/test_sandbox_provider_adapters.py"
      to: "src/infrastructure/sandbox/providers/local_compose.py"
      via: "parity assertions for semantic lifecycle outputs"
      pattern: "ready|hydrating|unhealthy|stopped"
---

<objective>
Implement the provider adapter boundary so local Docker Compose and Daytona BYOC can share the same control-plane semantics.

Purpose: AGNT-03 requires profile portability without route-level branching or user workflow changes.
Output: A profile adapter contract, concrete providers, provider factory wiring, and parity-focused adapter tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-CONTEXT.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-RESEARCH.md
@src/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define sandbox provider protocol and shared semantic models</name>
  <files>src/infrastructure/__init__.py, src/infrastructure/sandbox/__init__.py, src/infrastructure/sandbox/providers/__init__.py, src/infrastructure/sandbox/providers/base.py</files>
  <action>Create typed provider protocol(s) and semantic DTOs used by services for sandbox discovery, provisioning, health checks, workspace attach, and stop operations. Keep the contract semantic (state and health fields), not provider-native payload shapes, so route/service code remains provider-agnostic.</action>
  <verify>`uv run python -c "from src.infrastructure.sandbox.providers.base import SandboxProvider; print('ok')"`</verify>
  <done>Services can depend on one adapter interface with no provider-specific branching requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement local_compose and daytona adapter classes</name>
  <files>src/infrastructure/sandbox/providers/local_compose.py, src/infrastructure/sandbox/providers/daytona.py</files>
  <action>Implement both adapters to satisfy the shared provider contract with matching semantic outputs (`ready`, `hydrating`, `unhealthy`, `stopped`). Include idempotent stop behavior and fail-closed handling for unknown health states. For Daytona, support both cloud and self-host base URL/token configuration from settings; do not hardcode cloud-only endpoints.</action>
  <verify>`uv run python -c "from src.infrastructure.sandbox.providers.local_compose import LocalComposeSandboxProvider; from src.infrastructure.sandbox.providers.daytona import DaytonaSandboxProvider; print('ok')"`</verify>
  <done>Both adapters instantiate and expose the full contract methods needed by Phase 2 orchestration services.</done>
</task>

<task type="auto">
  <name>Task 3: Wire provider factory and parity tests</name>
  <files>src/config/settings.py, src/infrastructure/sandbox/providers/factory.py, src/tests/services/test_sandbox_provider_adapters.py</files>
  <action>Add profile selection settings and provider factory logic that chooses adapter implementation by config/profile key. Add tests that verify local/daytona semantic parity for equivalent scenarios and that unsupported profile values raise explicit configuration errors.</action>
  <verify>`uv run pytest src/tests/services/test_sandbox_provider_adapters.py -q`</verify>
  <done>Profile switching becomes an environment/config concern and adapter parity tests pass.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_sandbox_provider_adapters.py -q` and confirm both providers satisfy the same semantic assertions.
</verification>

<success_criteria>
Control-plane services can request sandbox operations through one stable abstraction, with equivalent semantics across local compose and Daytona.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-02-SUMMARY.md`
</output>
