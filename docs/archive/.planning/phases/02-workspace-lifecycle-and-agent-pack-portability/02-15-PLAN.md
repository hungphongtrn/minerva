---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 15
type: execute
wave: 2
depends_on: ["02-13"]
files_modified:
  - src/db/session.py
  - src/db/repositories/workspace_lease_repository.py
  - src/services/workspace_lease_service.py
  - src/tests/integration/test_phase2_lease_contention.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Concurrent same-workspace resolve writes do not hang indefinitely; contention resolves within a bounded time window."
    - "Lease contention outcomes are deterministic and returned as retryable conflict semantics instead of deadlock-like unresponsiveness."
    - "After contention events, the API remains responsive for subsequent requests."
  artifacts:
    - path: "src/db/session.py"
      provides: "DB lock wait safeguards for request/session scope"
      contains: "lock"
    - path: "src/db/repositories/workspace_lease_repository.py"
      provides: "Lock-conflict-aware lease acquisition behavior"
      contains: "acquire_active_lease"
    - path: "src/services/workspace_lease_service.py"
      provides: "Bounded timeout/backoff contract for lease acquisition"
      contains: "acquire_lease"
    - path: "src/tests/integration/test_phase2_lease_contention.py"
      provides: "Concurrency regression proving no hang under simultaneous resolve"
      contains: "concurrent"
  key_links:
    - from: "src/services/workspace_lease_service.py"
      to: "src/db/repositories/workspace_lease_repository.py"
      via: "Lease acquisition retry/timeout loop"
      pattern: "acquire_active_lease"
    - from: "src/db/session.py"
      to: "src/db/repositories/workspace_lease_repository.py"
      via: "Engine/session lock timeout safeguards"
      pattern: "timeout|lock"
    - from: "src/tests/integration/test_phase2_lease_contention.py"
      to: "src/api/routes/workspaces.py"
      via: "Simultaneous POST /workspaces/{id}/sandbox/resolve requests"
      pattern: "sandbox/resolve"
---

<objective>
Close blocker UAT Test 7 by making same-workspace lease contention bounded, conflict-aware, and non-blocking for overall API responsiveness.

Purpose: Concurrent resolve deadlocks are a blocker because they freeze user operations and can starve the service under contention.
Output: Lock-wait safeguards plus deterministic lease timeout/backoff semantics and concurrency regression coverage.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-UAT.md
@src/db/session.py
@src/db/repositories/workspace_lease_repository.py
@src/services/workspace_lease_service.py
@src/api/routes/workspaces.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DB lock-wait safeguards for lease contention paths</name>
  <files>src/db/session.py, src/db/repositories/workspace_lease_repository.py</files>
  <action>Add explicit lock wait protections for lease acquisition (database-specific where needed) so blocked lock acquisition fails fast instead of waiting indefinitely. Ensure lock timeout/operational lock exceptions are caught and translated into retryable conflict outcomes rather than hanging request threads.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_lease_contention.py -k "lock_wait" -q`</verify>
  <done>Contention scenarios no longer depend on unbounded database lock waits.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded lease contention contract in service layer</name>
  <files>src/services/workspace_lease_service.py</files>
  <action>Introduce a deterministic acquisition contract with bounded timeout/backoff (for example short retry loop with max wait), returning `LeaseResult.CONFLICT` plus retry guidance when contention persists. Keep fail-closed behavior for ambiguous states and avoid silent infinite waits.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_lease_contention.py -k "bounded_timeout or conflict" -q`</verify>
  <done>Lease acquisition under contention always returns within bounded time with explicit conflict semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add no-hang concurrency regression for simultaneous resolve requests</name>
  <files>src/tests/integration/test_phase2_lease_contention.py</files>
  <action>Add an integration regression that fires simultaneous same-workspace resolve requests and asserts: one request can acquire/complete, competing request receives conflict or bounded wait outcome, total runtime stays under the configured contention threshold, and follow-up request proves service remains responsive after contention.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_lease_contention.py -q`</verify>
  <done>The prior deadlock/starvation symptom is prevented and covered by repeatable regression tests.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_lease_contention.py -q` and confirm concurrency scenarios complete within bounded time with no request hang.
</verification>

<success_criteria>
Simultaneous same-workspace writes produce deterministic bounded outcomes (success + conflict/retry) and never leave the API unresponsive.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-15-SUMMARY.md`
</output>
