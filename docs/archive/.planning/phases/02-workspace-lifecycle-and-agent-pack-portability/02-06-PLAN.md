---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/routes/workspaces.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Authenticated owners can call `GET /workspaces/me/status` and receive their workspace without UUID coercion failures."
    - "Authenticated owners can call `POST /workspaces/{id}/sandbox/resolve` for their own workspace and receive a routing result."
    - "Non-owners are denied workspace sandbox resolution with HTTP 403 (not malformed-identifier 400 paths)."
  artifacts:
    - path: "src/api/routes/workspaces.py"
      provides: "Workspace ownership checks with explicit UUID normalization and consistent owner-vs-non-owner branching"
      contains: "resolve_sandbox"
  key_links:
    - from: "src/api/routes/workspaces.py"
      to: "Workspace.owner_id"
      via: "principal user_id normalization to UUID before query/compare"
      pattern: "UUID\(|owner_id"
    - from: "src/api/routes/workspaces.py"
      to: "WorkspaceLifecycleService.resolve_target"
      via: "authorized resolve path"
      pattern: "resolve_target"
---

<objective>
Close the route-layer ownership contract bug that blocks legitimate users from workspace status and sandbox resolve flows.

Purpose: Verification and UAT both identified UUID-vs-string ownership checks in workspace routes as the highest-severity blocker for WORK-01/WORK-02 observable behavior.
Output: Normalized workspace authorization in route handlers, no duplicate dead branch in resolve handler, and owner-success/non-owner-forbidden behavior restored.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-VERIFICATION.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-UAT.md
@src/api/routes/workspaces.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize principal identity types in workspace ownership checks</name>
  <files>src/api/routes/workspaces.py</files>
  <action>Introduce a single route-local helper that converts authenticated `principal.user_id` into `UUID` once and raises deterministic HTTP errors for missing/invalid principal identity. Reuse that helper in `get_my_workspace`, `get_workspace`, and `resolve_sandbox` so all ownership queries and comparisons are UUID-to-UUID (never UUID-to-string). Preserve guest restrictions and do not weaken existing 403 behavior for non-owners.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -k "me_status or resolve_sandbox_returns_routing_target" -q`</verify>
  <done>`GET /workspaces/me/status` and owner `POST /workspaces/{id}/sandbox/resolve` return 200 in integration tests; UUID mismatch errors are eliminated from route ownership queries.</done>
</task>

<task type="auto">
  <name>Task 2: Harden resolve endpoint flow and remove duplicate dead branch</name>
  <files>src/api/routes/workspaces.py</files>
  <action>Delete the duplicated unreachable `resolve_sandbox` block that appears after the first try/except return path. Keep one authoritative execution path that: validates workspace UUID, verifies ownership for authenticated users, calls lifecycle resolution, maps lease/conflict errors, and returns response payload consistently. Ensure non-owner access still resolves to HTTP 403 and malformed IDs remain HTTP 400.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_security_regressions.py -k "cannot_resolve_sandbox_for_other_workspace" -q`</verify>
  <done>`resolve_sandbox` has a single non-duplicated logic path, owner requests succeed, and cross-workspace sandbox resolve requests return 403.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_acceptance.py -k "me_status or resolve_sandbox_returns_routing_target" -q` and `uv run pytest src/tests/integration/test_phase2_security_regressions.py -k "cannot_resolve_sandbox_for_other_workspace" -q`.
</verification>

<success_criteria>
Workspace route ownership checks are type-safe and deterministic, fixing the high-severity UAT blocker and restoring owner-visible continuity and sandbox resolve behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-06-SUMMARY.md`
</output>
