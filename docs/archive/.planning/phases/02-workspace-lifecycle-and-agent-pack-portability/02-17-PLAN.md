---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 17
type: execute
wave: 3
depends_on: ["02-11", "02-12", "02-14"]
files_modified:
  - src/services/run_service.py
  - src/api/routes/runs.py
  - src/infrastructure/sandbox/providers/daytona.py
  - src/tests/integration/test_phase2_run_routing_errors.py
  - src/scripts/phase2_profile_parity_harness.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "A registered valid agent pack never returns pack/routing client-error semantics (400/403/404 pack_* or workspace_resolution_failed) in either local_compose or daytona profiles."
    - "For the same valid-pack run request, local_compose and daytona return equivalent semantic outcomes: 201 success or infrastructure-class failure (503/500), not profile-specific routing contract divergence."
    - "Parity automation in CI mode executes both profiles with real credentials and reports PASS only when both profile runs and parity checks pass."
  artifacts:
    - path: "src/services/run_service.py"
      provides: "Deterministic routing error categorization that separates pack validation errors from provider/infra runtime failures"
      contains: "_categorize_routing_error"
    - path: "src/api/routes/runs.py"
      provides: "HTTP mapping that preserves non-400 semantics for valid-pack runtime/provider failures"
      contains: "_map_routing_error"
    - path: "src/infrastructure/sandbox/providers/daytona.py"
      provides: "Daytona SDK error propagation with provider-specific failure context"
      contains: "SandboxProvisionError"
    - path: "src/tests/integration/test_phase2_run_routing_errors.py"
      provides: "Regression coverage for valid-pack parity behavior under both profiles"
      contains: "test_run_does_not_fail_with_pack_error_for_valid_pack"
    - path: "src/scripts/phase2_profile_parity_harness.py"
      provides: "CI parity execution that requires and validates both local_compose and daytona runs"
      contains: "run_ci_mode"
  key_links:
    - from: "src/api/routes/runs.py"
      to: "src/services/run_service.py"
      via: "execute_with_routing result.outputs.routing_error_type -> _map_routing_error"
      pattern: "routing_error_type"
    - from: "src/services/run_service.py"
      to: "src/infrastructure/sandbox/providers/daytona.py"
      via: "lifecycle/orchestrator provider exceptions categorized as infrastructure errors"
      pattern: "provider_unavailable"
    - from: "src/scripts/phase2_profile_parity_harness.py"
      to: "src/tests/integration/test_phase2_run_routing_errors.py"
      via: "profile-switched pytest subprocess under SANDBOX_PROFILE"
      pattern: "SANDBOX_PROFILE"
---

<objective>
Close Truth 11 by eliminating the daytona-only valid-pack 400 routing divergence and proving deterministic cross-profile parity with real-credential CI evidence.

Purpose: AGNT-03 requires the same registered pack to run with equivalent semantics across local_compose and BYOC daytona, so a daytona-only 400 for valid packs is a contract break.
Output: Corrected daytona routing/error classification, locked regression coverage, and a parity harness report that passes for both profiles.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-VERIFICATION.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-14-SUMMARY.md
@src/services/run_service.py
@src/api/routes/runs.py
@src/infrastructure/sandbox/providers/daytona.py
@src/tests/integration/test_phase2_run_routing_errors.py
@src/scripts/phase2_profile_parity_harness.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and correct daytona valid-pack routing error classification</name>
  <files>src/services/run_service.py, src/infrastructure/sandbox/providers/daytona.py</files>
  <action>Reproduce `test_run_does_not_fail_with_pack_error_for_valid_pack` under `SANDBOX_PROFILE=daytona` and trace the exact error message/error_type path from Daytona provider exceptions through lifecycle resolution into `RunService._categorize_routing_error`. Update categorization precedence and provider exception wording/propagation so valid-pack runtime/provider failures are classified as infrastructure (`provider_unavailable` or `sandbox_provision_failed`) and never as pack/client errors (`pack_*`, `workspace_resolution_failed`, generic routing 400). Keep existing pack ownership/validation 4xx semantics unchanged.</action>
  <verify>`SANDBOX_PROFILE=daytona uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "test_run_does_not_fail_with_pack_error_for_valid_pack" -q`</verify>
  <done>Daytona valid-pack path no longer produces a 400-class client routing error; failures, when present, are infrastructure-class and parity-compatible with local semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Harden run-route HTTP mapping and add deterministic parity regression tests</name>
  <files>src/api/routes/runs.py, src/tests/integration/test_phase2_run_routing_errors.py</files>
  <action>Tighten `_map_routing_error` and related run-route error handling to ensure valid-pack runtime failures cannot fall through to generic 400 routing responses. Add/adjust integration tests to assert: (a) valid pack returns only 201/503/500 in both profiles, (b) 4xx pack errors occur only for explicitly invalid/mismatched/not-found pack fixtures, and (c) returned `error_type` is deterministic and profile-equivalent for the same scenario. Ensure tests are runnable profile-by-profile via `SANDBOX_PROFILE` without code edits.</action>
  <verify>`SANDBOX_PROFILE=local_compose uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "valid_pack or parity" -q && SANDBOX_PROFILE=daytona uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "valid_pack or parity" -q`</verify>
  <done>Regression suite proves valid-pack routing contract parity across local_compose and daytona and prevents reintroduction of daytona-only 400 outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce and capture CI parity harness PASS for both profiles</name>
  <files>src/scripts/phase2_profile_parity_harness.py</files>
  <action>Adjust CI mode behavior so parity is considered passing only when both local_compose and daytona profile runs execute and pass (no implicit daytona skip in CI evidence path). Preserve local developer ergonomics in non-CI mode. Run harness in CI mode with `.env` credentials loaded, export a machine-readable report artifact, and ensure output clearly shows local PASS, daytona PASS, and parity PASS.</action>
  <verify>`set -a && source .env && set +a && uv run python src/scripts/phase2_profile_parity_harness.py --mode ci --output .planning/debug/02-17-profile-parity.json`</verify>
  <done>Harness returns exit code 0 only when both profiles pass and parity passes, with persisted report evidence for verification.</done>
</task>

</tasks>

<verification>
Run `SANDBOX_PROFILE=daytona uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "test_run_does_not_fail_with_pack_error_for_valid_pack" -q`, then run `SANDBOX_PROFILE=local_compose uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "valid_pack or parity" -q && SANDBOX_PROFILE=daytona uv run pytest src/tests/integration/test_phase2_run_routing_errors.py -k "valid_pack or parity" -q`, then run `set -a && source .env && set +a && uv run python src/scripts/phase2_profile_parity_harness.py --mode ci --output .planning/debug/02-17-profile-parity.json`; confirm both profiles PASS and overall parity PASS.
</verification>

<success_criteria>
Truth 11 is closed: a valid registered pack no longer yields daytona-only 400 routing failures, deterministic parity tests are green across profiles, and CI harness evidence shows PASS for both local_compose and daytona with real credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-17-SUMMARY.md`
</output>
