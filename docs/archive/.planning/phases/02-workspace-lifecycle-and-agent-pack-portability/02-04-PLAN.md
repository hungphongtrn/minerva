---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/services/agent_scaffold_service.py
  - src/services/agent_pack_validation.py
  - src/services/agent_pack_service.py
  - src/tests/services/test_agent_scaffold_service.py
  - src/tests/services/test_agent_pack_validation.py
  - src/tests/services/test_agent_pack_service.py
autonomous: true
must_haves:
  truths:
    - "Users can scaffold required Picoclaw template artifacts in a workspace folder with no manual file bootstrapping."
    - "Agent pack registration blocks invalid folders and returns a deterministic checklist of missing/invalid scaffold entries."
    - "Path-linked pack metadata stores source digest so source changes can be detected as stale and revalidated."
  artifacts:
    - path: "src/services/agent_scaffold_service.py"
      provides: "Template scaffold creation for AGENT.md, SOUL.md, IDENTITY.md, and skills/"
      contains: "class AgentScaffoldService"
    - path: "src/services/agent_pack_validation.py"
      provides: "Checklist validator and digest computation"
      contains: "class AgentPackValidationService"
    - path: "src/services/agent_pack_service.py"
      provides: "Path-linked registration, update, and stale detection"
      contains: "class AgentPackService"
  key_links:
    - from: "src/services/agent_pack_service.py"
      to: "src/services/agent_pack_validation.py"
      via: "register/validate flow"
      pattern: "validate.*checklist"
    - from: "src/services/agent_pack_service.py"
      to: "src/db/repositories/agent_pack_repository.py"
      via: "upsert registration and source digest persistence"
      pattern: "source_digest|validation_status"
---

<objective>
Implement the template-to-pack workflow core for scaffold creation, checklist validation, and path-linked pack persistence.

Purpose: AGNT-01 and AGNT-02 require an end-to-end folder-based workflow before API and run integration can expose it.
Output: Services for scaffold generation, validation, registration, and stale digest detection with deterministic test coverage.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-CONTEXT.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-RESEARCH.md
@src/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement agent scaffold generator service</name>
  <files>src/services/agent_scaffold_service.py, src/tests/services/test_agent_scaffold_service.py</files>
  <action>Create a scaffold service that initializes required template artifacts (`AGENT.md`, `SOUL.md`, `IDENTITY.md`, `skills/`) under a provided pack path. Enforce safe normalized paths (reject traversal/outside-root writes) and make generation idempotent so re-running scaffold does not corrupt existing valid files.</action>
  <verify>`uv run pytest src/tests/services/test_agent_scaffold_service.py -q`</verify>
  <done>Given a target directory, service produces required scaffold structure and safely handles reruns.</done>
</task>

<task type="auto">
  <name>Task 2: Implement validator with checklist contract and digest computation</name>
  <files>src/services/agent_pack_validation.py, src/tests/services/test_agent_pack_validation.py</files>
  <action>Implement scaffold validation returning deterministic checklist entries (`code`, `path`, `message`, `severity`) and compute stable source digest from pack content for change detection. Validation must classify missing files/directories as blocking errors for registration in Phase 2.</action>
  <verify>`uv run pytest src/tests/services/test_agent_pack_validation.py -q`</verify>
  <done>Validation exposes machine-readable checklist output and digest generation for both valid and invalid scaffold states.</done>
</task>

<task type="auto">
  <name>Task 3: Implement path-linked agent pack registration and stale detection</name>
  <files>src/services/agent_pack_service.py, src/tests/services/test_agent_pack_service.py</files>
  <action>Implement registration/update service that normalizes source path, runs validator, and persists pack metadata through `src/db/repositories/agent_pack_repository.py` (use repository methods for upsert plus `source_digest`/validation status updates; do not bypass repository with direct session writes in service code). Add stale detection comparing persisted digest to current path digest and expose a revalidate operation that refreshes status/report fields through the same repository boundary.</action>
  <verify>`uv run pytest src/tests/services/test_agent_pack_service.py -k "repository or register or stale" -q && uv run pytest src/tests/services/test_agent_pack_service.py -q`</verify>
  <done>Valid packs can be upserted path-linked, invalid packs return checklist without persistence, and tests assert `AgentPackService` uses `AgentPackRepository` for upsert/status/digest persistence and stale-source revalidation.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_agent_scaffold_service.py src/tests/services/test_agent_pack_validation.py src/tests/services/test_agent_pack_service.py -q`.
</verification>

<success_criteria>
Template scaffold, registration validation, and digest-based change detection are implemented and provably deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-04-SUMMARY.md`
</output>
