---
phase: 02-workspace-lifecycle-and-agent-pack-portability
plan: 12
type: execute
wave: 2
depends_on: ["02-11"]
files_modified:
  - src/tests/integration/test_phase2_acceptance.py
  - src/tests/integration/test_phase2_security_regressions.py
autonomous: true
gap_closure: true
replan_reason: "Update integration tests to work with Daytona Async Python SDK instead of HTTP mocks"
must_haves:
  truths:
    - "When the Daytona profile is selected, workspace lifecycle resolution behaves with the same runtime parity expectations as local_compose for equivalent Phase 2 scenarios."
    - "Daytona payloads that are unknown, invalid, or error-state are fail-closed at routing time and never promoted into healthy sandbox selection."
    - "Handling of Daytona SDK-backed responses preserves profile isolation and pack-binding behavior without introducing ambiguous or bypass routing paths."
  artifacts:
    - path: "src/tests/integration/test_phase2_acceptance.py"
      provides: "End-to-end acceptance scenario validating SDK-backed Daytona routing and pack-binding parity"
      contains: "daytona"
    - path: "src/tests/integration/test_phase2_security_regressions.py"
      provides: "Security regression for fail-closed Daytona state/health handling"
      contains: "fail_closed"
  key_links:
    - from: "src/tests/integration/test_phase2_acceptance.py"
      to: "src/services/workspace_lifecycle_service.py"
      via: "Resolve target with Daytona provider configured for SDK-backed responses"
      pattern: "resolve_target"
    - from: "src/tests/integration/test_phase2_security_regressions.py"
      to: "src/services/sandbox_orchestrator_service.py"
      via: "Unknown/unhealthy Daytona payload path blocked from healthy routing"
      pattern: "resolve_sandbox"
    - from: "src/tests/integration/test_phase2_acceptance.py"
      to: "src/tests/integration/test_phase2_security_regressions.py"
      via: "Acceptance + security command evidence for gap closure"
      pattern: "phase2"
---

<objective>
Close the remaining verification risk by adding acceptance and security evidence that Daytona's real SDK-backed adapter preserves Phase 2 behavior and fail-closed guarantees.

Purpose: Replacing simulation at provider level is not complete until phase-level acceptance and SECU-05 evidence explicitly exercise the SDK-backed Daytona path.
Output: Updated integration coverage plus green acceptance/security evidence proving the gap is closed end-to-end.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-VERIFICATION.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-workspace-lifecycle-and-agent-pack-portability-UAT.md
@.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-11-SUMMARY.md
@src/tests/integration/test_phase2_acceptance.py
@src/tests/integration/test_phase2_security_regressions.py
@src/services/workspace_lifecycle_service.py
@src/services/sandbox_orchestrator_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add acceptance test coverage for SDK-backed Daytona runtime path</name>
  <files>src/tests/integration/test_phase2_acceptance.py</files>
  <action>Add/adjust acceptance tests so Daytona profile assertions run through SDK-backed provider behavior (mocked SDK responses, not in-memory state assumptions). Mock `AsyncDaytona` and `Sandbox` objects to return appropriate states. Keep existing registered-pack parity coverage, remove duplicate test-name shadowing in `TestRegisteredPackBindingParity`, and add one explicit assertion that provider metadata/state originates from Daytona SDK response semantics while preserving pack-binding parity with local_compose. Test scenarios: mock `daytona.get()` returns active sandbox, mock `daytona.create()` provisions new sandbox, mock sandbox state transitions are handled correctly.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py -k "daytona and (sdk_backed or RegisteredPackBindingParity)" -q`</verify>
  <done>Acceptance suite contains deterministic evidence that Daytona profile behavior remains correct after moving to SDK-backed interactions, without duplicate/shadowed tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add SECU-05 regression for Daytona fail-closed unknown/error SDK response handling</name>
  <files>src/tests/integration/test_phase2_security_regressions.py</files>
  <action>Add a focused security regression that exercises Daytona provider SDK responses with unknown/error states and verifies routing does not treat them as healthy candidates. Mock `AsyncDaytona.get()` to return sandbox with unknown state, or raise `DaytonaError` exception. Assert fail-closed outcomes (unhealthy exclusion and replacement/provision-failed path), and ensure no cross-workspace or ambiguous routing bypass is introduced by SDK response handling.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_security_regressions.py -k "daytona and fail_closed" -q`</verify>
  <done>Security suite explicitly proves Daytona SDK payload ambiguity/errors cannot bypass health and isolation boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Produce acceptance and security evidence for gap closure</name>
  <files>src/tests/integration/test_phase2_acceptance.py, src/tests/integration/test_phase2_security_regressions.py</files>
  <action>Run targeted Daytona acceptance/security tests first, then run full Phase 2 acceptance and security suites to confirm no regressions from SDK-backed migration. Capture pass counts and relevant test names in the plan summary as closure evidence for the reported gap. Ensure all tests mock `AsyncDaytona` appropriately and verify SDK method calls are made with correct parameters.</action>
  <verify>`uv run pytest src/tests/integration/test_phase2_acceptance.py src/tests/integration/test_phase2_security_regressions.py -q`</verify>
  <done>Phase 2 acceptance and SECU-05 suites are green with explicit evidence tied to Daytona SDK-backed behavior.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration/test_phase2_acceptance.py src/tests/integration/test_phase2_security_regressions.py -q` and confirm green results include new Daytona SDK-backed acceptance and fail-closed security scenarios.
</verification>

<success_criteria>
Integration coverage proves Daytona runtime behavior is SDK-backed and fail-closed under unknown/error payloads, and full Phase 2 acceptance plus security suites pass without portability regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-lifecycle-and-agent-pack-portability/02-12-SUMMARY.md`
</output>

<notes>
Testing patterns for Daytona SDK:
- Mock `AsyncDaytona` class: `mock_daytona = Mock(spec=AsyncDaytona)`
- Mock sandbox object: `mock_sandbox = Mock()` with properties `id`, `state`, `status`
- Common states: `state="running"`, `state="stopped"`, `state="error"`
- Common status: `status="ready"`, `status="initializing"`, `status="error"`
- Mock exceptions: `from daytona.errors import DaytonaError`
- Async mocking: `mock_daytona.get = AsyncMock(return_value=mock_sandbox)`
- Verify calls: `mock_daytona.create.assert_called_once_with(...)`
- Context manager mock: `mock_async_daytona.return_value.__aenter__.return_value = mock_daytona`
</notes>
