---
phase: 01-identity-and-policy-baseline
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - src/api/router.py
  - src/tests/integration/conftest.py
  - src/tests/integration/test_phase1_acceptance.py
  - src/tests/integration/test_phase1_security_regressions.py
autonomous: true
must_haves:
  truths:
    - "Phase 1 acceptance tests prove all listed roadmap success criteria are observable."
    - "Known high-risk regressions (revoked key reuse, cross-tenant leakage, guest persistence, allowlist bypass) are prevented by tests."
    - "Verification commands provide a single green/red signal for phase readiness."
  artifacts:
    - path: "src/tests/integration/test_phase1_acceptance.py"
      provides: "End-to-end acceptance coverage for AUTH-01/02/03/05/06 and SECU-01/02/03"
      contains: ["test_api_key_auth", "test_key_rotate_revoke", "test_workspace_isolation", "test_guest_mode", "test_runtime_policy"]
    - path: "src/tests/integration/test_phase1_security_regressions.py"
      provides: "Regression tests for Phase 1 pitfalls"
      contains: ["revoked key cache staleness", "rls bypass", "guest persistence", "allowlist bypass"]
    - path: "src/tests/integration/conftest.py"
      provides: "Shared fixtures and seeded principals/workspaces"
      contains: ["owner fixture", "member fixture", "guest fixture"]
  key_links:
    - from: "src/tests/integration/test_phase1_acceptance.py"
      to: "src/api/routes/api_keys.py"
      via: "HTTP lifecycle flow assertions"
      pattern: "create|rotate|revoke"
    - from: "src/tests/integration/test_phase1_acceptance.py"
      to: "src/api/routes/workspace_resources.py"
      via: "workspace authorization assertions"
      pattern: "workspace"
    - from: "src/tests/integration/test_phase1_security_regressions.py"
      to: "src/runtime_policy/enforcer.py"
      via: "deny/allowlist behavior checks"
      pattern: "authorize_"
---

<objective>
Validate the complete Phase 1 baseline with end-to-end acceptance and security regression tests.

Purpose: Phase 1 is only done when all goal behaviors are provably observable, not just implemented in isolated units.
Output: Integration fixtures, acceptance test suite mapped to roadmap success criteria, and targeted regression tests for known pitfalls.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-RESEARCH.md
@.planning/phases/01-identity-and-policy-baseline/01-02-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-03-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Phase 1 integration fixtures and seeded identities/workspaces</name>
  <files>src/api/router.py, src/tests/integration/conftest.py</files>
  <action>Finalize API router composition so identity, workspace, and run policy routes are all registered through a single app router. Create reusable fixtures for owner/member users, two distinct workspaces, active/revoked keys, and guest requests. Include helper fixtures for run policy contexts (allowed/denied egress, tools, and secrets) so acceptance tests are readable and deterministic.</action>
  <verify>uv run pytest src/tests/integration/test_phase1_acceptance.py -k "fixture" -q</verify>
  <done>Fixtures allow acceptance tests to compose identity/policy scenarios without inline setup duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Implement acceptance tests mapped to every Phase 1 success criterion</name>
  <files>src/tests/integration/test_phase1_acceptance.py</files>
  <action>Write end-to-end tests that prove: personal API key auth works, rotate/revoke changes outcomes, workspace data isolation is enforced, owner/member role behavior differs, anonymous requests become guest mode and skip persistence, and runtime policy enforces default-deny egress/tool/secret semantics with explicit allow paths.</action>
  <verify>uv run pytest src/tests/integration/test_phase1_acceptance.py -q</verify>
  <done>Each roadmap success criterion has at least one passing acceptance test with clear assertion mapping.</done>
</task>

<task type="auto">
  <name>Task 3: Add security regression suite for known Phase 1 pitfalls</name>
  <files>src/tests/integration/test_phase1_security_regressions.py</files>
  <action>Add focused regressions for: revoked key stale acceptance, cross-tenant access attempts, guest writes to persistent tables, and tool allowlist bypass attempts at alternate callsites. Keep tests explicit about expected denial behavior to prevent silent policy drift.</action>
  <verify>uv run pytest src/tests/integration/test_phase1_security_regressions.py -q && uv run pytest src/tests/integration -q</verify>
  <done>Regression suite passes and fails loudly if any critical identity/policy boundary is weakened.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/integration -q`.
</verification>

<success_criteria>
A single integration suite demonstrates all Phase 1 requirements are behaviorally satisfied and protected against the highest-risk regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-05-SUMMARY.md`
</output>
