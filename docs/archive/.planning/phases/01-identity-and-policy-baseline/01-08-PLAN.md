---
phase: 01-identity-and-policy-baseline
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/run_service.py
  - src/api/routes/runs.py
  - src/tests/runtime_policy/test_guest_and_runtime_policy.py
  - src/tests/services/test_run_policy_enforcement.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Run execution enforces default-deny egress policy before reporting success."
    - "Run execution enforces default-deny tool policy before reporting success."
    - "Denied egress/tool requests return deterministic policy-denied responses."
  artifacts:
    - path: "src/services/run_service.py"
      provides: "Execution path that invokes policy authorizers for requested egress/tool actions"
      contains: ["authorize_egress", "authorize_tool", "status=\"denied\""]
    - path: "src/api/routes/runs.py"
      provides: "HTTP mapping for deterministic 403 policy denials"
      contains: ["HTTP_403_FORBIDDEN", "Policy violation"]
    - path: "src/tests/services/test_run_policy_enforcement.py"
      provides: "Service-level regression tests for default-deny enforcement"
      contains: ["deny all", "allowlist", "deterministic"]
  key_links:
    - from: "src/api/routes/runs.py"
      to: "src/services/run_service.py"
      via: "start_run -> execute_run call chain"
      pattern: "execute_run"
    - from: "src/services/run_service.py"
      to: "src/runtime_policy/enforcer.py"
      via: "authorize_egress and authorize_tool before successful completion"
      pattern: "authorize_egress|authorize_tool"
---

<objective>
Close Gap 3 by enforcing egress and tool allowlist checks in the active run execution path.

Purpose: SECU-01 and SECU-02 require real default-deny enforcement at execution time, not only policy model availability.
Output: Run execution invokes egress/tool checks, returns deterministic denial payloads, and has regression coverage for deny/allow behavior.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-identity-and-policy-baseline-VERIFICATION.md
@.planning/phases/01-identity-and-policy-baseline/01-04-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce egress and tool checks in execute_run before success paths</name>
  <files>src/services/run_service.py, src/api/routes/runs.py</files>
  <action>Thread requested egress/tool intents from run request into execution context and call `authorize_egress`/`authorize_tool` for each requested target before returning success. Keep guest persistence behavior unchanged and keep secret filtering intact. Do not short-circuit policy checks based on guest/auth mode.</action>
  <verify>
Run `uv run pytest src/tests/runtime_policy/test_guest_and_runtime_policy.py -k "egress or tool" -q`.
  </verify>
  <done>
Runs with empty or non-matching allowlists are denied before success response generation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Return deterministic denied responses for egress/tool policy failures</name>
  <files>src/services/run_service.py, src/api/routes/runs.py</files>
  <action>Standardize denied response mapping so policy denials always produce `status="denied"` at service layer and HTTP 403 with stable, parseable detail at route layer. Include action and reason in denial detail to make allowlist misses diagnosable and testable.</action>
  <verify>
Run `uv run pytest src/tests/runtime_policy/test_guest_and_runtime_policy.py -k "denied" -q`.
  </verify>
  <done>
Policy denials are deterministic across egress and tool checks and no longer depend on incidental exception formatting.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for default-deny enforcement in active execution path</name>
  <files>src/tests/runtime_policy/test_guest_and_runtime_policy.py, src/tests/services/test_run_policy_enforcement.py</files>
  <action>Add service-level and route-level tests that prove: empty egress allowlist denies outbound URL requests, empty tool allowlist denies requested tools, explicit allowlist entries pass, and denial payloads are deterministic. Keep tests focused on active `start_run -> execute_run` path so stubs cannot bypass enforcement.</action>
  <verify>
Run `uv run pytest src/tests/services/test_run_policy_enforcement.py -q`, then run `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "DefaultDenyEgress or DefaultDenyTools" -q`.
  </verify>
  <done>
Default-deny egress/tool assertions pass and fail loudly if enforcement hooks are removed from execution.
  </done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/services/test_run_policy_enforcement.py -q` and `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "DefaultDenyEgress or DefaultDenyTools" -q`.
</verification>

<success_criteria>
Runtime execution path actively enforces egress and tool allowlists with deterministic 403 denials when policies do not explicitly allow the requested action.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-08-SUMMARY.md`
</output>
