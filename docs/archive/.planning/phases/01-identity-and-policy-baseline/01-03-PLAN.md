---
phase: 01-identity-and-policy-baseline
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/authorization/policy.py
  - src/authorization/guards.py
  - src/db/rls_context.py
  - src/api/routes/workspace_resources.py
  - src/tests/authorization/test_workspace_isolation.py
autonomous: true
must_haves:
  truths:
    - "Users can only read and write resources inside their own workspace."
    - "Owner and member roles produce different API authorization outcomes."
    - "Cross-tenant DB access is denied by RLS even if handler filtering is incomplete."
  artifacts:
    - path: "src/authorization/policy.py"
      provides: "Owner/member action matrix"
      exports: ["authorize_action"]
    - path: "src/db/rls_context.py"
      provides: "Transaction-scoped DB identity context setter"
      contains: ["set_config", "app.workspace_id", "app.user_id", "app.role"]
    - path: "src/api/routes/workspace_resources.py"
      provides: "Workspace resource CRUD with authz enforcement"
      exports: ["list_resources", "create_resource", "update_resource"]
  key_links:
    - from: "src/api/routes/workspace_resources.py"
      to: "src/authorization/policy.py"
      via: "per-action authorization guard"
      pattern: "authorize_action"
    - from: "src/api/routes/workspace_resources.py"
      to: "src/db/rls_context.py"
      via: "request transaction context"
      pattern: "set_rls_context|set_config"
    - from: "src/db/rls_context.py"
      to: "src/db/migrations/versions/0001_identity_policy_baseline.py"
      via: "matching app.* settings used by RLS policy"
      pattern: "app.workspace_id"
---

<objective>
Implement workspace isolation and role-aware authorization behavior.

Purpose: AUTH-03 and AUTH-05 require both app-level role decisions and DB-level tenant boundaries that cannot be bypassed.
Output: Owner/member authorization matrix, RLS context wiring, workspace resource routes, and isolation tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-RESEARCH.md
@.planning/phases/01-identity-and-policy-baseline/01-01-SUMMARY.md
@.planning/phases/01-identity-and-policy-baseline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build owner/member authorization matrix and reusable guards</name>
  <files>src/authorization/policy.py, src/authorization/guards.py</files>
  <action>Implement role-based authorization for workspace actions (read/write/admin) with explicit owner/member behavior differences. Expose guard helpers that accept principal + action + resource workspace and fail closed by default. Keep authorization checks centralized so route handlers do not duplicate role logic.</action>
  <verify>uv run pytest src/tests/authorization/test_workspace_isolation.py -k "role" -q</verify>
  <done>Role matrix exists in one place and produces deterministic allow/deny decisions for owner/member actions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire transaction-scoped RLS session context for tenant boundaries</name>
  <files>src/db/rls_context.py, src/api/routes/workspace_resources.py</files>
  <action>Set transaction-local Postgres settings (`app.workspace_id`, `app.user_id`, `app.role`) before tenant data queries and ensure route handlers execute inside that context. Align setting names with migration RLS policy expressions and keep context setup mandatory for workspace resource paths.</action>
  <verify>uv run pytest src/tests/authorization/test_workspace_isolation.py -k "rls" -q</verify>
  <done>Workspace resource requests always set RLS context and cross-workspace read/write attempts are blocked by DB policy.</done>
</task>

<task type="auto">
  <name>Task 3: Expose workspace resource endpoints and prove AUTH-03/AUTH-05 behavior</name>
  <files>src/api/routes/workspace_resources.py, src/tests/authorization/test_workspace_isolation.py</files>
  <action>Add workspace resource endpoints with both auth dependency and authorization guards applied. Write tests for same-workspace success, cross-workspace denial, owner/member behavior differences, and a regression showing RLS still blocks cross-tenant access if a route omits tenant filter logic.</action>
  <verify>uv run pytest src/tests/authorization/test_workspace_isolation.py -q</verify>
  <done>API behavior demonstrates tenant isolation and role differences exactly as required by Phase 1 success criteria.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -q`.
</verification>

<success_criteria>
Workspace data access is constrained to the caller's workspace and owner/member authorization differences are observable via API outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-03-SUMMARY.md`
</output>
