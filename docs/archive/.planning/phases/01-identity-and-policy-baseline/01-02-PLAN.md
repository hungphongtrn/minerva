---
phase: 01-identity-and-policy-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/identity/key_material.py
  - src/identity/repository.py
  - src/identity/service.py
  - src/api/dependencies/auth.py
  - src/api/routes/api_keys.py
  - src/api/routes/whoami.py
  - src/api/router.py
  - src/tests/identity/test_api_keys.py
autonomous: true
must_haves:
  truths:
    - "Valid personal API keys authenticate requests and produce a user principal."
    - "Rotated keys invalidate prior key material."
    - "Revoked keys fail immediately on subsequent requests."
  artifacts:
    - path: "src/api/dependencies/auth.py"
      provides: "API key dependency and principal resolution"
      exports: ["resolve_principal"]
    - path: "src/identity/service.py"
      provides: "API key issuance, rotate, revoke, and validation logic"
      contains: ["create_key", "rotate_key", "revoke_key", "validate_key"]
    - path: "src/api/routes/api_keys.py"
      provides: "Key lifecycle endpoints"
      exports: ["create_api_key", "rotate_api_key", "revoke_api_key"]
  key_links:
    - from: "src/api/dependencies/auth.py"
      to: "src/identity/service.py"
      via: "validate_key call"
      pattern: "validate_key"
    - from: "src/api/routes/api_keys.py"
      to: "src/identity/service.py"
      via: "service lifecycle methods"
      pattern: "create_key|rotate_key|revoke_key"
    - from: "src/api/router.py"
      to: "src/api/routes/whoami.py"
      via: "protected route wiring"
      pattern: "whoami"
---

<objective>
Deliver personal API key authentication and lifecycle controls (create/rotate/revoke).

Purpose: AUTH-01 and AUTH-02 are the gateway controls for every protected operation in this runtime.
Output: Auth dependency, identity service, key lifecycle routes, and automated tests for valid/invalid/rotated/revoked flows.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-and-policy-baseline/01-RESEARCH.md
@.planning/phases/01-identity-and-policy-baseline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement secure API key material handling and validation service</name>
  <files>src/identity/key_material.py, src/identity/repository.py, src/identity/service.py</files>
  <action>Implement key generation with `secrets.token_urlsafe`, one-way key hashing, and timing-safe validation with `hmac.compare_digest`. Persist only hashed material and key metadata (prefix, status, revoked timestamp/version). Include rotate and revoke service methods that invalidate prior key material deterministically to prevent stale acceptance.</action>
  <verify>uv run pytest src/tests/identity/test_api_keys.py -k "material or validate" -q</verify>
  <done>Service can create, rotate, revoke, and validate keys without storing plaintext keys in persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Expose API key lifecycle endpoints and protected whoami probe</name>
  <files>src/api/dependencies/auth.py, src/api/routes/api_keys.py, src/api/routes/whoami.py, src/api/router.py</files>
  <action>Create auth dependency that reads `x-api-key`, resolves principal through identity service, and returns 401 for invalid/revoked keys. Add key management routes for create/rotate/revoke plus a protected `/api/v1/whoami` route to verify authenticated principal shape. Ensure route contracts distinguish active vs revoked behavior clearly.</action>
  <verify>uv run pytest src/tests/identity/test_api_keys.py -k "endpoint or whoami" -q</verify>
  <done>Valid key calls succeed, invalid/revoked key calls fail with authorization errors, and lifecycle endpoints mutate key state correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Add AUTH-01/AUTH-02 automated tests with revoke/rotate regressions</name>
  <files>src/tests/identity/test_api_keys.py</files>
  <action>Write tests that cover: successful auth with active key, failure with unknown key, rotate invalidates previous key immediately, revoke blocks subsequent requests, and regenerated key continues working. Include one regression asserting revoked keys cannot pass even if any in-memory cache path exists.</action>
  <verify>uv run pytest src/tests/identity/test_api_keys.py -q</verify>
  <done>All identity auth/lifecycle tests pass and failures clearly identify which contract (active, rotated, revoked) broke.</done>
</task>

</tasks>

<verification>
Run `uv run pytest src/tests/identity/test_api_keys.py -q`.
</verification>

<success_criteria>
API key auth is operational end to end, and rotate/revoke semantics are enforced in API behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-02-SUMMARY.md`
</output>
