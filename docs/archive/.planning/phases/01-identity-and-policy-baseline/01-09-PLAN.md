---
phase: 01-identity-and-policy-baseline
plan: 09
type: execute
wave: 3
depends_on: ["01-07"]
files_modified:
  - src/authorization/policy.py
  - src/tests/authorization/test_workspace_isolation.py
  - src/tests/integration/test_membership_role_behavior.py
  - src/tests/integration/test_phase1_acceptance.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Member role cannot create, update, or delete workspace resources; those mutation attempts return deterministic 403 responses."
    - "Owner/admin/member outcomes differ for workspace resources: owner/admin can mutate, member can read only."
    - "The reported UAT scenario (member POST /workspaces/{workspace_id}/resources) is denied by policy, not by incidental route behavior."
  artifacts:
    - path: "src/authorization/policy.py"
      provides: "Authorization matrix with read-only MEMBER permissions for WORKSPACE_RESOURCE"
      contains:
        ["Role.MEMBER", "ResourceType.WORKSPACE_RESOURCE", "Action.READ"]
    - path: "src/tests/authorization/test_workspace_isolation.py"
      provides: "Policy-level regression assertions for member mutation denials"
      contains: ["can_perform", "Role.MEMBER", "Action.CREATE", "Action.UPDATE", "Action.DELETE"]
    - path: "src/tests/integration/test_membership_role_behavior.py"
      provides: "API-level proof that member mutation attempts are forbidden while read remains allowed"
      contains: ["status_code == 403", "status_code == 200", "status_code == 201"]
    - path: "src/tests/integration/test_phase1_acceptance.py"
      provides: "Phase 1 acceptance coverage for member create denial behavior"
      contains: ["TestRoleBehavior", "member", "403"]
  key_links:
    - from: "src/api/routes/workspace_resources.py"
      to: "src/authorization/policy.py"
      via: "create/update/delete routes call authorize_action(Action.CREATE/UPDATE/DELETE) against WORKSPACE_RESOURCE policy"
      pattern: "authorize_action"
    - from: "src/tests/integration/test_membership_role_behavior.py"
      to: "/api/v1/workspaces/{workspace_id}/resources"
      via: "member POST verifies route-level denial of workspace resource creation"
      pattern: "client\.post\(f\"/api/v1/workspaces/\{workspace_alpha\.id\}/resources\""
---

<objective>
Close the diagnosed UAT gap where member users can create workspace resources.

Purpose: AUTH-05 requires deterministic, observable role divergence; member mutation permissions on workspace resources violate that requirement.
Output: Tightened authorization matrix plus unit/integration/acceptance regressions that fail if member create/update/delete permissions are reintroduced.
Scope: UAT-gap closure only for the diagnosed member-mutation failure on workspace resources; avoid unrelated feature work or broad non-gap verification expansion.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-and-policy-baseline/01-identity-and-policy-baseline-UAT.md
@.planning/phases/01-identity-and-policy-baseline/01-07-SUMMARY.md
@.planning/debug/member-create-resource-gap.md
@src/authorization/policy.py
@src/api/routes/workspace_resources.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten MEMBER workspace resource permissions to read-only</name>
  <files>src/authorization/policy.py</files>
  <action>Update AUTHORIZATION_MATRIX so Role.MEMBER for ResourceType.WORKSPACE_RESOURCE includes only Action.READ. Remove Action.CREATE, Action.UPDATE, and Action.DELETE from member permissions. Keep OWNER and ADMIN permission sets unchanged so privileged mutation behavior remains intact.</action>
  <verify>Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -q`.</verify>
  <done>Policy engine denies member create/update/delete on WORKSPACE_RESOURCE while still allowing member read.</done>
</task>

<task type="auto">
  <name>Task 2: Add policy-level regression tests for member mutation denials</name>
  <files>src/tests/authorization/test_workspace_isolation.py</files>
  <action>Add explicit matrix assertions proving member cannot perform CREATE, UPDATE, or DELETE on WORKSPACE_RESOURCE and keep existing positive assertions for owner/admin mutation capabilities. Include at least one authorize_action path assertion showing member mutation attempts raise HTTP 403 in same-workspace context.</action>
  <verify>Run `uv run pytest src/tests/authorization/test_workspace_isolation.py -q`.</verify>
  <done>Authorization unit tests fail if member mutation permissions are accidentally reintroduced.</done>
</task>

<task type="auto">
  <name>Task 3: Update API-level role behavior tests to match the corrected policy</name>
  <files>src/tests/integration/test_membership_role_behavior.py, src/tests/integration/test_phase1_acceptance.py</files>
  <action>Replace outdated integration expectations that allowed member resource mutation. Add/adjust tests so member POST /workspaces/{workspace_id}/resources returns 403, owner/admin mutation still succeeds, and member READ still succeeds. Extend Phase 1 acceptance role tests with an explicit member create denial check tied to AUTH-05 observable behavior.</action>
  <verify>Run `uv run pytest src/tests/integration/test_membership_role_behavior.py -q` and `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "RoleBehavior" -q`.</verify>
  <done>Reported UAT failure is directly covered by integration and acceptance tests and now produces deterministic 403 outcomes for member create attempts.</done>
</task>

</tasks>

<verification>
Run the gap-focused verification batch only: `uv run pytest src/tests/authorization/test_workspace_isolation.py -q`, `uv run pytest src/tests/integration/test_membership_role_behavior.py -q`, and `uv run pytest src/tests/integration/test_phase1_acceptance.py -k "RoleBehavior" -q`.
</verification>

<success_criteria>
Member requests to create/update/delete workspace resources are denied with 403, owner/admin mutation paths still succeed, and the diagnosed UAT gap for test 4 is closed with regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-policy-baseline/01-09-SUMMARY.md`
</output>
